# PR #108 PRODUCT-002 fix evidence

- Accepted scope: PRODUCT-002 in the product-impact action scope; starting HEAD `c3517f0dbb4346d1182c6bdeb16c0efffc42dd53`.
- Require completed owner synchronization before either selecting an existing PR or creating a new PR. Explicit selection no longer treats a skipped synchronization as successful. Authentication and account reselection remain available; after authentication, retry the complete owner lifecycle boundary before publishing Context/Global/history.
- Actual fixture: `test/unit/pr108-product-002-production.test.ts` with `test/helpers/pr108-production-fixture.ts`; real Git, state/CAS, JSONL history, registration and commands; external VS Code/GitHub mocks only.
- TDD: `product002-durable-red` had 1 pass / 4 fail before the fix. The failures covered new PR partial publication, explicit-selection bypass, existing selection despite incomplete synchronization, and new private PR recovery.
- Green: `product002-production-green`, 18 pass / 0 fail / 0 skip, including all 5 new production tests, the earlier source guard regression, the existing owner composition regression and the 11-test T407 private repository suite.
- Assertions include byte-identical manifest/Context/Global/history on failure, unchanged selection, no owner publication, successful interactive authentication, and whole-owner synchronization before new Context creation.
- Commands: `node /mnt/data/pr108-transpile-local.cjs`; `node tools/run-ci-command.mjs product002-production-green node --test test-dist/test/unit/pr108-product-002-production.test.js test-dist/test/unit/pr108-product-002-new-pr-fail-closed.test.js test-dist/test/unit/t407-private-pr-context.test.js test-dist/test/unit/issue-106-product-composition.test.js`.
- Add the `pr108-product-*.test.js` suite to the existing diagnostic-wrapped Issue106 CI gate, so new product regressions run in required CI. CI is used only for verification and diagnostics, never for code updates.
- Local diagnostics: `test-output/ci/product002-{durable-red,production-green}.*` (stdout/stderr/combined/result JSON).
- Local execution is Node 22.16.0 / TypeScript 5.8.3 transpile-only, not build/typecheck/lint/full-CI equivalence. Current-HEAD CI will be checked separately after final publication.
- Commit and push pending at report generation; the resulting SHA is recorded in the PR summary. No merge or independent review verdict.
