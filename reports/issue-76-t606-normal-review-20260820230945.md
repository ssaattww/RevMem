# T606 normal review report

## タスク

T606 / Issue #76 / draft PR #77 の初回normal reviewを一度だけ全範囲で実施した。review modeは`initial_review`、reviewer identityは`/root/t606_normal_review`（implementationおよびself-fixを担当していないnormal reviewer）、reviewed implementation HEADは`1ab73a5e8902511bafeb9c49402f98d198dda8f1`、baseは`origin/main`の`fb7df6ab79bb23ae16b43b61aa66ab743460be69`、rangeは`fb7df6ab79bb23ae16b43b61aa66ab743460be69...1ab73a5e8902511bafeb9c49402f98d198dda8f1`である。技術verdictはこのreviewed HEADだけに適用する。

## sub-agentを使う理由

使用しない。親の明示指示でsub-agentは禁止されており、このreviewerがIssue、PR、design、tracking、全changed files、直接依存、production consumer、provided validation、既存exact-head CIを一度のfull-scope passで確認した。後続reviewは同一reviewerによる本reportのfinding限定closureだけとし、新規観点を追加しない。

## 対象範囲

Authoritative sourceはIssue #76、draft PR #77、`doc/design/vscode-review-range-tracker-design.md` §16.10/§17/§18、AC-24、`tasks/tasks-status.md`のT606とT403/T601〜T605 contracts、`tasks/phases-status.md`、implementation report/handoffである。全14 changed filesと、operation feedbackの全production consumer、Current Context、PR Progress、Global、Review Contexts、normal/PR diff mark-unmark、startup migration、Git executor/stable-code boundary、GitHub acquisition/cache fallback、state/history/snapshot/cache atomic persistence、storage lock、multi-root routing/registry、package/CI wiringを確認した。

Failure matrixはGit executable missing、timeout、nonzero、corruption、safe.directory、GitHub 401/403/404/429、network、malformed/incomplete response、storage ENOSPC/EACCES/partial write/flush/replace/lock timeout/process interruption、retry classification/backoff/max3/cancellation/permanent/authentication/validation/stale、各UIのactivity/freshness、Output lifecycle/dedup/redaction、root isolationまでproduction経路で照合した。Git stable-code境界、T403 offline fallback、T603/T604 atomic publication/lock、T605 root-scoped storageにはbaseからの破壊的regressionを確認しなかったが、T606 production統合とfocused evidenceには以下のfindingsがある。

## 対象外

実Remote service/network E2E、性能計測/最適化（T607）、初期版全体acceptance/VSIX（T608）、mergeはIssueのnon-goalとして対象外である。test/build/typecheck/lint/architecture/diff-checkは再実行せず、CIも起動・再実行・待機していない。コード、design、README、tasks/phases、handoff、既存report、GitHub、PR、Issue、branch、commit、pushは変更していない。予約済みの当reportだけを更新した。

## 実行コマンド

Read-only evidence collectionとして`git rev-parse`、`git status --short`、`git log`、`git diff --name-status/--stat/--check`、`git diff base...HEAD -- <paths>`、`rg -n`/`rg --files`、`Get-Content`を使用した。GitHubは`gh issue view 76`、`gh pr view 77`、`gh run view 32378477169`、`gh run view 32378470824`と既存failed logだけを参照した。provided local evidenceは`npm run test:t606` 21 Green、`npm run build`、`npm run typecheck:contracts`、`npm run lint`、`npm run validate:architecture`、`npm run validate:architecture:negative`、`git diff --check`成功として評価したが再実行していない。

Markdown word checkはfocused/fullとも`unsupported`である。repositoryに`tools/lint/`、`tools/lint/README.md`、`markdown-targets.json`、`markdown-whitelist.yaml`、`prh.yml`がなく、`package.json`にも`lint:md`がない。ユーザーの再実行禁止にも従いcommandは実行していない。本文を目視し、通常proseをbacktick/quoteでlint回避した箇所は確認しなかった。`unsupported`はpassとして扱わずheldへ記録した。

## 対象ファイル

Changed filesは`.github/workflows/ci.yml`、`README.md`、`handoffs/issue-76-t606-implementation-20260820225743.yaml`、`package.json`、`reports/issue-76-t606-implementation-20260820225743.md`、`src/application/operation-feedback/operation-feedback.ts`、`src/ui/global-understanding/vscode-global-understanding-runtime.ts`、`src/ui/normal-editor/review-command-registration.ts`、`src/ui/review-contexts/vscode-review-contexts-runtime.ts`、`tasks/phases-status.md`、`tasks/tasks-status.md`、`test/unit/ci-workflow-contract.test.ts`、`test/unit/normal-editor-review-command-registration.test.ts`、`test/unit/t606-failure-policy-retry-diagnostics.test.ts`の全14件である。

Direct dependency/consumerとして`src/application/operation-feedback/startup-feedback-composition.ts`、`src/ui/operation-feedback/vscode-operation-feedback.ts`、`src/t305-extension.ts`、`src/extension.ts`、`src/t405-review-contexts-runtime.ts`、`src/t405-pull-request-review-runtime.ts`、`src/t305-projection-refresh.ts`、Current Context/PR Progress/Global UI model、Git/GitHub/cache adapters、review command/history、state repository/atomic store/lock/router、non-Git snapshot/cache storage、workspace root registry、および対応するT403/T601〜T605/CI contract testsとreportsを確認した。

## 指摘事項

- `T606-R001` — severity `high` — origin: normal review; location: `src/application/operation-feedback/operation-feedback.ts:247-261,271-309`、`src/adapters/state-repository/coherent-file-system-review-state-repository.ts:189-205`、`src/adapters/github/fetch-github-pull-request-adapter.ts:154-182`、`src/adapters/github/fetch-github-pull-request-diff-adapter.ts:70-75,290-336`、全production `runWithActiveOperationFeedback` caller。description: failure taxonomy/cancellationはtest用shapeには対応するが、実production contractへ接続されていない。実際の`StaleReviewStateError`は`permanent`になり、GitHub 401/403/429等はadapterで`unavailable.reason`へ畳まれるため`status`判定へ届かない。またproduction retry callerは1件も`AbortSignal`を渡さず、superseded generation中もbackoff/retryが継続する。impact: stale CASをstale UI/diagnosticへ投影できず、authenticationとretryable GitHub failureのtyped policyがproductionで成立せず、古いroot/contextのoperationが不要な再試行を続ける。required action: 実在するtyped error/result（`StaleReviewStateError`、Git timeout、GitHub unavailable/auth/rate-limit、validation）をlosslessに共通分類へ変換し、fallback attempt/final causeを保持する。Global/Review Contexts/Current Context/PR ProgressのgenerationへAbortSignalまたは同等のcancel fenceを接続し、stale/auth/validation/permanentを再試行しないproduction testを追加する。

- `T606-R002` — severity `high` — origin: normal review; location: `src/ui/current-context/current-context-ui-controller.ts:156-181`、`src/ui/current-context/current-context-runtime-coordinator.ts:15-44`、`src/ui/current-context/vscode-current-context-runtime.ts:74-118`、`src/ui/review-contexts/vscode-review-contexts-runtime.ts:124-167,201-227`、`src/t305-extension.ts:464-497,607-610`。description: Current Context refresh/selectはshared operation feedbackを通らず、recompute rejection時に既存Tree/Status Barをclearまたは明示staleへ更新しない。Review Contextsも`source.load()` rejectionではassignment前のitemsをそのまま保持し、stale/unknown markerを付けない。impact: Git/GitHub/storage failure後も以前のcontext/listがfreshに見え、AC-24と設計§17.1/17.3の「直前の確実な状態は明示stale、それ以外はunknown/fail-closed」に違反する。Current ContextはactivityもSTART/ERRORも出ず、failure時のactivity終了を証明できない。required action: 両viewにgeneration-awareなfailure stateを定義し、直前の確実なsnapshotを明示staleとして表示するかclearしてunknownへ戻す。refresh/selectをshared lifecycleに接続し、失敗・supersede・root switchで古い表示をfreshとして残さないproduction seam testを追加する。

- `T606-R003` — severity `medium` — origin: normal review; location: `src/application/operation-feedback/operation-feedback.ts:383-486,435-447,515-543`、`src/t405-review-contexts-runtime.ts:692-710,878-885`、`src/t405-pull-request-review-runtime.ts:422-447`。description: Output lifecycleが全consumerでSTART/OK/ERROR一対にならない。`reportActiveOperationFailure`によるPR Progress/GitHub fallbackはwrapper外でterminal ERRORだけを追加し、storage lockもterminal OK/ERRORだけを追加する一方、外側のReview Contexts operationはOKで終了し得る。Current ContextはR002のとおりlifecycle自体がない。impact: 一つのobservable flowについてSTARTなしERRORまたは内側ERRORと外側OKが混在し、attempt/final cause、duration、activity completionをoperation単位で追跡できない。dedupの正しさもwrapperと実UI境界全体では証明されない。required action: operation identityを共有する一つのlifecycleへ各terminal diagnosticを結合し、各observable activityにつきSTART一回とOKまたはERROR一回だけを出す。fallback attempt列、storage lock timeout/recovery、同一Errorのwrapper/UI伝播をconcrete hostでassertする。

- `T606-R004` — severity `medium` — origin: normal review; location: `src/ui/review-contexts/vscode-review-contexts-runtime.ts:201-227,257-262`、`src/t405-review-contexts-runtime.ts:260-310,714-737`。description: `runOperation`はdefaultでretryを有効化し、`openReviewContextDiff`も明示disableせず最大3回の対象になる。また`provider.refresh()`はpure readではなく`source.load()`内でPR metadata/context persistence同期を行う。impact: open/editor side effectが部分的に成功してからretryable errorになった場合に同じ操作を再実行し、multi-root refreshでは先行rootの同期後に後続rootが失敗するとstateful refresh全体を再実行する。Issueの「idempotent read/refreshだけをretryし、mutation/partial successを再実行しない」境界が保証されない。required action: remote/cache取得など副作用前のidempotent readだけをretry boundaryへ切り出し、diff open、Quick Pick、persistence同期、decoration/config mutationは一度だけ実行する。partial-side-effect後のretryable failureを固定するtestを追加する。

- `T606-R005` — severity `high` — origin: normal review; location: `package.json:148`、`test/unit/t606-failure-policy-retry-diagnostics.test.ts:24-76`、`test/unit/ci-workflow-contract.test.ts:251-259`。description: focused `test:t606`はT606 helper 4 tests、normal command tests、CI文字列contractだけで、Issueのfull failure fixture/production composition acceptanceを満たさない。production boundary testも4ファイルの正規表現存在確認であり、Git nonzero/corruption/safe.directory、GitHub 401/403/404/429/network/malformed/incomplete fallback、storage partial/flush/replace/lock/process interruption、Current/PR/Global/Review Contexts freshness、multi-root isolationを実行しない。T403/T604/T605 suitesもfocused commandに含まれない。impact: 21 Greenはfailure matrixやproduction activationを証明せず、R001〜R004とexact-head regressionを検出できなかった。required action: productionと同じcomposition/factory chainで全matrixを実行するfocused suiteへ置き換え、必要な既存T403/T604/T605 contract suitesも重複実装せず同じ`test:t606`から必須実行する。package/CI contractはsuite名だけでなく必要fixture群を固定する。

- `T606-R006` — severity `medium` — origin: exact-head CI evidence; location: GitHub Actions runs `32378477169`（pull_request）/`32378470824`（push）、HEAD `1ab73a5e8902511bafeb9c49402f98d198dda8f1`、`test/unit/review-contexts-runtime-wiring.test.ts:113-116`。description: 両current-head runはUnit testsで541 pass/1 failとなり、`Issue #63 lets operation feedback observe Review Contexts failures before UI reporting`のwiring contractが変更後のcall shapeと一致しない。後続T602〜T606、Git/GitHub、Extension Host stepsは全skipされた。impact: reviewed HEADにGreen CI evidenceがなく、T606 focused CI stepを含むfull workflowは実行されていない。required action: public/production behaviorを先にR001〜R005へ合わせ、その正しいcontractを既存wiring testへ反映し、全finding fix後の新exact-headでdefault Unitと全required CIをGreenにする。

- `T606-R007` — severity `medium` — origin: report/tracking/documentation review; location: `README.md:26,60`、`reports/issue-76-t606-implementation-20260820225743.md:13,29,33-37`、`tasks/tasks-status.md:366,390`、`tasks/phases-status.md:34,40,186`、PR #77 body。description: repository evidenceは「全activity failure時終了」「fail-closed UI」「cancellable retry」「production composition」「focused full failure fixture」を完了として記載するが、R001〜R005とcurrent-head CI failureが存在する。implementation reportはCI evidenceなしと記録した後にexact-head failureが得られたため、review時点の実態とも不一致である。impact: 次reviewer/merge gate/利用者が未実装・未検証のfailure保証を完了済みと誤認する。required action: R001〜R006修正とvalidation後にREADME、implementation/follow-up report、handoff、tasks/phases、PR validation evidenceを同じHEADの実態へ同期し、未実施/unsupported/failed evidenceをsuccessへ変換しない。

## 結果

Verdictは`fail`。required findingsはHigh 3件（T606-R001、R002、R005）、Medium 4件（T606-R003、R004、R006、R007）である。severity reclassificationはなく、全findingはこの初回normal reviewで新規確定した。

Criterion disposition:

- requirement/design conformance: `checked_finding`（R001〜R005）。
- Git missing/timeout/nonzero/corruption/safe.directoryとstable-code boundary: `checked_finding`（既存stable-code分離は維持、production retry/classification/cancelはR001、focused fixturesはR005）。
- GitHub 401/403/404/429/network/malformed/incomplete fallback/attempt/final cause: `checked_finding`（T403 fallback自体のregressionなし、production taxonomy/lifecycle/testはR001/R003/R005）。
- storage ENOSPC/EACCES/partial write/flush/replace/lock/process interruption/atomicity: `checked_finding`（T603/T604 atomic publicationとroot lockのdiff regressionなし、unified lifecycle/focused証拠はR003/R005）。
- retry classification/backoff/max3/cancellation/permanent/authentication/validation/stale: `checked_finding`（R001/R004）。
- PR Progress/Global/Review Contexts/Current Context/mark-unmark activity completionとfreshness: `checked_finding`（Global clear、PR Progress clear、mark/unmark non-retryは確認、Current/Review Contextsと全lifecycleはR002/R003）。
- Output START/OK/ERROR once、dedup、bounded single-line redaction: `checked_finding`（allowlisted/generic redactionと単一boundary dedupは確認、全consumer lifecycleはR003、full evidenceはR005）。
- multi-root isolation: `checked_finding`（T605 root-scoped route/registryのimplementation regressionなし、T606 retry/cancelとfocused isolation evidenceはR001/R005）。
- production activation/composition: `checked_finding`（startup host、Global/Review Contexts/mark-unmark接続の一部は確認、R001〜R005）。
- public API/backward compatibility: `checked_no_finding`（新規exportとoptional retry parameterはadditive、breaking change記録を要する既存契約変更は確認なし）。
- security/privacy: `checked_no_finding`（shell不使用、raw dependency message/stack/source/token/path/titleをOutputへコピーしないallowlist projectionを確認。実Remote serviceはnon-goal）。
- scope discipline/unrelated changes: `checked_no_finding`。
- tests/package/CI wiring/evidence: `checked_finding`（R005/R006）。
- reports/tracking/README/PR accuracy: `checked_finding`（R007）。
- Markdown word check: `held`（repository wiring不在でfocused/fullともunsupported。自動用語検査漏れはowner明示のnon-code follow-up）。
- 実Remote service/network E2EとT607/T608: `not_applicable`（Issue明示non-goal）。

Provided evidence assessment: local focused 21 Green、build、contract typecheck、ESLint、architecture正負、diff-checkは当時の実行証拠として受領する。ただしfocused範囲不足とexact-head CI 541/542 failureを上書きしない。current-head CIは二runともfailureである。unexploredは`none`。next actionはimplementation ownerがT606-R001〜R007を同一batchで修正し、required local validationと新exact-head CIを取得した後、この同一normal reviewerへfinding-limited closureを依頼することである。

## リスク

HeldはMarkdown word check `unsupported`のみで、passへ変換していない。実Remote service/network E2E、性能、VSIX/full acceptanceはIssueの明示non-goalであり、当review verdictを追加でblockするunexplored areaではない。unexploredは`none`。

残存riskは、現状のretryがsuperseded/root-switch後も走ること、古いCurrent/Review Contexts表示がfreshに見えること、terminal-only診断でoperation因果が曖昧になること、focused 21 Greenが実production matrixを代表しないこと、exact-head CI後続gateが未実行なことである。mergeは禁止され、report persistenceは通常review用repository fileでありreport-attestation commitではない。
