# Sub-agent実行レポート

## タスク

- 目的: PR #94のexact-head required CI失敗原因と、恒久実装・一時生成物・不足実装の境界を確定する。
- タスク種別: requirement / CI intake investigation

## sub-agentを使う理由

- 理由: requirement・CI診断は独立したTerra/high担当がworkspaceとGitHub evidenceを直接照合するため。

## 対象範囲

- 対象: PR #94、Issue #92、base `main`、HEAD `1171bb9132ddd72c263715bd5beb605137a69da2`、required pull_request CI `33376611286`、PR差分全体の分類。

## 対象外

- 対象外: 実装、commit、push、merge、CI再実行、performance CI追加、PR外の変更。

## 実行コマンド

- 実行コマンド:
  - `git status --short; git branch --show-current; git rev-parse HEAD; git log --oneline --decorate -12; git diff --name-status`
  - `gh pr view 94 --repo ssaattww/RevMem --json ...`、`gh issue view 92 --repo ssaattww/RevMem --json ...`
  - `gh run view 33376611286 --repo ssaattww/RevMem --json ...`、`gh run view 33376611286 --repo ssaattww/RevMem --log-failed`
  - `git diff --name-status 017e5aeebadbd8b676f72af6791ca455b926c55d..1171bb9132ddd72c263715bd5beb605137a69da2`、`git log --all -- <対象path>`、`git show <commit>:<path>`
  - `rg` と `Get-Content` による `.github/issue-92-*.py`、Issue #92 tests/design/reports、`tasks/*.md`、`.github/workflows/ci.yml` の読取り。

## 対象ファイル

- 変更または確認したファイル:
  - 恒久product（一部のみ実体あり）: `src/ui/pr-progress/pr-progress-diff-review-context.ts`、`src/ui/pr-progress/vscode-pull-request-progress-tree.ts`。
  - 壊れた／未実体化のproduct: `src/application/review-commands/original-selection-review-plan.ts`、`package.json`。後者は全191行を1行に圧縮した上で、selectionの2 commandと新規test registrationを欠く。
  - 意図された契約test: `test/unit/core-contracts.test.ts`、`diff-editor-review-command-service.test.ts`、`issue-92-pr-progress-context-menu.test.ts`、`issue-92-pr-progress-selection-review.test.ts`、`original-diff-selection-projection.test.ts`、`review-history-original-side.test.ts`、`review-state-service.test.ts`、`t405-pull-request-review-runtime.test.ts`。
  - 意図されたdesign/report: `doc/design/immutable-revision-review-snapshots.md`、`doc/design/vscode-review-range-tracker-design.md`、Issue #92名のreports（`2026-08-30-*`、`2026-08-31-issue-92-immutable-revision-review-snapshots.md`）。
  - 削除／復元対象: `.github/connector-*-probe.tmp`、`.github/issue-92-*.py`、`.github/issue-92-option-b-good.tar.gz`、`.github/issue-92-option-b-parts/*`、`.github/workflows/issue-92-*.yml`、rootの `__*__` probe files、`reports/__connector_file_input_test__.md`、`reports/2026-08-31-issue-92-ci-repair.md`、`reports/2026-08-31-issue-92-exact-head-ci-trigger.md`。

## 指摘事項

- 指摘要約または「指摘なし」:
  - **Blocking — exact HEAD CI failure:** PR #94はDraft/open、baseは`main`の`017e5aeebadbd8b676f72af6791ca455b926c55d`、current headは正確に`1171bb9132ddd72c263715bd5beb605137a69da2`である。required `pull_request` CI run `33376611286`は同じ`headSha`で結論は`failure`、merge checkoutは`7c2191dcac4b1ab8cf328b321ccf4401d220fb3f`である。最初の失敗job/stepは`build-and-lint` / `Build`、`src/application/review-commands/original-selection-review-plan.ts(1,34)`の`TS1351`である。このfileはTypeScriptではなくローカルLinux path（`/mnt/data/.../original-selection-review-plan.ts`）1行であり、後続checkはすべてskipされた。
  - **Blocking — product実装がcommitされていない:** Issue #92はPR Progressのexact diff tabで4 review操作を公開し、original context行をimmutable hunk座標で写像、削除／置換前行をcurrent `baseSha..headSha`へ保持、Context/Global/Originalを1 transaction、stale/non-PR/別tabを拒否、exact revision snapshotで`A -> B -> C -> A`を復元することを要求する。2つのPR Progress UI fileはtab-provenance境界を実装するが、plan module、command-service/runtime結線、snapshot model/mapper/store/service、snapshot testはcommit済みproduct treeにない。Issue #92 testsも欠落結線（`createOriginalSelectionReviewPlan`、`commitTransactionSequence`、`originalToModifiedLineMappings`）を明示している。
  - **`package.json`は破損かつ不完全:** commit `86c1e24a7b7bc8750a171f08f123a1997bf96702`は整形済み191行manifestをblob `86d1651...`（JSON 1行）にし、whole-file menu 2件だけを変更した。現在のparsed manifestでは`markSelectionReviewed`/`unmarkSelectionReviewed`が`!isInDiffEditor`のままで、`test:unit`に`original-diff-selection-projection`、`issue-92-pr-progress-selection-review`、`immutable-revision-review-snapshot`はいずれも未登録である。整形済みbase manifestを復元後、4 command predicateと必要test entryを意図的に適用する必要がある。
  - **意図された本文は有効なcommit候補ではなくpayload証跡のみ:** `.github/issue-92-option-b-repair.py`は完全なplan moduleと`src/application/review-commands/{index,diff-editor-review-command-service}.ts`、`src/t405-pull-request-review-runtime-base.ts`、manifestへの変換を定義する。`.github/issue-92-final-fixes.py`はexact-revision snapshot結線と登録を追加定義し、対応するrevision-snapshot scriptsに残りのsource/test payloadがある。workflowは生成変更をcommitする意図だったが、そのproduct commitは存在しない。これらscript/workflowを恒久実装として残す／実行してはならない。唯一のcommit済みmodule blob（`e5df3ed`で導入された`96bd5f3...`）も同じ無効path placeholderである。
  - **分類:** 実体のある2 UI product file、Issue #92 design doc、記名Issue #92 contract test/reportは保持して再検証する。snapshot design scopeは意図されたが未実装として扱う。worker script、payload archive/parts、Issue #92 temporary workflow、connector/probe file、path-only reportは削除する。`reports/2026-08-31-issue-92-ci-repair.md`と`reports/__connector_file_input_test__.md`はbare `/mnt/data/...`参照でありdurable reportではない。exact-head trigger fileも完了証跡でなくtemporary workflow proseである。既存`.github/workflows/ci.yml`はこのPRで未変更で、stdout/stderr/combined-log failure artifactを既に提供する。
  - **Tracking/TDD gap:** `tasks/tasks-status.md`、`tasks/phases-status.md`のいずれもIssue #92/PR #94、repair、exit criteriaを追跡していない。governing repository ruleは`tasks/phases-status.md:10`で、behavior変更では実装前に失敗testを追加し、実装後にexit conditionを証明する。`tasks/tasks-status.md:40`はIssue #90だけを指すため、Issue #92をgovernできない。

## 結果

- 結果:
  - Requirement/CI intakeを完了した。product、test、design、workflow、tracking、GitHub、CI stateは変更しておらず、writeは本reportのみである。
  - Node/npm package scriptsと完全なCI command chainが定義済みのためlocal executionは利用可能である。本調査ではvalidationを実行していない。後続commandはfocused Issue #92 test付きの`npm run compile:test`、次に`npm run build`、`npm run typecheck:contracts`、`npm run validate:architecture`、`npm run validate:architecture:negative`、`npm run lint`、`npm test`、`git diff --check`である。performance CIは追加・実行しない。
  - 最小sequential repair plan（各slice <=0.5h）:
    1. 0.5h: tracked Issue #92 repair scopeを作り、probe/worker/payload/temporary workflow/reportだけを削除／復元し、baseから整形済み`package.json`を戻したうえで4 command/test registrationを明示適用する。既存CIは維持する。
    2. 0.5h: TDD Red後、調査済みpayloadからplan moduleとindex exportを実体化し、focused projection/menu compile testをGreenにする。
    3. 0.5h: command-service/runtimeのatomic original-side projection結線を実体化・testし、stale-tab/tab-identity境界を維持する。
    4. 以降各0.5h: payload scriptを実行せず、design要求のsnapshot contract/service/mapper/storeを依存順にRed/Greenで実装し、実証後にのみtrackingとdurable reportを更新する。
    5. 0.5h: final candidateに非performance validation chainを実行し、新しいmatching `pull_request` CI runを取得して別途reviewする。
  - 推奨する最初のbounded implementation sliceはitem 1、続けて`original-selection-review-plan.ts`を狭くRed/Greenで復元すること。unaudited automationをproduct codeとして受容せず、即時syntax blockerを解消できる。

## リスク

- 未解決のリスクまたは後続対応:
  - GitHub記録上のPR baseは`017e5ae...`である一方、live `origin/main`は`67ac398...`まで進んでいる。rebase/merge-base解決は後続の明示的integration判断であり、本調査では推定していない。
  - payload scriptsには広範囲で未reviewの実装とcommit/push workflowが埋め込まれている。本文抽出は証跡のみであり、丸ごと適用すれば無関係変更の再導入、TDD迂回、temporary automation残置の危険がある。
  - 意図されたsnapshot featureは元のIssue titleより実質的に大きく、対応するtracked taskがない。このscopeをmaintainerが受入／分割するまで、最初のCI syntax failureを直しただけでIssue #92 repair完了とはできない。
  - 既存reportは古いSHAのGreen runを主張するが、current exact-head evidenceはfailure run `33376611286`である。過去runは代替できない。
