# Review report

## Review type

Issue #81 / PR #82 / T609 の same-normal-reviewer bounded CI-delta R2 verification。reviewerは `/root/issue81_normal_review` で、実装ownerおよびindependent reviewerとは別である。exact-head pull-request CIで観測されたT607 IFR004 exported activation fixture failureと、`625a7c4...cd7c0aa` のtest-only deltaだけを検証した。初回review、新規観点、新規finding、severity変更はない。

## Target identity

Repositoryは `ssaattww/RevMem`、branchは `task/issue-81-repository-encoding`、baseは `3bba5defe32b7da134817492427e09c70c97beaf`、CI failureで無効となったold attestation HEADは `625a7c499926ab9772f9895caf39c97c9fe4e943`、今回のreviewed frozen HEADは `cd7c0aa8a7076ec8034e9a9cd1862c2cec1c4675` である。local HEAD、upstream、指定frozen HEADは一致した。CI run `32563955876` / job `97009699173` はT405/T609をpass後、T607 IFR004 fixtureの `document.encoding` undefinedでfailedしたため、old attestationはinvalidかつnon-reusableである。

## Scope

Scopeは、`test/unit/t607-performance-incremental-ui.test.ts` のfake TextDocument `encoding` contract、production `toDocumentDescriptor` のoptional `encodingHint`分岐、exported activation compositionのfixture fidelity、test-only scope discipline、provided Red/Green/T607/static evidence、observed T405と `T609-IFR001`〜`IFR006` の直接非回帰に限定した。production、public API、data/config/workflow、design、tracking、handoff、PRの編集、test/build/lint/Host/CI実行・待機、commit、push、mergeは対象外である。

## Evidence reviewed

Authorityとしてfailed CI run/job identity、old independent CI-delta closure、`reports/issue-81-t609-ci-followup-r2-20260822181141.md`、`625a7c4...cd7c0aa` の2 changed files、`src/extension.ts:350-394`、local `@types/vscode/index.d.ts:119-140` のTextDocument encoding contract、T607 exact fixtureとpackage/CI wiring、tasks/phases/current handoffをread-onlyで確認した。deltaはfake documentへ `encoding: ""` を1行追加する。Productionは `document.encoding.length === 0` ならdescriptorへ`encodingHint`を付けず、非空ならその値を渡す。VS Code contractは`encoding`をrequired stringとし、現在の可能値として`utf8`、`utf8bom`、`shiftjis`等を列挙するが、empty stringは列挙しない。Provided evidenceはexact Red 0/1→Green 1/1、`test:t607` 81/81、build/lint/diffcheck passで、full/Host/current exact-head CIはない。

## Finding dispositions

Observed T607 CI failureは **incomplete / not ready** とした。`encoding: ""` はundefined TypeErrorを解消し、productionのoptional no-hint branchを正しく選ぶが、actual VS Code TextDocumentで文書化されたencoding値を表さない。このためGreen evidenceはsynthetic empty sentinelでexported factoryを通ることを証明する一方、「actual TextDocument fixture fidelity」と、actual non-empty encodingが`encodingHint`へ伝播して同じT607 compositionを維持することを証明しない。これは指定されたR2 criterionの不足であり、新規finding identity/severityは作成しない。Observed T405/T406 duplicate-error CI failureはclosed維持。`T609-IFR001` High、`IFR002` High、`IFR003` High、`IFR004` Medium、`IFR005` Medium、`IFR006` Lowも、production deltaがないため全件closed維持する。

## Validation assessment

Scope discipline、production/API/data/config/workflow非変更、T405とIFR001〜006非回帰、T607 package/CI wiring、optional empty encoding behaviorは `checked_no_finding` とした。Actual TextDocument fixture fidelityとactual non-empty encoding propagation evidenceは `checked_finding / incomplete matrix` である。Exact Red 0/1→Green 1/1と`test:t607` 81/81はprovided execution factとして受理するが、invalid actual encoding valueによるpassをactual-runtime fidelityへ拡張しない。build/lint/diffcheck passもprovided evidenceとして受理した。full/Host/current exact-head CIはpassへ変換しない。本reviewでtest/build/lint/Host/CIを実行・待機した回数は0である。

## Held items

Non-blocking heldは、full local equivalence/Extension Host未再実行、repo-local wiring不在でunsupportedのMarkdown wording check、reviewed HEAD `cd7c0aa` に一致するpull-request CI未実行である。tasks/phases/handoffはold attestation `625a7c4` のCI failureとcurrent R2 deltaをまだ反映せず、fresh closure前に親の同期が必要である。これらはactual fixture fidelityのnormal-path blockerと分離する。user-confirmation-required capability gapはない。

## Unexplored

`unexplored = 0`。CI failure、2 changed files、TextDocument API contract、empty/non-empty optional encoding分岐、exported activation fixture、T607/package/CI wiring、scope、productionと既存closure非回帰、provided validation、tracking状態を処分した。full/Host、Markdown、current exact-head CI、tracking同期はunexploredへ隠さずheldとして明示した。

## Verdict

**Verdict: INCOMPLETE.** Observed T405/T406 failureと `T609-IFR001`〜`T609-IFR006` はclosedを維持し、production regressionはない。しかしobserved T607 CI failureのclosureは、actual TextDocument fixtureへ文書化されたnon-empty encodingを設定し、その`encodingHint`伝播を同じexported activation compositionで確認するまでreadyではない。Optional no-hint behaviorも必要なら、actual fixtureとは分離したsynthetic seam evidenceとして保持する。修正とfocused/T607/static evidenceを同じnormal reviewerへ戻した後に、同じindependent reviewerのCI-delta closure、新attestation、exact-head CIへ進む。old attestation `625a7c4` は再利用せず、本reviewはmergeを許可しない。
