# T207 通常レビューレポート

## メタデータと対象identity

- report type: normal review / initial review
- reviewer: T207実装担当から独立したreviewer 1/2。本レビューでは実装・finding修正を行っていない
- repository: `ssaattww/RevMem`
- branch: `task/t207-git-history-integration`
- base: `de21d050b3f82438c1962d0326664057fb8ee1d2`
- reviewed implementation HEAD: `19d3130ed83a72471dc0cabde512b7388d682c2c`
- range: `de21d050b3f82438c1962d0326664057fb8ee1d2..19d3130ed83a72471dc0cabde512b7388d682c2c`
- persistence mode: repository file
- reserved report path: `reports/issue-1-t207-review-20260802013500.md`
- verdict: `fail`

## 目的・scope・non-goals

- scope: T207差分6file、temporary Git repositoryでのedit、commit、branch、rename、delete、restart、state/history整合、AC-07〜AC-10・AC-12、末尾EOL production fix、Windows cleanup retry、package script、tracking/report。
- authoritative requirements: `tasks/tasks-status.md`のT207終了条件、`doc/design/vscode-review-range-tracker-design.md` 2.1、10.1〜10.5、15.1〜15.4、20.2〜20.4、21章、および`doc/design/document-context-routing.md` 5.1、6.6〜6.7。
- non-goals: T206以前の承認済み差分、Issue #28の修正、無関係なcleanup、実装、commit、push、PR、merge、独立最終review。
- write boundary: 本レポートだけ。実装、他report、tracking、design、workflow、commit等は変更していない。

## 確認した変更file・直接依存

- 変更6file: `package.json`、`reports/issue-1-t207-implementation-20260802011000.md`、`src/application/review-context/git-context-revision-mapper.ts`、`tasks/tasks-status.md`、`test/integration/t207-git-history.integration.test.ts`、`test/support/temporary-directory.ts`。
- 直接依存: `src/extension.ts`、document/Git-context session provider、Git file-state transition validator、history recorder/JSONL store/storage router、temporary Git fixture、CI workflow、関連設計とtracking。
- scope discipline: T207の統合test、support、最小production修正、script、report/trackingに限定され、無関係な変更は検出しなかった。
- coding standards: 新規public/protected production APIはなく、既存public test supportのXML documentationも維持されている。命名・visibility・lint上の追加findingなし。nested agent禁止のためcoding-standards Skillの委譲手順は実行せず、本reviewerが直接確認した。

## Findings

### T207-R1-P1 — high — introduced_by_change

- location: `src/application/review-context/git-context-revision-mapper.ts:28-35`、concealing test helper=`test/integration/t207-git-history.integration.test.ts:25-32`、consumer=`src/extension.ts:257-264`、invalidator=`src/adapters/document-review-state/document-review-state-session-provider.ts:589-593,685-703`
- description: 新しい`lineCountOf`は空本文を0行、末尾EOL付き本文を終端空行なしとして数える。一方production descriptorはVS Codeの`editor.document.lineCount`をそのまま使用し、空documentは1行、`"base\nedited\n"`は3行として扱う。revision mapperは同じ本文をそれぞれ0行、2行として永続化するため、mapping直後に通常editorを開くとstrictな`lineCount`比較がstaleと判定し、確認済みfile stateを削除する。T207 testはproduction修正と同じ非VS Code規約をhelperへ複製したため、この不整合を隠している。
- impact: 末尾改行を持つ一般的なtext fileと空fileで、commit/branch/rename後に確実にmappingされた確認済み範囲が次回openで`invalidated-by-edit`として失われる。AC-07〜AC-09、AC-12、restart整合、および「変更箇所だけ未確認化」に反する。
- evidence: `"base\nedited\n"`に対し、production descriptor相当のsplit行数は3、新mapperは2で`staleByProvider=true`。focused T207 testがpassするのはtest helperも2を返すためである。旧実装のsplit行数はproduction descriptor規約と一致していた。validator側のfull-text evidenceは物理行を数える別責務であり、その都合を永続化/editor行数へ移したことが原因。
- required action: editor/persisted stateのcanonical line-count contractをVS Code `TextDocument.lineCount`と一致させる。Git full-text evidenceの物理行数検証は別値として扱い、test descriptorを実production規約へ直す。末尾EOLあり・なし・空本文について、revision mapping後の同一openとrestartで範囲が消えないRed/Green testを追加する。

### T207-R1-P2 — high — insufficient_acceptance_evidence

- location: `test/integration/t207-git-history.integration.test.ts:101-140,142-168,181-207`、claim=`reports/issue-1-t207-implementation-20260802011000.md:37-45`、exit criterion=`tasks/tasks-status.md:221`
- description: scenarioはAC-07〜AC-10・AC-12とstate/history整合を満たしたと報告するが、assertionがその契約を実証していない。変更前は未変更行0だけを確認済みにし、行1を編集した後にfile state全体が`undefined`であることを期待するため、editの「変更部分だけ未確認」を反対に全消去で受理する。commit後も変更行を事前に確認済みにしていないので、変更行だけが解除されたことを検証しない。branchはcontext IDの相違だけで、元branchへ戻した状態分離・復元を確認しない。renameは一意renameだけで、AC-10のcopy・複数候補・曖昧moveの非継承を一件も実行しない。restart後はdelete済みfileの不存在だけを確認し、残存する確認済みstateの復元を確認しない。historyはrepository単位で同じhistory fileになるmain/featureのrouteを2回読み同じevent列を重複flattenし、eventのcontext/revision/path/range/reasonとcurrent stateとの対応を検証せず、typeの存在だけを確認する。
- impact: mapperが全範囲を保持・全消去する、branch stateを混線させる、曖昧mappingを誤継承する、restartで非削除stateを失う、誤ったhistory内容を記録する回帰があってもT207はpassし得る。T207の終了条件と実装レポートの受け入れ主張を支持できない。
- evidence: 設計10.1/10.2は変更前後の未変更範囲維持と重複範囲だけの解除、10.3は曖昧mapping非継承、21章は変更部分だけの未確認化・branch分離・restart復元を要求する。該当操作/assertionがtest内に存在しないか、上記のとおり逆方向・存在確認だけである。
- required action: 1つの連続scenario内で、変更行と未変更行の両方を事前にmarkし、edit/commit後の正確な区間を確認する。mainへ戻してbranch state分離、unique renameのidentity/ranges、ambiguous copy/moveの新規未確認、delete後も別fileのstateがrestartで復元されることをassertする。historyはrepository historyを1回だけ読み、期待event列のcontextId、revisionId、filePath、previous/nextRanges、reasonをdurable current stateと照合する。

### T207-R1-P3 — medium — validation_wiring

- location: `package.json:147-157`、`.github/workflows/ci.yml:45-52`
- description: `test:t207`はfocused scriptとして追加されたが、新規`test-dist/test/integration/t207-git-history.integration.test.js`は`test:git`にもaggregate `test`にも含まれず、CI workflowも`test:t207`を呼ばない。
- impact: exact-HEAD CI runがsuccessでもT207の唯一のintegration scenarioは未実行であり、後続変更でtestが壊れても必須gateはgreenのままになる。run `30703593090`はcurrent HEADのconfigured gate成功証拠だが、T207 acceptance evidenceにはならない。
- evidence: CIのTemporary Git integration stepは`npm run test:git`だけを実行し、そのfile listにT207 testがない。repository aggregate `test`も`test:unit && test:git && test:github && test:vscode`である。手元の`npm run test:t207`は1/1 passしたが、手動focused実行はCI regression wiringを代替しない。
- required action: T207 integration testを`test:git`へ含めるか、CIで`test:t207`を明示実行し、必要ならCI workflow contract testを更新する。修正後のimmutable HEADに紐づくmatching CIを取得する。

## Coverage dispositions

- requirement/design conformance: `checked_finding`（P1、P2）
- correctness and edge cases: `checked_finding`（P1: empty/trailing-EOL）
- scope discipline/unrelated changes: `checked_no_finding`
- changed files and direct dependencies: `checked_finding`（P1〜P3）
- API/data/configuration/compatibility: `checked_finding`（persisted lineCount/editor contract）。新規public APIとbreaking external contractはなく`Design/BreakingChanges.md`は`not_applicable`
- workflow/package script: `checked_finding`（P3）
- error handling/failure diagnostics: `checked_no_finding`。Windows cleanup retryはNode `rm`のrecursive retry optionを適切に使用
- security/secret handling: `checked_no_finding`
- tests and validation adequacy: `checked_finding`（P1〜P3）
- current-HEAD CI: `checked_finding`（run `30703593090`、head SHA一致、completed/success。ただしT207 test未収録）
- report/tracking/documentation accuracy: `checked_finding`（P2によりAC達成主張が過大）
- regression/maintainability: `checked_finding`（P1、重複lineCount helper、P3）
- held: Issue #28（Windows POSIX fixture）は既存owner保持のnon-blocking held。本T207差分起因として昇格しない
- unexplored: 必須criterionなし

## Validation assessment

- `npm run test:t207`: 1/1 pass。ただしP1/P2のtest oracle不足により受け入れ根拠として不十分。
- `git diff --check de21d050b3f82438c1962d0326664057fb8ee1d2..19d3130ed83a72471dc0cabde512b7388d682c2c`: pass。
- exact-HEAD CI: run `30703593090`、`19d3130ed83a72471dc0cabde512b7388d682c2c`、completed/success。全configured gate成功だがP3によりT207 test未実行。
- implementation evidenceとして記録された`test:t205` 31/31、`test:t206` 25/25、compile、lint、architecture成功は再利用した。full suiteは指示どおり再実行していない。
- coding standards: 新規public/protected production memberなし。既存のpublic test helper documentationを維持。追加findingなし。

## Verdict・remaining risks・next action

- verdict: `fail`。required/open findings=`T207-R1-P1` high、`T207-R1-P2` high、`T207-R1-P3` medium。
- severity reclassification: なし。
- held/non-blocking: Issue #28。Windows cleanup retry自体は妥当だが、CI未収録のためLinux上でのT207継続実行証拠はない。
- remaining risks: 現在のfocused passとCI greenはproduction editor line-count不整合とacceptance assertion欠落を検出しない。技術verdictはreviewed implementation HEAD `19d3130ed83a72471dc0cabde512b7388d682c2c`だけに適用する。
- next action: P1〜P3を修正し、focused T207、関連T205/T206、configured gateを実行して新immutable HEADへcommit/pushする。その後、finding identity/severityを保持して同じnormal reviewerによるfix verificationを行う。独立最終reviewへはまだ進まない。
- merge boundary: 本reviewはmergeを許可せず、mergeを実行していない。
