# T603 初回通常レビュー 完全再調査 R2

## メタデータ

- Repository: `ssaattww/RevMem`
- Pull Request: #53 `T603 schema migration・破損隔離・回復`
- Review mode: `initial_review` 継続再調査
- Branch: `task/t603-schema-migration-recovery`
- Base: `main` (`146aec15783294da1795f268315c85d1a0dffa56`)
- Reviewed implementation HEAD: `6a7d66e2cfbdfe226b8e958151b318ced83e6d13`
- Reviewed implementation range: `146aec15783294da1795f268315c85d1a0dffa56..6a7d66e2cfbdfe226b8e958151b318ced83e6d13`
- Re-audit開始時PR HEAD: `8dca8376bf29332f219fdc4323b98d67709506ba`
- Previous normal-review report: `reports/issue-1-t603-review-20260816151902.md`
- Previous normal-review handoff: `handoffs/issue-1-t603-review-20260816151902.yaml`
- Reviewer: ChatGPT T603 normal reviewer（同一chat、2026-08-16）
- Verdict: **fail**
- Required findings: **14件（high 3 / medium 10 / low 1）**
- Generated at: `2026-08-16T15:38:00+09:00`

本報告は、前回reviewのfinding identity/severityを保持したまま、利用者から「skillに挙げられている項目を全て網羅する」よう指示を受け、アップロード済み`chatgpt-worker-skills.zip`の`work-context-manager`、`review-worker`、`chat-review-worker`、`report-writer`、`chat-handoff-manager`を実物から再読してゼロベースでcoverageを閉じ直した完全再調査版である。

前回report/handoffは履歴として保持するが、前回の「主要direct dependencies」「unexploredなし」というcoverage主張は不十分だった。また前回handoffにはschema-v3 enum/shape違反とlossless `source_payloads`不足があった。本R2 reportと同時に生成するR2 handoffを次worker向けのauthoritative review packetとする。前回finding `T603-R001`〜`T603-R007`は撤回・severity変更せず、本報告で証拠を拡張する。severity reclassificationは0件である。

## 1. Authority / scope / conflict

### 1.1 Authority order

1. 利用者の現指示: T603をskillの全項目で網羅reviewする。
2. Repository instruction: `AGENTS.md`、RevMem project instruction。
3. Accepted task/phase/design: `tasks/tasks-status.md` T603、`tasks/phases-status.md` P6、`doc/design/vscode-review-range-tracker-design.md` rev4。
4. PR #53/current repository evidence。
5. implementation/review reports and handoffs。

Issue #1も確認したが、現在はPhase 0のclosed issueであり、T603の追加受け入れ条件は持たない。T603の具体的authorityはtask/phase/designである。

### 1.2 T603 accepted scope

- schema migration chain
- migration前backup
- JSON / JSONL / snapshot corruption detection / isolation / recovery
- legacy schema fixtureの段階移行
- migration failure時の復元
- uncertain evidenceをreviewedとして表示しない
- AC-22 / AC-23 / AC-24に関係する永続化堅牢化

### 1.3 Authoritative-source conflict: T603-B001

`tasks/tasks-status.md` T603はJSONLの「破損検出・隔離・回復」を要求する。一方、現行設計rev4 §15.4は既存JSONLに破損行を検出した場合、**後続eventをappendせずrejectする**と明記する。PR #53は破損log全体をquarantineした後、valid lineをsalvageして**新event appendを成功**させる。taskの「回復」はappend継続まで明記しておらず、設計の具体的failure contractと実装が衝突する。

このconflictをreviewer判断で暗黙解消しない。現行authorityのままでは実装は設計不適合として`T603-R008`をrequired findingとする。実装workerは、設計に合わせてrejectへ戻すか、owner/userにappend継続をaccepted designとして明示更新してもらう必要がある。

## 2. Skill-required discovery / review coverage

### 2.1 PR変更file — implementation HEADの全9file確認

1. `.github/workflows/ci.yml`
2. `handoffs/issue-1-t603-implementation-20260816150000.yaml`
3. `reports/issue-1-t603-implementation-20260816145900.md`
4. `src/adapters/non-git-snapshots/node-non-git-snapshot-adapters.ts`
5. `src/adapters/state-repository/jsonl-review-history-store.ts`
6. `src/adapters/state-repository/persistence-schema-recovery.ts`
7. `src/adapters/state-repository/validated-file-system-review-state-repository.ts`
8. `test/unit/review-history-jsonl-store.test.ts`
9. `test/unit/t603-schema-migration-recovery.test.ts`

### 2.2 Production変更fileの内部direct imports — 全確認

- `src/adapters/state-repository/atomic-text-file-store.ts`
- `src/adapters/state-repository/contracts.ts`
- `src/adapters/state-repository/storage-router.ts`
- `src/adapters/state-repository/coherent-file-system-review-state-repository.ts`
- `src/adapters/state-repository/file-system-review-state-repository.ts`
- `src/adapters/state-repository/owner-global-state-loader.ts`
- `src/adapters/state-repository/owner-reconciliation-validation.ts`
- `src/adapters/state-repository/index.ts`
- `src/adapters/non-git-snapshots/index.ts`
- `src/application/non-git-snapshots/index.ts`
- `src/core/contracts/index.ts`
- `src/core/contracts/review-state.ts`
- `src/core/contracts/review-history.ts`
- `src/core/contracts/schema-version.ts`
- `src/core/review-history/index.ts`
- `src/core/review-history/review-history-event-codec.ts`

Node builtin importsはruntime platform APIとして挙動境界を確認したが、repository direct dependency fileではないため上記file listには含めない。

### 2.3 Direct consumers / runtime integration — 確認

- `src/extension.ts`
- `src/t305-extension.ts`
- `src/t505-global-understanding-source.ts`
- `src/adapters/state-repository/debounced-review-state-repository.ts`
- `src/adapters/workspace-review-state/workspace-review-state-session-provider.ts`
- `src/adapters/workspace-review-state/snapshot-tracking-workspace-review-state-session-provider.ts`
- `src/adapters/document-review-state/document-review-state-session-provider.ts`
- `src/application/review-history/review-history-recorder.ts`
- `src/application/global-understanding/global-understanding-background-recalculator.ts`
- `src/application/global-understanding/cooperative-global-understanding-calculation.ts`
- `src/application/editor-decoration/normal-editor-decoration-model.ts`

### 2.4 Config / workflow / docs / tracking — 確認

- `package.json`
- `package-lock.json`（PRで変更なし、dependency変更なし）
- `tsconfig.test.json`
- `test/unit/ci-workflow-contract.test.ts`
- `.github/workflows/ci.yml`
- `AGENTS.md`
- `Design/BreakingChanges.md`
- `README.md`
- `tasks/tasks-status.md`
- `tasks/phases-status.md`
- `doc/design/vscode-review-range-tracker-design.md`
- Issue #1
- PR #53 metadata/comments/commits
- GitHub Actions runs/jobs/artifacts
- previous implementation and review reports/handoffs

`unexplored`: **なし**。required criterionは全て以下でdispositionする。

## 3. TDD / CI / diagnostics evidence

### 3.1 Exact-head Red

- Test-only HEAD: `3c88a86bd0e1a47a43a259b53db431cc8724d40e`
- Actual commit message: `test: define T603 migration recovery behavior`
- Matching run: `31929190714`
- Actual job: `95121366762`
- Result: failure
- Build / typecheck / architecture / lint / existing Unit / T602: success
- New T603 focused step: failure
- Actual failure artifact: `9258779700`
- Artifact name: `ci-failure-diagnostics-31929190714-1`

artifactを実取得済み。少なくともT603 test log、build/typecheck/architecture/lint/unit/T602 logs、environment、git status、generated-file inventory、`src/`、`test/`、`dist/`、`test-dist/`、configuration/workflow contextを含む。workflowは`2>&1 | tee`を使い、stdout/stderrを統合logに保存する。failure diagnostic artifact policy自体は満たす。

ただしRed suiteは6 fixtureだけで、後から実装したrepository-style manifest/context/global migrationとlatest generation pointer migration等を仕様化していない。これは`T603-R012`。

### 3.2 Reviewed implementation HEAD CI

- Reviewed implementation HEAD: `6a7d66e2cfbdfe226b8e958151b318ced83e6d13`
- Exact matching run: `31930532442`
- Job: `95124484220`
- Result: success
- Build / contract typecheck / architecture positive+negative / lint / unit / T602 / T603 / T403 / T404 / T304 / T502 / T503 / T504 / T505 / temporary Git / mock GitHub / VS Code Extension Host: success

別SHAのrunは代用していない。

### 3.3 Re-audit開始時PR HEAD CI

前回normal-review report/handoffを追加したcurrent PR HEAD `8dca8376bf29332f219fdc4323b98d67709506ba`にもexact matching run `31931358963` / job `95126587128`がありsuccess。`8dca...`の追加内容は前回review report/handoffのみで、technical implementation targetは`6a7d...`のまま。

### 3.4 Current CI coverage gap

`package.json`の`test:unit`は`review-history-jsonl-store.test.js`を実行しない。`test:t206`は同testを実行するが、PR #53のCI workflowは`test:t206`を呼ばない。T603 focused stepは`t603-schema-migration-recovery.test.js`だけを直接実行する。したがってPRで変更された`test/unit/review-history-jsonl-store.test.ts`はcompile/lint対象にはなるが、current-HEAD CIでtestとして実行されない。しかも同testのcorrupt-only fixtureは`prepared.migrated === false`のwrite pathを通り、T603 mixed legacy+corrupt fixtureの`prepared.migrated === true` pathとは異なる。これは`T603-R011`。

## 4. Required findings

### T603-R001 — high — current-schema structural corruptionとowner-wide uncertaintyでstale reviewed cacheを再露出できる

- Origin: `introduced_by_change`
- Continuity: 前回R001を保持。severity `high`変更なし。証拠範囲を全direct dependency/consumerへ拡張。
- Location:
  - `src/adapters/state-repository/persistence-schema-recovery.ts` `validateContextDocument`, `validateGlobalDocument`, `validateManifestDocument`
  - `src/adapters/state-repository/validated-file-system-review-state-repository.ts` `load`, `loadGlobal`, `prepareTarget`, `uncertainTargets`
  - `coherent-file-system-review-state-repository.ts`, `file-system-review-state-repository.ts`
- Description:
  - preflightはcontext kind/descriptors/timestamps/deep file fields/ranges、Global file fields等をcanonical validationしない。
  - `prepareTarget`が`ready`を返した時点でtargetのuncertain flagを解除し、その後`super.load`またはowner-reconciliation validationが失敗しても再設定しない。
  - low-level load failureは以前のtarget cacheを消さない。
  - manifest/globalはstorage owner-wide evidenceだが`uncertainTargets`は`root + contextId`単位。target Aでmanifest/global破損を検出しても、以前cache済みtarget Bの`getCurrent(B)`はowner-wide stale Globalを返し得る。
  - deep current-schema corruptionの一部はlow-level validatorも通過し、quarantineされないままconsumerへ到達する。
- Impact: persisted evidenceが不確実または破損しているのにreviewed rangeを旧cacheから表示・利用でき、T603終了条件とAC-24に直接違反する。
- Required action:
  - current-schema保存modelをfully canonical validateしてから`ready`にする。
  - downstream load/validation failureでもuncertaintyを保持し、cacheをhide/clearする。
  - manifest/global uncertaintyはstorage-root/owner-wideに全cached targetへ伝播し、context-only uncertaintyだけをtarget-localにする。
  - valid load → structural corruption → load failure → `getCurrent() === undefined`、owner-wide corruption後のsibling context cache非露出、deep field/range corruptionの回帰testを追加する。

### T603-R002 — high — v0→v1 migration stepがcurrent定数へ結合され、次回schema bumpでchainが壊れる

- Origin: `introduced_by_change`
- Continuity: 前回R002、severity `high`保持。
- Location: state/history/snapshotの全v0→v1 migration step。
- Description: stepは`toVersion: 1`を宣言するが出力`schemaVersion`に`REVIEW_RANGE_SCHEMA_VERSION`を入れる。currentが2になり1→2 stepを追加すると0→1 stepが2を出力し、chainの`current.schemaVersion === step.toVersion`検査で停止する。
- Impact: adjacent migration foundationが最初のfuture schema bumpで失敗し、v0→v1→v2段階移行不能になる。
- Required action: historical stepはimmutable target version `1`を出力する。state/history/snapshotでmulti-step chain testを追加する。

### T603-R003 — medium — manifest referenceだけ全contextをv1へ進め、未選択context documentはv0のまま残る

- Origin: `introduced_by_change`
- Continuity: 前回R003、severity `medium`保持。
- Location: `MANIFEST_STEPS`, repository branch of `preparePersistedReviewState`, manifest reference contract。
- Description: manifest migrationは全context reference schemaVersionをv1にするが、prepareはtarget contextしかmigrationしない。target absentならcontextを1つもmigrationせずmanifest/globalだけpublishする。
- Impact: `RepositoryStateManifestContextReference.schemaVersion`が「referenced immutable documentのschema」を表す既存contractと実体が不一致になる。
- Required action: manifest publish前に全referenced contextをmigrate/validateするか、mixed versionを真に表現するcontractへ変更し、multi-context/target-absent fixtureを追加する。

### T603-R004 — medium — legacy parent migrationがnested future schemaを上書きしてdowngradeする

- Origin: `introduced_by_change`
- Continuity: 前回R004、severity `medium`保持。
- Location: `migrateContextV0ToV1`, `WORKSPACE_STEPS`, `MANIFEST_STEPS`。
- Description: root v0 migrationがnested file/context/global/referenceのschemaVersionを個別validationせずv1へ上書きする。nested v2もv1へ書き換える。
- Impact: future/unsupported dataを保全・rejectせず旧modelとして再解釈・publishできる。
- Required action: nested persisted modelもsource versionを個別検証/migrateし、future nested versionを変更せずrejectする。root-v0 + nested-v2 testsを追加する。

### T603-R005 — medium — malformed schemaVersionをfuture扱いしquarantineせず、snapshot `null`はlegacy扱いになる

- Origin: `introduced_by_change`
- Continuity: 前回R005、severity `medium`保持。
- Location: `runSchemaMigrationChain`, recovery handlers, snapshot `parseMigratedRecord`。
- Description: string/negative/non-integer等のinvalid schema metadataも`UnsupportedPersistedSchemaVersionError`になり、futureと同様にquarantineを迂回する。snapshotはnullish fallbackにより明示`schemaVersion: null`もmissing v0扱いする。
- Impact: corrupt metadataが毎回operationを停止する一方、nullはcorruptionをlegacyへ補完する。
- Required action: valid non-negative integerかつcurrent超過だけをfuture/unsupportedとし、invalid metadataはcorruption quarantineへ送る。legacy fallbackは`undefined`だけに限定し、state/history/snapshot testsを追加する。

### T603-R006 — medium — snapshot payload本体のcorruptionを検出しても隔離・除去しない

- Origin: `introduced_by_change`
- Continuity: 前回R006、severity `medium`保持。
- Location: `NodeNonGitSnapshotStorage.get`, `NonGitSnapshotTracker.read`。
- Description: wrapper JSON/fieldはstorageでquarantineするが、invalid base64実体、gzip failure、payload hash mismatch、inner envelope corruptionはtrackerが`corrupt`を返すだけでactive snapshot fileを残す。
- Impact: payload corruptionがlatest pointerから反復選択され、T603のsnapshot「隔離・回復」がwrapper corruptionにしか適用されない。
- Required action: payload integrity検証とquarantine/deleteを同一recovery boundaryへ接続し、latest pointerもconservativeに処理する。damaged compressed payload/hash/envelope testsを追加する。

### T603-R007 — medium — implementation report/handoff/PRの永続証跡がauthoritative GitHub dataと一致しない

- Origin: `introduced_by_change`
- Continuity: 前回R007、severity `medium`保持。
- Location: implementation report/handoff/PR body/completion comment。
- Evidence:
  - Red run `31929190714`: actual job `95121366762`, artifact `9258779700`; recorded job `95120961967`は404、artifact `5717201218`。
  - run `31929450198`: actual artifact `9258848153`; recorded `5717346948`。
  - run `31929675591`: actual job `95122502746` / artifact `9258909583`; recorded `95122672378` / `5717460644`。
  - run `31929798612`: actual job `95122794408`; report `95123012762`。
  - run `31929935759`: actual job `95123109494`; report/PR `95123412884`。
  - actual implementation commits `5fa3b0c2...`, `c77e375c...`; handoffは`8e5851...`, `9dda6c...`。
  - Red commit actual messageは`test: define T603 migration recovery behavior`。
  - PR bodyは次を独立reviewとするがimplementation handoffはnormal reviewを要求し、independent-final reserved pathは無い。
- Impact: next workerが存在しないjob/artifact/SHAを追いexact-head/continuityを誤判定できる。
- Required action: GitHub authoritative valuesでimplementation report/handoff/PR evidenceを訂正し、normal review → finding closure → reserved independent finalのlifecycleを一致させる。

### T603-R008 — medium — corrupt JSONLのsalvage+append成功はaccepted designと逆で、BreakingChangesにも記録されていない

- Origin: `introduced_by_change`
- Location:
  - `src/adapters/state-repository/jsonl-review-history-store.ts`
  - `test/unit/review-history-jsonl-store.test.ts`
  - `doc/design/vscode-review-range-tracker-design.md` rev4 §15.4
  - `Design/BreakingChanges.md`
- Description:
  - rev4 §15.4は既存JSONL破損行検出時に「後続eventをappendせずreject」と明記する。
  - PRは旧test `rejects a corrupt existing JSONL line instead of appending after it`を、quarantine後にappend成功する期待へ変更した。
  - これはobservable append failure contractの変更であり、`AGENTS.md`のbreaking-change記録要件に対して`Design/BreakingChanges.md`へ記録がない。
  - taskの「回復」はappend継続まで具体化しておらず、design conflictが未解消。
- Impact: append-only auditのfailure semanticsが設計と異なるまま変更され、consumer/運用がどちらを正とすべきか決められない。
- Required action: owner/user decisionでcontractを確定する。現designを維持するならcorrupt historyはquarantineしてappend reject、salvage+continueを採用するならdesignとBreakingChangesを明示更新し、そのaccepted contractをtestする。

### T603-R009 — high — corrupt manifest referenceから正常な別documentをquarantine/deleteできる

- Origin: `introduced_by_change`
- Location: `resolveReferencedFile`, repository preparationのglobal/context validation failure handling, `quarantinePersistedText`。
- Description:
  - reference validationは文字列が`contexts/`または`global-state/`で始まり、resolve後にstorage root内なら受理する。
  - `contexts/../global-state/x`のようにexpected subtreeを抜けてもroot内なら通る。
  - `contexts/<other-context>/valid.json`のように別contextの正常documentを誤参照するcorrupt manifestもpathとして通る。
  - child内容がexpected targetと不一致になると、recoveryは**corrupt manifest/referenceではなく参照先child fileそのもの**をquarantineしてactive originalを削除する。
  - global referenceがcontext subtreeへ正規化されるcaseは浅いGlobal validationと組み合わさり、異種documentをGlobalとして誤解釈する余地もある。
- Impact: 1つのcorrupt pointer/manifestにより、別contextやowner Globalの正常な永続evidenceを破壊できる。corruption isolationがcollateral data lossへ変換される。
- Required action:
  - resolve後のpathがcanonical expected subtree直下に残ることを検証し、`..`/cross-subtreeをrejectする。
  - reference identity mismatchではchildを破損扱いして削除せず、manifest/reference側をuncertain/quarantineする。
  - sibling-context misreference、cross-subtree context/global reference、valid child preservation testsを追加する。

### T603-R010 — medium — 設計が要求するstartup migrationではなくlazy-on-access migrationで、旧保存modelが未移行のまま残る

- Origin: `introduced_by_change`
- Location: `src/extension.ts`, state repository preflight, JSONL append, snapshot get/getLatest/entries。
- Description:
  - rev4 §15.3は「起動時に段階移行」を要求する。
  - production activationはrepository/history/snapshot objectをconstructするだけでstorage-wide migrationを列挙実行しない。
  - stateは対象contextがload/save/commitされるまで、snapshotはget/getLatest/entriesされるまでmigrationしない。
  - historyにはstartup reader/sweepがなく、monthly fileはその月へappendするときだけmigrationされる。過去月のlegacy JSONLは通常operationでは永久に未移行となり得る。
- Impact: 「全保存modelを起動時に段階移行」の終了条件を満たさず、未アクセスdataのschema状態が長期間mixedになる。R003のmanifest/reference不整合も長期化する。
- Required action: startupで所有storageを安全にenumerate/migrateするentrypointとintegrationを追加するか、lazy migrationをaccepted designへ明示変更する。少なくともhistorical history files、repository contexts、snapshot entries/latest pointersのrestart/startup integration testを追加する。

### T603-R011 — medium — PRで変更したJSONL regression testがcurrent CIで一度も実行されない

- Origin: `coverage_miss`
- Location: `package.json`, `.github/workflows/ci.yml`, `test/unit/review-history-jsonl-store.test.ts`。
- Description:
  - `test:unit`の明示file listに`review-history-jsonl-store.test.js`がない。
  - `test:t206`には入るがCIは`test:t206`を実行しない。
  - T603 focused CIは`t603-schema-migration-recovery.test.js`のみ。
  - したがって変更されたJSONL testはcompile/lintされるだけで実行されない。
  - このtestのcorrupt-only caseは`prepared.migrated=false` branchであり、T603 mixed legacy+corrupt fixtureの`prepared.migrated=true` branchと実行経路も異なる。
- Impact: PRが既存contract testを書き換えてもCIがその期待を検証せず、JSONL recoveryの一方のwrite pathが未検証になる。
- Required action: `test:unit`またはT603 focused package scriptに当該testを含め、workflow contract testでCI wiringを固定する。

### T603-R012 — medium — TDD Redがrepository-style migrationとlatest-pointer migrationを仕様化していない

- Origin: `coverage_miss`
- Location: Red commit `3c88a86...`, `test/unit/t603-schema-migration-recovery.test.ts`, later implementation files。
- Description:
  - RevMem policyはbehavior implementationでtestを先に追加しRedを確認してから実装する。
  - test-only Redの6 casesはworkspace state、workspace rollback、malformed workspace JSON、mixed JSONL、snapshot entry legacy migration、snapshot wrapper malformed JSONだけ。
  - 後続実装はrepository manifest/global/context migration、multi-document publish、target-absent path、snapshot latest pointer migration/quarantine等を追加したが、それらのpre-implementation failing fixtureが存在しない。
- Impact: T603の実装範囲の相当部分がproject-required TDD evidenceを持たず、R003/R009/R010等のrepository/pointer boundaryが未試験のままGreenになっている。
- Required action: behavior findingsの修正と併せ、repository-style and pointer scenariosを先にfailing regression testとして追加し、Red→implementation→Greenのexact-head evidenceを残す。

### T603-R013 — medium — JSONL salvageがstorage owner/context・month・eventId一意性を検証せず「確実なevent」以外も保持する

- Origin: `introduced_by_change`
- Location: `prepareExistingHistory`, `JsonlReviewHistoryStore.append`, strict event codec/direct recorder contract。
- Description:
  - salvageは各lineをJSON parse→schema migration→`serializeReviewHistoryEvent`するだけで、既存lineの`repositoryId/contextId`が現在のstorage targetと一致するか確認しない。
  - eventの`occurredAt`が現在の`events-YYYY-MM.jsonl`の月と一致するか確認しない。
  - designが履歴処理全体で一意と要求する`eventId`のduplicateも既存line間/新eventとの間で検出しない。
  - new eventだけはtarget identityを検査するため、wrong-owner/wrong-month/duplicate existing canonical lineを「recoverable」としてactive fileへ再publishできる。
- Impact: quarantine recovery後のactive audit logにstorage identityや月配置と矛盾するevent、重複IDを残し、「確実なeventのみsalvage」というT603の説明とaudit fidelityを損なう。
- Required action: file-level recovery validatorでtarget identity、month、eventId uniquenessを検証し、証明できないlineはsalvageしない。wrong-owner、wrong-context、wrong-month、duplicate-ID fixturesを追加する。

### T603-R014 — low — quarantine helperがAtomicTextFileStoreを使って書く一方、削除だけ直接`node:fs rm`してstorage abstractionを破る

- Origin: `introduced_by_change`
- Location: `quarantinePersistedText`。
- Description: helperはinjected `AtomicTextFileStore`からread/writeされるdataを対象にするが、active original removalはstore経由でなくhost filesystemの`rm(filePath)`を直接呼ぶ。contractにはdelete operationが無い。
- Impact: custom/deterministic storeではquarantine sidecarがstoreに書かれてもoriginalはstoreから消えず、production helperの意味とtest seamが一致しない。将来remote/alternate persistence adapterでも誤動作しやすい。
- Required action: deletionをpersistence contractへ追加して同一store経由にするか、helperをNode filesystem専用境界へ限定する。custom storeでquarantine後originalが読めないことをtestする。

## 5. Required coverage dispositions

| Criterion | Disposition | Evidence |
| --- | --- | --- |
| requirement and design conformance | `checked_finding` | R001〜R006、R008〜R010、authoritative conflict B001 |
| correctness and edge cases | `checked_finding` | R001〜R006、R009、R013、R014 |
| scope discipline / unrelated changes | `checked_no_finding` | implementation net diffはT603 persistence/recovery、tests、focused CI、required implementation artifactsに限定 |
| changed files and direct dependency impact | `checked_finding` | 全9変更file、全内部direct imports、主要direct consumers/runtimeを確認。R001/R006/R009/R010/R013/R014 |
| API / data / configuration / workflow / compatibility | `checked_finding` | R002〜R005、R008〜R011、R013。public TS API自体の新規source breakは認めない |
| error handling and failure diagnostics | `checked_finding` | R001/R005/R006/R009/R014。CI failure artifact policy自体は`checked_no_finding`相当 |
| security / secret handling | `checked_no_finding` | token/source本文を新規CI logへ出す変更なし。backup/quarantineは既存extension storage内でNode atomic storeの0600 temp semanticsを使用 |
| tests and validation adequacy | `checked_finding` | R001〜R006のmissing boundaries、R011、R012、R013 |
| current-HEAD CI evidence | `checked_no_finding` | implementation `6a7d...`→run `31930532442` success、re-audit開始時PR HEAD `8dca...`→run `31931358963` success。別SHA代用なし |
| report / tracking / documentation accuracy | `checked_finding` | R007、R008。task/phasesはrepository update-rule上reviewerが直接変更しない |
| regression / maintainability risks | `checked_finding` | R002/R003/R004/R009/R010/R014 |

`unexplored`: なし。

## 6. Held / not applicable

### Held H001 — T604 concurrency/cleanup ownership

- cross-window/process file lock、stale lock、atomic history append、cache/snapshot cleanup、backup/quarantine retentionはT604所有。
- T603がread pathをmigration writeへ変えたため複数repository instance/windowとのrace riskは存在するが、cross-owner serializationの最終closureはT604へheldする。
- ただしR009のcollateral quarantineやR001のsame-instance/cache fail-closedはT604へ延期できない。

### Held H002 — future v2 semantic transform

具体的v1→v2 field transformはfuture schema contractが無いためheld。ただしR002は現在のv0→v1 step自体がfuture chainを破壊するためT603 required finding。

### Held H003 — T606 generalized retry/error policy

storage容量不足、retry、stale UI、privacy-safe diagnostic policyの全障害fixtureはT606所有。T603固有corruption classification/isolationはR001/R005/R006/R009/R013として現在required。

### Not applicable

- independent-final report attestation: 本roundはnormal initial review継続でありN/A。
- merge: 利用者所有、reviewerは実施しない。
- task tracking direct edit: repository ruleによりreviewer write boundary外。

## 7. Prior review / handoff errata

前回review finding `R001`〜`R007`は有効でseverityも正しかったためerratumによるfinding変更は無い。ただしreview process evidenceには次の訂正が必要だった。

- 前回はuploaded ZIP skillを開始前に実読しておらず、`changed_files_and_direct_dependencies`を「主要direct deps」で閉じた。今回全direct imports/consumersを確認した。
- 前回reportの`unexplored: なし`はcoverage不足のため早計だった。本R2でrequired discoveryを完了し、初めて`unexplored: なし`を主張できる。
- 前回schema-v3 handoffはenum/shape違反があり、`source_payloads`もcore Skill complete outputではなくsummaryだった。次workerは前回handoffを継続packetとして使用せず、本R2のcorrected schema-v3 handoffを使う。
- 前回review commit `8dca...`自体のexact-head CI `31931358963`はsuccessであり、この訂正はtechnical implementation HEADを変更しない。

## 8. Validation assessment

- Requirements completion: **failed** — 14 required findings open。
- Reviewed implementation exact-head CI: **supported** — `31930532442` / `95124484220` success。
- Re-audit pre-persistence PR HEAD CI: **supported** — `31931358963` / `95126587128` success。
- TDD Red existence: **supported** — `31929190714` exact test-only HEAD failure + diagnostic artifact。
- TDD completeness: **failed** — R012。
- Failure diagnostics artifact content: **supported**。
- Changed JSONL regression execution in CI: **failed** — R011。
- Skill-required review coverage after this R2: **supported** — 11/11 criteria disposition、all changed files/direct dependencies、`unexplored` 0。

## 9. Verdict

**fail**

Open required findings: **14件**。

- high: R001, R002, R009
- medium: R003, R004, R005, R006, R007, R008, R010, R011, R012, R013
- low: R014

特にR001はuncertain evidenceからreviewed cacheを再露出でき、R009はcorrupt manifestから正常な別documentを削除できるため安全性終了条件を満たさない。R002はfuture schema chainを破壊し、R008/R010はaccepted designとの不整合、R011/R012はvalidation/TDD evidence欠落を示す。

## 10. Next action

1. implementation workerがR001〜R014を全て扱う。R001〜R007のidentity/severityは保持する。
2. B001（corrupt JSONLでrejectするかsalvage+appendを許すか）はowner/userのaccepted design decisionを先に確定し、必要ならdesign/BreakingChangesを更新する。
3. behavior fixはRevMem policyどおりregression testを先に追加してRedを確認してから実装する。R011ではCI wiringも修正する。
4. implementation follow-up report/handoff/PR evidenceをauthoritative GitHub valuesへ同期する。
5. 新implementation HEADに一致するworkflow runのみでCIを判定する。
6. 同じnormal review chatでR001〜R014のfix verificationを行う。全finding closure後に独立final review pathを予約しfresh reviewerへ渡す。
7. mergeは利用者が実施する。

## 11. Persistence / merge boundary

- Persistence mode: repository file
- This report path: `reports/issue-1-t603-review-r2-20260816153800.md`
- Corrected handoff path: `handoffs/issue-1-t603-review-r2-20260816153800.yaml`
- Previous review artifacts remain historical evidence and are not deleted or rewritten。
- 本reviewerはimplementation/test/design/workflow/trackingを修正しない。
- 本reviewerはmergeしない。
