# Issue #79 / PR #80 T607 CI follow-up

## タスク

Attestation HEAD `6770d2d8cbf66a11bf6747cc3e6e1f052ad86e0a` の exact `pull_request` CI run `32444576455` / unit job `96661656810` が失敗した2件の既存test contractだけを同期する。旧attestationは無効で、新attestationとexact-head CIはpending/heldである。

## sub-agentを使う理由

親から委譲されたCI delta実装であり、production実装・独立review・CI操作を分離するため、この限定scopeだけを実施した。

## 対象範囲

`PR68-R003 stale PR A success cannot overwrite newer PR B progress` のlate rejectionを即時に観測するtestと、`T505-R005 requesting a debounced refresh immediately invalidates the in-flight generation` のfail-closed clearを正確に期待するtestを更新した。README、tasks、phases、handoffはこのCI失敗・test-only delta・new attestation pendingを同期する。

## 対象外

Production code、設計、CI workflow、PR/Issue metadata、commit、push、review、attestation、CIの開始・待機・poll、full Windows unit suiteは対象外である。Markdown wording toolingはrepository wiring不在のためunsupportedのままとする。

## 実行コマンド

- `npm run compile:test; node --test test-dist/test/unit/issue-66-pr68-review-findings.test.js test-dist/test/unit/t505-refresh-invalidation.test.js`: 13 pass、0 fail。
- `npm run test:t607`: 79 pass、0 fail。
- `npm run typecheck:contracts`、`npm run lint`、`npm run validate:architecture`、`npm run validate:architecture:negative`、`git diff --check`: pass。negative architectureはexpected 11。

上記2ファイルのCI failureと同じcontractを直接検証し、test:t607とstatic gatesでT607 wiringを確認したため、Windows full unitは再実行しない。new attestation HEADのexact `pull_request` CIがmerge gateであり、CIは起動していない。

## 対象ファイル

- `test/unit/issue-66-pr68-review-findings.test.ts`
- `test/unit/t505-refresh-invalidation.test.ts`
- `README.md`
- `tasks/tasks-status.md`
- `tasks/phases-status.md`
- `handoffs/issue-79-t607-implementation-20260821094238.yaml`
- `reports/issue-79-t607-ci-followup-20260821125018.md`

## 指摘事項

- CI failure 1: superseded PR A activationはtyped `OperationCancelledError`でrejectする。testはrejectionを後からawaitしていたため、Nodeが`PromiseRejectionHandledWarning`を報告した。開始直後にoutcome handlerを接続し、typed cancellationとPR B progressがcurrentのままをassertする。
- CI failure 2: debounced refresh requestはin-flight generationをfail-closedで即時clearする。testのno-clear expectationをexactly one `clear`へ同期し、stale republishがないことをevent sequenceで固定する。

Production behaviorは変更していない。新規finding、severity変更、scope拡大はない。

## 結果

2件のtest-only deltaはlocal Greenである。attestation HEAD `6770d2d8cbf66a11bf6747cc3e6e1f052ad86e0a` は失敗CIにより無効のままであり、このreportはnew attestationでもmerge authorizationでもない。次はcommit/push後、同一independent reviewerによるfinding-limited new attestationである。

## リスク

Linuxでの最終確認はnew attestation HEADと一致する`pull_request` CIに限定して必要である。ローカルfocused evidenceはCI successを代替しない。Markdown wording toolingはunsupported heldのままである。
