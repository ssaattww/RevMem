# Issue #79 / PR #80 T607 CI follow-up R5

## タスク

Attestation HEAD `6b5cad916eda37eec7e241b34751c93667c66bc2` のexact `pull_request` CI run `32447060823` / job `96668498820` が全Node/focused/Git/Mock stageをT607まで通過後、最終VS Code Extension Host phase `lifecycle-restore-confirmed-and-unmark` 1件だけで失敗したrestart decoration restorationを診断し、deterministicなawaited drainを追加する。旧attestationは無効かつ再利用不可で、新attestationとexact-head CIはpending/heldである。

## sub-agentを使う理由

親から委譲された5回目のCI delta実装である。commit、push、review、CI操作からExtension Host lifecycleの診断・同期・local validationを分離するため、このscopeだけを実施した。

## 対象範囲

download済みdiagnostic `t607-ci-32447060823/test-output/vscode-launch-diagnostics/lifecycle-restore-confirmed-and-unmark-*.json`、normal-editor decoration controller、test-mode extension API、lifecycle suiteを確認した。persistence確認後の明示refreshはawait済みだが、split editorのvisible-editor eventがbounded refresh中に別generationを開始すると、明示refreshは正しくsupersedeされ、そのPromiseだけが先に完了できる。test-mode APIからevent-loop境界を含む全active decoration generationのidle drainをawaitしてからapplied intervalsを読む。

## 対象外

Persistence schema、review command、decoration model、production event ordering、CI workflow、設計、PR/Issue metadata、commit、push、review、attestation、CIの開始・待機・pollは対象外である。restart persistence assertionとunmark flowは削除・緩和しない。固定sleep、full `test:vscode` rerun、対象外phaseの再実行は行わない。

## 実行コマンド

- CI Red: `6b5cad9` のrun `32447060823` / job `96668498820` はT607までの全先行stageを通過し、最終VS Host phaseでrestored confirmed decorationがexpected `[{0,1}]` / actual `[]`となった。
- `npm run build`、`npm run compile:test`: pass。
- focused lifecycle: `node test-dist/test/vscode/run-extension-host.js --lifecycle-through-restore` はlocal Windowsのprerequisite `confirm` phaseで対象外の`mark selection command`が10秒timeoutし、restore phase未到達。diagnostic `test-output/vscode-launch-diagnostics/lifecycle-confirm-1787287057974.json`を保存し、再試行しない。
- `npm run test:t607`: 80 pass / 0 fail。新規`T607 decoration drain waits for an event-triggered bounded refresh`を含む。
- `npm run typecheck:contracts`、`npm run lint`、`npm run validate:architecture`、`npm run validate:architecture:negative`、`git diff --check`: pass。negative architectureはexpected 11。

## 対象ファイル

- `src/ui/normal-editor/normal-editor-decoration-controller.ts`
- `src/extension.ts`
- `test/unit/normal-editor-decoration-controller.test.ts`
- `test/vscode/suite/index.ts`
- `test/vscode/run-extension-host.ts`
- `README.md`
- `tasks/tasks-status.md`
- `tasks/phases-status.md`
- `handoffs/issue-79-t607-implementation-20260821094238.yaml`
- `reports/issue-79-t607-ci-followup-r5-20260821133836.md`

## 指摘事項

- 診断: restart persistence lossではなくtest observation raceである。explicit generationのsupersessionはT607 contractどおりだが、後続event generationがapplied mapをpublishする前にfixtureが読めた。
- 修正: controllerがactive refresh Promiseを追跡し、scheduler 1 turnでqueued VS Code listenerを受け入れた後、active setが空になるまでawaitする`drain()`を追加した。production refresh ordering・generation fencingは変更しない。
- Test boundary: `drainVisibleEditorDecorations()`はExtension Test modeだけで公開する。confirm、restart restore、unmarkの各assertion直前でrefreshとdrainをawaitし、focused runnerはconfirm＋restore phaseだけを選べる。

## 結果

bounded refreshのevent raceを固定sleepなしで同期し、T607 unit regressionとsuiteはGreenである。restart persistenceとunmarkのExtension Host assertionは維持した。attestation HEAD `6b5cad916eda37eec7e241b34751c93667c66bc2` は無効かつ再利用不可であり、このreportはnew attestationでもmerge authorizationでもない。次はcommit/push後、同一independent reviewerによるfinding-limited new attestationである。

## リスク

local focused Extension Hostは対象restore phaseへ到達しなかったため、その結果をGreenとして扱わない。Linux restart restorationの最終証拠はnew attestation HEADと一致する`pull_request` CIであり、それがmerge gateである。exact CIはheld、Markdown wording toolingはrepository wiring不在のためunsupported heldである。
