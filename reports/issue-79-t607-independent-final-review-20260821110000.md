# T607 independent final review report

## タスク

T607 / Issue #79 / PR #80 の一度限りの全範囲 `independent_final_review`。repository は `ssaattww/RevMem`、PR branch は `task/t607-performance-incremental-ui`、review worktree branch は `review/t607-independent`、base は `main` の `2afa1b6a8299b2d25a1ef2c7186508028bbd5fb6`、reviewed implementation HEAD は `c4a99db2bf24286cd39e98efdceeaa9c1cd7a6c3`、range は `2afa1b6a8299b2d25a1ef2c7186508028bbd5fb6...c4a99db2bf24286cd39e98efdceeaa9c1cd7a6c3` である。local HEAD、PR head OID、指定された frozen HEAD は開始時に一致し、worktree は clean だった。技術 verdict は `c4a99db2bf24286cd39e98efdceeaa9c1cd7a6c3` だけに適用する。予約済み report path は `reports/issue-79-t607-independent-final-review-20260821110000.md` である。

## sub-agentを使う理由

reviewer identity は `/root/t607_independent_review` である。この reviewer は T607 の実装、通常 review、R001〜R006 の修正または finding-limited closure、検証実行に参加しておらず、implementation owner と normal reviewer `/root/t607_normal_review` の双方から独立している。この reviewer 自身から追加 sub-agent は使用せず、過去の verdict に依存する前に frozen range を独立に全範囲確認した。

## 対象範囲

Authoritative source は Issue #79、PR #80、`AGENTS.md`、`doc/design/vscode-review-range-tracker-design.md` rev5 の設計原則、PR/Global/UI/operation feedback/performance/test contract、`Design/BreakingChanges.md`、`README.md`、`tasks/tasks-status.md`、`tasks/phases-status.md`、T607 implementation handoff、implementation report、initial normal review、normal closure R1〜R4、normal review follow-up である。base...HEAD の changed files 28 / 28 と、T301 calculator、T504 source/background recalculator/core aggregate、Review Contexts projection/runtime、normal-editor session/command、PR/Global VS Code Tree、operation feedback、public barrels、package/CI wiringを直接追跡した。

Required coverage disposition は次のとおりである。

- requirement / design conformance: `checked_finding`（T607-IFR001、T607-IFR002、T607-IFR004）
- correctness / edge cases: `checked_finding`（T607-IFR003、T607-IFR005）
- scope discipline / unrelated changes: `checked_no_finding`
- changed files / direct dependency / consumer / composition impact: `checked_finding`（T607-IFR001〜T607-IFR005）
- API / data / configuration / workflow / T301・T504・T606 compatibility: `checked_finding`（同期 API 自体と保存形式は維持されるが production cooperative composition が T607-IFR001、T607-IFR002、T607-IFR004 を満たさない）
- error handling / failure diagnostics / operation-feedback terminal lifecycle: `checked_no_finding`（ただし中断可能性は concurrency criterion の T607-IFR001〜T607-IFR003）
- security / secret / privacy-safe diagnostics: `checked_no_finding`
- generation / abort / cancellation / invalidate / dispose / stale nonpublication・nonselection・nonapply: `checked_finding`（Tree の stale publication fence は確認したが、実 work の中断と editor content generation は T607-IFR001〜T607-IFR003）
- memory / allocation ownership / deterministic work budget: `checked_finding`（T607-IFR001、T607-IFR002、T607-IFR004）
- tests / provided validation / package and CI adequacy: `checked_finding`（T607-IFR001、T607-IFR002、T607-IFR004、T607-IFR005）
- reports / tracking / documentation accuracy and normal R001〜R006 closure: `checked_finding`（T607-IFR006、および R002・R004 closure evidence discrepancy）
- regression / maintainability risk: `checked_finding`（T607-IFR001〜T607-IFR005）

## 対象外

test、build、compile、typecheck、lint、architecture validation、benchmark、`git diff --check` は起動・再実行していない。CI は PR metadata 取得時に current state を一度だけ観測し、run log、artifact、再実行、待機、poll は行っていない。implementation、test、design、workflow、configuration、README、tasks/phases、handoff、既存 report、branch、commit、push、PR/Issue/GitHub metadata、merge は変更していない。`Design/BreakingChanges.md` は確認したが、本 PR は公開 contract の追加と内部 scheduling 変更であり、現在の差分に記録すべき新しい breaking change は確認しなかった。review goal 内の unexplored area は none である。

## 実行コマンド

Read-only evidence collection として `git status --short`、`git rev-parse`、`git log`、`git diff --stat/--name-status/--word-diff`、`git show --stat`、`rg -n`、`rg --files`、`Get-Content`、`Test-Path` を使用した。GitHub は `gh issue view 79` と `gh pr view 80` を一度の取得で参照した。provided local evidence は `npm run test:t607` 71 pass / 0 fail、`npm run build`、`npm run typecheck:contracts`、`npm run lint`、`npm run validate:architecture`、`npm run validate:architecture:negative`（expected 11）、`git diff --check` の pass として受領し、再実行していない。この local evidence は下記の missing production workload と exact-head CI failure を上書きしない。

PR #80 の一度だけの current CI observation では、head OID `c4a99db2bf24286cd39e98efdceeaa9c1cd7a6c3` に対する `build-and-lint` check runs `32437665345` と `32437663099` はいずれも `FAILURE` だった。依頼どおり merge gate まで held とし、原因を推測せず、成功へ読み替えない。Markdown word check は focused/full とも `unsupported` である。repository に `tools/lint/`、`tools/lint/README.md`、`markdown-targets.json`、`markdown-whitelist.yaml`、`prh.yml`、`cspell.config.jsonc`、`lint:md` wiring がなく、実行禁止にも従い command は実行していない。通常 prose を backtick または quote で lint 回避した箇所は目視で確認しなかった。

## 対象ファイル

Changed files 28 / 28 は `.github/workflows/ci.yml`、`README.md`、`doc/design/vscode-review-range-tracker-design.md`、`handoffs/issue-79-t607-implementation-20260821094238.yaml`、`package.json`、T607 implementation/normal review/closure R1〜R4/follow-up reports 7件、`src/adapters/crypto/node-sha256-stable-hash.ts`、editor-decoration index/model、`src/extension.ts`、`src/t405-pull-request-review-runtime.ts`、Global Understanding model/index/VS Code runtime、normal-editor index/controller、PR Progress index/provider、tasks/phases、normal-editor controller test、T607 workload testである。

Direct dependencies / consumers / composition として `src/core/pr-progress/pr-diff-progress.ts`、`src/ui/pr-progress/vscode-pull-request-progress-tree.ts`、`src/t305-projection-refresh.ts`、`src/application/global-understanding/cooperative-global-understanding-calculation.ts`、`src/application/global-understanding/global-understanding-background-recalculator.ts`、`src/core/global-understanding/global-understanding-progress.ts`、`src/t505-global-understanding-source.ts`、`src/application/review-contexts/review-contexts-controller.ts`、`src/t405-review-contexts-runtime.ts`、`src/ui/review-contexts/vscode-review-contexts-runtime.ts`、document review state session provider、normal-editor review command service、operation-feedback boundary、および既存 T301/T304/T504/T505/T606 testsを確認した。

## 指摘事項

- **T607-IFR001 — High — independent final review — `src/t405-pull-request-review-runtime.ts:606-624,450-496`; `src/core/pr-progress/pr-diff-progress.ts:321-356`; `test/unit/t607-performance-incremental-ui.test.ts:268-307`.** Production PR Progress は repository load 後に persisted context 全体の clone / file identity projection と `calculatePullRequestDiffProgress` の hunk・10,000 changed-line validation/count を一つの同期呼出しで完了し、その後の provider checkpoint も `Promise.resolve()` の microtask だけである。したがって 10,000-line fixture は line aggregation 中に scheduler へ戻らず、timer/I/O/input が新 generation を開始して abort を伝えることもできない。test は同期 calculator を yield accounting の前に呼び、100 file の Tree projection checkpoint だけを数えながら「T301 validation」が bounded だと主張している。Impact は design §19/§19.1 の 10,000 changed-line staged UI、最大128 item、cancellation/current-generation contract が production composition で成立せず、Extension Host を長時間占有し得ることである。Required action は T301 の既存同期 API/結果意味を壊さず cooperative counterpart または runtime orchestration を追加し、context projection、diff hunk/line/interval validation、count、reviewability record validation、sort/projection の全段階を実 scheduler yield と `<=128` item accounting、AbortSignal/generation fenceへ接続すること、および actual `PullRequestReviewRuntime.activateProgress` で 10,000 changed lines の supersession・abort・single current swap・memory ownership を固定する regression を追加することである。
- **T607-IFR002 — High — independent final review — `src/t505-global-understanding-source.ts:105-180,244-343`; `src/application/global-understanding/global-understanding-background-recalculator.ts:182-230,293-379`; `src/t405-review-contexts-runtime.ts:344-364`; `src/application/review-contexts/review-contexts-controller.ts:144-188`; `test/unit/t607-performance-incremental-ui.test.ts:87-128`.** Issue #79 が要求する large repository Global/Review Contexts aggregation は production workload で bounded 化または回帰固定されていない。Global source は candidate/evidence/state/target の大規模 Set/Map/spread/map/interval copy を同期実行し、background recalculator は開始時に state/included を全量 deep-copy/order し、25-file ごとの `emit` で calculated prefix 全体を同期 copy/sort/validate する。外側 AbortSignal はこの recalculator へ渡らず、abort 後も work/cache mutation が継続し得る。Review Contexts は全 persisted PR の progress を順次取得し、projection/sort を load と publish で同期再実行するが、T607 差分と fixture は存在しない。T607 large-Global test は既に集計済みの fabricated Tree snapshot だけを投影するため、この production aggregation を通らない。Impact は大規模 repository/context で Extension Host 占有、遅い cancellation、不要な prefix 再集計・二重保持が残り、accepted scope を満たさないことである。Required action は T504 source snapshot、aggregate/status、open-target projection、Review Contexts progress/projection/publicationを同じ明示 `<=128` budget と実 scheduler/AbortSignal/generationへ接続し、incremental countersまたは一回の final sortで prefix 全再集計を除去し、actual production source/runtime で large repository と多数 saved contexts の stale nonpublication、abort/dispose、cache ownership、terminal feedbackを決定的に固定することである。
- **T607-IFR003 — High — independent final review — `src/extension.ts:447-484,542-557`; `src/ui/normal-editor/normal-editor-decoration-controller.ts:104-137,214-226`.** Cooperative hashing が document content generation に束縛されていない。`toDocumentDescriptor` は `editor.document.getText()` の文字列を取得して複数 timer stage で hash した後に現在の `lineCount` を読むが、controller は visible/active/settings のみを購読し、text edit は request generation を invalidate しない。よって hash 中に同じ line count の編集が入ると、旧本文 hash で state/modelを取得し、新本文の `lineAt` から option を作り、`editor.setDecorations` へ current として適用できる。command path の default `isCurrent = () => true` でも旧本文 hash を現在 state target として開ける。Impact は設計原則2.1、AC-24、stale nonapply を破り、変更済み本文へ確実でない確認済み表示または state mutation を行い得ることである。Required action は `TextDocument.version` 相当を descriptor の本文・lineCount と同一 snapshotへ束縛し、document change で該当 editor requestを abort/invalidateし、state I/O前・model後・option/bookkeeping後・host apply前・command commit前に version/content generation を再確認すること、および同一 line count 編集を各 yield 境界へ挿入して stale state load/apply/commit が0件になる production composition regression を追加することである。
- **T607-IFR004 — High — independent final review — `src/application/editor-decoration/normal-editor-decoration-model.ts:282-335,466-544`; `src/extension.ts:470-475`; `test/unit/t607-performance-incremental-ui.test.ts:367-459`.** Decoration の `<=128` contract は interval helper 内だけで、全同期段階を覆っていない。async model は current PR で同期 `calculatePullRequestDiffProgress`、file search、全 hunk line `flatMap`、`normalizeLineIntervals` を呼び、other-context union の `[...occupied, ...visible]`、sort 前 wrapper `decorations.map`、return 時 `sorted.map` も work counter 外である。descriptor は cooperatively hash する前に full `document.getText()` を一度に materialize する。R4 fixture は injection seam に fabricated content hash と fake host の同期 options map を渡すため、actual activation の document extraction/hash、current-PR diff、VS Code option/bookkeeping/applyを end-to-end で通らない。Impact は large document、多数 interval/current-PR diff、many-context で同期 work と一時配列二重保持が明示 budget を超え、T607-R004 closure と design §19.1/§19.1 harness evidence が成立しないことである。Required action は document extraction、current-PR change evidence、union、final decorate projectionを含む全段階を一つの generation-aware budgetへ移し、不要な spread/map copyを除去すること、および actual activation composition を large Unicode document、2,048 intervals、current PR 10,000 lines、複数 contexts、split editors、supersession で通して各同期 operation `<=128`、current editor ごとの一回だけの host apply、stale I/O/apply 0件を厳密に assertすることである。
- **T607-IFR005 — Medium — independent final review — `src/adapters/crypto/node-sha256-stable-hash.ts:20-41`; `test/unit/t607-performance-incremental-ui.test.ts:432-441`.** `digestCooperatively` は UTF-16 code unit indexで `value.slice` し、各 slice を別々に UTF-8 encodeして hashへ渡す。stage 境界が surrogate pair の間に来ると各片が replacement character として encodeされ、同じ文字列に対する canonical `digest(value)` と異なる SHA-256 になる。fixture は ASCII だけなのでこの contract violation を検出しない。Impact は large Unicode document の content identity が chunk boundary依存となり、既存 state/snapshot hashとの照合を誤って fail-closedにするか、別経路で作った同一本文 identity と不整合になることである。Required action は surrogate pairを分断しない文字 stageまたは正しい streaming UTF-8 byte encoderで hashし、high surrogate が 65,536-character boundary の直前/位置/直後にあるケースを含め、cooperative result が `digest` と常に一致する regression を追加することである。
- **T607-IFR006 — Low — independent final review — `README.md:26`; `handoffs/issue-79-t607-implementation-20260821094238.yaml:7-8,27-32`; `tasks/tasks-status.md:12,17-18,368,391`; `tasks/phases-status.md:41,187`; `reports/issue-79-t607-normal-finding-closure-r4-20260821104627.md:5,13,37`.** Repository-backed current-state records は independent target を `11d4c5d52ff4d07f9998c9951c9349ab1168748d` としているが、実際の frozen HEAD / PR head は `c4a99db2bf24286cd39e98efdceeaa9c1cd7a6c3` である。さらに R4 report 自体は後続 `5b34bfe` で追加され、`c4a99db` で正規化されたため、その report 内の「current HEAD 11d4c5d」は自身を含む current stateにはなり得ない。Impact は normal R006 の tracking/report identity closure、pre-freeze gate、current-head validation/CI provenance が不正確になることである。Required action は historical reports を黙って書き換えず、implementation findingsの修正・validation・normal fix verificationと同じ pre-freeze cycleで current erratum/follow-up、README、handoff、tasks/phasesを実際の新しい technical/final pre-freeze HEAD、current CI disposition、next actionへ同期し、全 non-final writeを commit/pushしてから targetを再freezeすることである。

## 結果

**Verdict: FAIL.** Required findings は High 4件（T607-IFR001〜T607-IFR004）、Medium 1件（T607-IFR005）、Low 1件（T607-IFR006）であり、全件をこの一度の full-scope independent pass で同時に確定した。severity reclassification / erratum はなく、すべて origin `independent final review` の新規 finding である。Normal R001 の narrow Global Tree projection、R003 の Tree lifecycle/feedback、R005 の design task-ID除去は `checked_no_finding`。R002 と R004 の closure は production bounded-work evidenceが不足し、R006 は後続 report commitsにより current-state accuracyを失った。Held は exact-head `pull_request` CI failure と Markdown wording tooling `unsupported`。Unexplored は none。

`report_attestation_allowed: false`。passing verdictではないため、この report を frozen HEAD 直後の terminal administrative attestation commitとして扱うことはできない。Next action は implementation owner が T607-IFR001〜T607-IFR006 を一括で tracked workへ戻し、project-required TDD/validation、report/tracking/handoff同期、commit/push、同一 normal reviewerによる finding-limited fix verificationを完了することである。required repository change は frozen stateを無効化するため、その後に workflow が許可する fresh independent final review targetを新しくfreezeする。mergeは禁止する。

## リスク

Current exact-head CI は一度の観測で failure であり、merge gateとして held のままである。原因は未調査で、local passを代用しない。Markdown wording focused/full は repository wiring不在の `unsupported` held である。wall-clock measurement は Windows / Node v24.18.0 / one run の advisory evidenceに限られ、deterministic work contractの代替ではない。本 report の persistence mode は fail verdictを記録する reserved repository fileであり、report-attestation head は null である。reportを含む commitを technical completionまたは merge authorizationとして扱ってはならず、今後の repository writeは通常 implementation/review lifecycleへ戻す必要がある。


