# Issue #79 / PR #80 T607 CI follow-up R2

## タスク

Attestation HEAD `63061974b2aead2c71a968c6b882bd3a9bab3106` のexact `pull_request` CI run `32445047413` / job `96662976057` がUnit tests通過後に失敗したT405 focused composition fixture 1件を診断し、deterministicに同期する。旧attestationは無効で、新attestationとexact-head CIはpending/heldである。

## sub-agentを使う理由

親から委譲された2回目のCI delta実装である。production実装、review、CI操作から限定fixture修正を分離するため、このscopeだけを実施した。

## 対象範囲

`test/unit/t405-composition-regression.test.ts` のruntime再登録と`reviewRange.openReviewContextDiff`の境界を確認した。登録時のfire-and-forget初期refreshを待たずに明示refreshを開始していたため、generation fencingが前者を正常にsupersedeしたstale通知が後続commandの共用`errors`へ遅延混入していた。fake Tree providerの初回publish eventをawaitし、各runtimeのstartupをcommand前にdrainする。

## 対象外

Production code、command generation、CI workflow、設計、PR/Issue metadata、commit、push、review、attestation、CIの開始・待機・pollは対象外である。T607 productionは変更していないため`npm run test:t607`を再実行しない。Windows `t405-selected-pr-session`のPOSIX fixture portabilityは既存の別scopeとしてheldにする。

## 実行コマンド

- 修正前: `npm run compile:test; node --test test-dist/test/unit/t405-composition-regression.test.js` は2 pass / 1 fail。後続open commandの`errors`にstale-operation表示が1件混入した。
- 修正後: 同じcommandは3 pass / 0 fail。
- `npm run test:t405`: 49 pass / 1 fail。変更対象composition regressionはpassし、別ファイル`R405-7 selected PR owns normal-editor...`だけがWindows上のPOSIX fixture ownershipでfail。
- 切り分け: `node --test test-dist/test/unit/t405-selected-pr-session.test.js` は1 pass / 1 failで同じ既知platform fixture failureを再現。
- `npm run typecheck:contracts`、`npm run lint`、`npm run validate:architecture`、`npm run validate:architecture:negative`、`git diff --check`: pass。negative architectureはexpected 11。

## 対象ファイル

- `test/unit/t405-composition-regression.test.ts`
- `README.md`
- `tasks/tasks-status.md`
- `tasks/phases-status.md`
- `handoffs/issue-79-t607-implementation-20260821094238.yaml`
- `reports/issue-79-t607-ci-followup-r2-20260821130055.md`

## 指摘事項

- 診断: production command-generation bugではない。explicit refreshは完了済みでopen commandも成功しており、fixtureがruntime登録時の自動startup refreshをawaitできていなかった。
- 修正: fake Tree providerの`onDidChangeTreeData`で初回publishをPromise化し、全5回のruntime登録をasyncにしてstartup完了後だけcommandへ進む。timerや固定sleepを使用しない。
- Held: Windowsでは`path.resolve("/repo")`と`fileSystemPathSemantics: "posix"`を混在させる既存`selected-pr-session` fixtureが1件failする。今回のfile・CI failure・Linux exact Unit passとは独立であり変更しない。

## 結果

CIで失敗したexact composition fileはdeterministic Greenで、production変更はない。attestation HEAD `63061974b2aead2c71a968c6b882bd3a9bab3106` は無効のままであり、このreportはnew attestationでもmerge authorizationでもない。次はcommit/push後、同一independent reviewerによるfinding-limited new attestationである。

## リスク

Merge gateはnew attestation HEADと一致する`pull_request` CIである。Windows `test:t405`全体は既知platform fixture 1件をheldし、exact CI Unit testsは`6306197`でpass済みだが、新HEADのCI successを代替しない。Markdown wording toolingはrepository wiring不在のためunsupported heldである。
