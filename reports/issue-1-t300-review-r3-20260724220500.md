# T300 専用レビューレポート R3

## 対象

- Pull Request: #17
- Task: T300
- Review target: `421c3650364e301db887f75c0d97ef05d3589381`
- Base: latest `main` `938904da6e63c7111dc8add56cb3a53acd9e9904`
- R2 findings: `reports/issue-1-t300-review-r2-20260724212500.md`

## R2指摘の確認

### High-1 runtime設定接続: Close

- activation時にeffective `reviewRange.exclude`を読込
- `workspace.onDidChangeConfiguration`を購読
- `reviewRange.exclude`へ影響する変更だけを反映
- semantic no-opと無関係設定はpolicy revisionを進めない
- invalid設定はerror表示し、last valid policyを維持
- subscriptionはExtensionContextと明示的deactivationの両経路でidempotentにdispose
- unit testとExtension Host testで実設定変更を確認

### High-2 POSIX Git path: Close

- policy candidate pathはGit形式repository-relative pathと明記
- slashだけをseparatorとして扱う
- backslashはdataとして保持
- `a\\b.ts`と`a/b.ts`を区別
- `src\\dist\\file.ts`が`**/dist/**`へ誤一致しない回帰testを追加

### Medium-1 glob compile上限: Close

- raw user glob件数最大256
- 1 pattern長最大1024
- brace展開・生成RegExp総数最大1024
- brace nesting最大32
- 上限超過を同期的に早期拒否し、last valid runtime policyを維持

### High-3 最新mainとhead SHA限定CI: Close

- baseは`938904da6e63c7111dc8add56cb3a53acd9e9904`
- compare結果はbehind 0
- review target headに紐づくrun `30095321420`で次が成功
  - install dependencies
  - build
  - lint
  - unit tests
  - temporary Git integration tests
  - mock GitHub integration tests
  - VS Code Extension Host tests

## 回帰確認

- `package.json`は最新mainの既存整形を保持し、設定とtest入口だけを追加
- `src/extension.ts`は既存通常エディタ処理、文言、履歴commentを保持し、設定controller接続だけを追加
- Extension Hostの既存3段階再起動・装飾assertを維持
- T104-2のstate repository変更、test、reports、tasksを保持
- Issue #13のbranch/PRへ変更なし

## 指摘

未解決のHigh、Medium、Low findingなし。

## 結論

R2の4指摘はすべてcloseした。T300は最新main上でTDD、runtime設定接続、Git path確実性、入力上限、全回帰CI、進捗同期を満たしている。マージは行わず、PR #17をユーザーレビュー可能状態で維持する。
