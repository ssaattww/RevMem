# T604 CI follow-up report

## タスク

- CI follow-up for Issue #72 / PR #73. Pre-CI technical head `1c664cd024882c8ffe21f03a4baec409f4c952a5` had technical closure; attestation head `dc66e5ea98281a094c1dff20a3fc21d689cf4492` is invalidated by CI run `32366251062`.

## sub-agentを使う理由

- User instruction prohibits sub-agents; none used.

## 対象範囲

- Only the T506 phase2 dead-owner, unexpired storage-root lease recovery delta. The approved cooperative-process threat model and IFR001/R2 finding identity are retained.

## 対象外

- No new review scope, native filesystem APIs, CI launch/wait, Extension Host/full suite local run, commit/push/PR/merge, or self-review.

## 実行コマンド

- Artifact inspection: run `32366251062`, `t506-restore-context-b-unmark-global` diagnostic. Red: `npm run test:t604` failed because confirmed-dead/unexpired descriptor timed out. Green: the same command passed 24/24. `npm run build`, `compile:test`, `typecheck:contracts`, `lint`, architecture positive/negative, and `git diff --check` passed. CI was not launched.

## 対象ファイル

- `storage-root-lock.ts`, focused T604 test, tasks/phases/handoff, and this report.

## 指摘事項

- Valid descriptor recovery now probes liveness before expiry: confirmed-dead owner recovers immediately and emits existing privacy-safe `stale-recovered`; live owner remains non-stealable after expiry; liveness probe rejection propagates/fails closed. Malformed/zero/truncated/future-invalid descriptors use the pre-existing `stat.mtime + leaseMs` bounded-age gate, so fresh partial data times out and only aged partial data recovers. The successor-safe rename protocol is unchanged. Tests cover unexpired confirmed-dead, expired live, unknown probe failure, fresh/aged partial matrix, and real killed child immediate recovery.

## 結果

- Run `32365902528` was the successful technical-head CI evidence. Run `32366251062` failed at T506 phase2: artifact records `StorageRootLockTimeoutError` from startup migration after phase1 Extension Host PID exited while its descriptor was still unexpired. The local delta is Green, but the old attestation is invalid. Held: post-commit exact-head CI, then same independent reviewer verification limited to IFR001/R2 stale recovery and a new attestation. Markdown lint remains unsupported. 

## リスク

- PID reuse is conservatively bounded to the platform liveness probe: only a confirmed dead PID allows early recovery; live or unknown remains fail-closed. Exact-head CI is still the merge gate and must be rerun by the parent after commit.
