# T300 専用レビューレポート R4

## 対象

- Pull Request: #17
- Task: T300
- Review target: `2a035b9cca32f2a6922cfd2861356e0e1d1985e4`
- Base: latest `main` `938904da6e63c7111dc8add56cb3a53acd9e9904`
- Previous review: `reports/issue-1-t300-review-r3-20260724220500.md`
- 対象差分: R2指摘対応後の最終head、runtime設定接続、Git path semantics、glob compile上限、回帰test、CI証跡

## 再レビュー観点

- R2のHigh/Medium指摘4件が実装・test・runtime証跡まで含めて解消されていること
- policy入力がGit形式repository-relative pathとして一貫して扱われること
- POSIX上でbackslashを含むfile nameをslash区切りpathと混同しないこと
- user glob入力がExtension Hostを過剰消費しないよう上限管理されていること
- `reviewRange.exclude`の初期読込、変更購読、無関係変更抑止、invalid設定時のlast valid維持が成立すること
- configuration subscriptionがExtensionContextと明示的deactivationの双方で安全に解放されること
- 最新mainに対してbehind 0であること
- CI確認対象がPR branchの最終head SHAに限定されていること

## R2指摘の再確認

### High-1 runtime設定接続: Close

- `ReviewFileExclusionConfigurationController`がactivation時にeffective `reviewRange.exclude`を読込む
- `workspace.onDidChangeConfiguration`を購読し、`reviewRange.exclude`へ影響する変更だけをserviceへ反映する
- service側でnormalized glob snapshotのsemantic no-opを抑止する
- invalid設定はerror表示し、policy置換前に例外となるためlast valid policyを維持する
- controllerはExtensionContext subscriptionと`deactivate()`の両経路でidempotentにdisposeされる
- unit testとExtension Host testで初期読込、変更、no-op、無関係設定、invalid設定を確認している

### High-2 POSIX Git path semantics: Close

- candidate path contractはGit形式repository-relative pathと明記されている
- slashだけをseparatorとして扱い、backslashはfile name dataとして保持する
- `a\\b.ts`と`a/b.ts`を区別する
- `src\\dist\\file.ts`を`**/dist/**`へ誤一致させない
- path normalizerからbackslash-to-slash変換は除去されている

### Medium-1 glob compile上限: Close

- raw user glob件数は最大256
- 1 pattern長は最大1024 UTF-16 code units
- brace展開・生成RegExp総数は最大1024
- brace nestingは最大32
- 上限超過時は同期的に拒否され、runtime controllerは直前のvalid policyを保持する

### High-3 latest main / head SHA CI: Close

- compare結果はbase `938904da6e63c7111dc8add56cb3a53acd9e9904`に対してahead 28、behind 0
- PRはmergeable
- 最終head `2a035b9cca32f2a6922cfd2861356e0e1d1985e4`に紐づくworkflow run `30095470068`を確認した
- runはcompleted / success
- job `build-and-lint`で次が成功した
  - install dependencies
  - build
  - lint
  - unit tests
  - temporary Git integration tests
  - mock GitHub integration tests
  - VS Code Extension Host tests

## 実装確認

### Configuration controller

- startは二重呼出しをno-opとする
- dispose後のstartは拒否する
- relevant configuration eventだけを処理する
- event受信後もdisposed状態を確認する
- invalid設定をserviceへcommitしない
- host abstractionによりVS Code API依存をadapter層へ限定する

### Shared policy service

- policy snapshot置換後だけrevisionを進める
- listenerへdetached normalized snapshotを渡す
- evaluationとconfiguration snapshotをPR進捗・Global集計で共有できる
- getterとevent配列のcaller変更が内部policyへ伝播しない

### Core policy

- binary、default glob、user globの優先順位は決定的
- first matchを理由として保持する
- absolute path、drive absolute path、parent traversal、NULを拒否する
- default globとmanifest defaultが一致する
- globはpolicy snapshot作成時にcompileされる

## 回帰確認

- latest mainのT104-2実装、test、reports、進捗情報を保持する
- 既存通常エディタ処理、装飾、保存lifecycleを維持する
- Extension Hostの既存3段階再起動・装飾assertを維持する
- Issue #13のbranch/PRへ変更なし
- T301、T304、T503の後続責務を先取りしていない

## 指摘

未解決のHigh、Medium、Low findingなし。

## 結論

R2の4指摘は最終headで解消されている。T300はruntime設定接続、Git path semantics、入力上限、共通decision contract、最新main追従、branch head SHA限定CIを満たしている。PR #17はユーザーレビュー可能であり、マージは行わない。
