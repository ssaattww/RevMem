# CI follow-upレポート

## メタデータ

- 対象: Issue #78 / PR #83
- failing HEAD: `400957af07347cd7186b2fe69fdddf6d92dbeaa0`
- pull_request run: `32600267122`
- duplicate push run: `32600264804`（待機対象外）
- verification capability: `local_execution_available`

## 失敗

- build、contracts、architecture、lint、unit、T403〜T505はpassした。
- T506 `restore-context-b-unmark-global`だけが`T506 Global Understanding must include review.ts.`でfailし、後続stepはskipされた。
- CI artifact `t506-restore-context-b-unmark-global-1787434947160.json`で、再起動後に開いた`review.ts`のfile-open lifecycle完了前にGlobal snapshotを読んだことを確認した。

## 修正

- `test/vscode/t506-suite/index.ts`は再起動後の文書open直後に既存`drainGlobalUnderstandingFileOpenForTest()`をawaitしてからGlobal snapshotを検査する。
- `test/unit/t610-folder-understanding.test.ts`にT506のopen→drain→snapshot順序を固定する静的回帰を追加した。
- production、timeout、sleep、performance workloadは変更していない。

## 検証

- `npm run compile:test`: pass。
- `node --test --test-name-pattern='T610 preserves T506 restart coverage' test-dist/test/unit/t610-folder-understanding.test.js`: 1/1 pass。
- `git diff --check`: pass（LF-to-CRLF warningのみ）。
- Extension Host、`test:t607`、full local gate、CI再実行は未実施。
- Markdown lintは`tools/lint/`と`lint:md`がないためunsupported。

## 次の対応

- commit/push後、同じ通常reviewerのclosureとfresh independent reviewを行う。
- 最終publication HEADのrequired pull_request CIだけを1回のmerge gateとして待ち、重複push runは待たない。
