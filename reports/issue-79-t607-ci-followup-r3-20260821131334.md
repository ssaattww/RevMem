# Issue #79 / PR #80 T607 CI follow-up R3

## タスク

Attestation HEAD `532620dc5cc91b3117fcb993ead5fd68e4e38b01` のexact `pull_request` CI run `32445673275` / job `96664691395` がUnit、T405、T406、T304、T502、T503通過後に失敗したT504 focused test 2件を診断し、T504のoperation-start immutable snapshot contractを回復する。旧attestationは無効かつ再利用不可で、新attestationとexact-head CIはpending/heldである。

## sub-agentを使う理由

親から委譲された3回目のCI delta実装である。commit、push、review、CI操作からproduction regressionの診断・修正・local validationを分離するため、このscopeだけを実施した。

## 対象範囲

`GlobalUnderstandingBackgroundRecalculator`のT607 staged projectionを確認した。Global file metadata、revision、included countが最初のcooperative yield後もcaller-owned objectから読まれ、T504で確立済みの「operation開始時に一つのcoherent snapshotを固定する」contractを破っていた。最初のawait前にrepository/revision、Global file scalarとreadonly interval vector reference、included descriptor、open pathを同期captureし、その後のvalidation・priority ordering・calculationを既存schedulerでcooperativeに進める。

## 対象外

CI workflow、設計変更、PR/Issue metadata、commit、push、review、attestation、CIの開始・待機・pollは対象外である。interval本体のunbounded deep cloneは行わず、公開readonly vectorのreferenceをcaptureする。T504/T607以外のsuiteとWindows full unitは、exact failing scopeとproduction contract suiteで十分なため再実行しない。

## 実行コマンド

- 修正前: `npm run compile:test; node --test test-dist/test/unit/t504-review-followup-r2.test.js` は2 pass / 2 fail。immutable Global snapshotはexpected 1 / actual 2、cooperative source yield testはloaded non-empty count mismatchで失敗した。
- 修正後: `npm run compile:test; node --test test-dist/test/unit/global-understanding-progress.test.js test-dist/test/unit/t504-review-followup.test.js test-dist/test/unit/t504-review-followup-r2.test.js` は15 pass / 0 fail。
- `npm run test:t607`: 79 pass / 0 fail。10,000件workload、actual Global source/recalculator、stale nonpublication、全accounted batch `<=128`を含む。
- `npm run typecheck:contracts`、`npm run lint`、`npm run validate:architecture`、`npm run validate:architecture:negative`、`git diff --check`: pass。negative architectureはexpected 11。

## 対象ファイル

- `src/application/global-understanding/global-understanding-background-recalculator.ts`
- `README.md`
- `tasks/tasks-status.md`
- `tasks/phases-status.md`
- `handoffs/issue-79-t607-implementation-20260821094238.yaml`
- `reports/issue-79-t607-ci-followup-r3-20260821131334.md`

## 指摘事項

- 診断: stale testではなくproduction regressionである。T607のcooperative projectionがcaller-owned Global/included stateをyield後に参照し、source/evidence/cache/progressが異なるrevision/countを観測できた。
- 修正: 最初のawait前にscalar値とcollection membershipをshallow frozen captureし、Global path duplicate検査、included priority ordering、snapshot work accountingは既存cooperative stageで継続する。
- Memory/budget: 大規模interval vectorはreadonly referenceをcaptureし、repository-sized二重copyを避ける。T607 regression suiteで全accounted batch `<=128`を維持した。

## 結果

T504のoperation-start immutable snapshot contractをproductionで回復し、CI失敗のexact T504三ファイルとT607 suiteはGreenである。attestation HEAD `532620dc5cc91b3117fcb993ead5fd68e4e38b01` は無効かつ再利用不可であり、このreportはnew attestationでもmerge authorizationでもない。次はcommit/push後、同一independent reviewerによるfinding-limited new attestationである。

## リスク

Merge gateはnew attestation HEADと一致する`pull_request` CIである。readonly interval vectorのin-place mutationは公開input contract外だが、reference replacementとmetadata mutationはoperation-start captureから隔離される。exact CIは新HEADまでheldであり、Markdown wording toolingはrepository wiring不在のためunsupported heldである。
