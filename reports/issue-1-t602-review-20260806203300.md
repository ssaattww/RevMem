# T602 初回レビュー報告（完全再調査版）

## メタデータ

- Repository: `ssaattww/RevMem`
- Pull Request: #49
- Review mode: initial review
- Base: `main` (`112198c33823a5fc6681399a19e0c5361614143f`)
- Reviewed implementation HEAD: `d1a2b5ffd69ca5154a426072c63942cfb3b177a6`
- Reviewed range: `112198c33823a5fc6681399a19e0c5361614143f..d1a2b5ffd69ca5154a426072c63942cfb3b177a6`
- Reviewer: ChatGPT normal reviewer（同一chatで再調査）
- Verdict: **fail**
- Generated/revised at: `2026-08-06T20:58:00+09:00`

本報告は、先行した短時間レビューを破棄せず訂正記録として保持し、ZIP内の `chat-review-worker`、`work-context-manager`、`review-worker`、`report-writer`、`chat-handoff-manager` に従って全変更と主要直接依存を再確認した結果である。

## 対象と確認範囲

確認したもの:

- T602タスク定義と設計書rev4 10.4、9.2、10.3
- PR #49の実装HEAD `d1a2b5f...` に含まれる全20変更ファイル
- Git revision mapper、Git diff parser、file-state transition、snapshot tracker、document session provider、state repository contractなどの主要直接依存
- 追加された全T602テストと既存CI workflow
- implementation report、handoff、PR本文、実装完了コメント
- reviewed implementation HEADに一致するCI run

対象外:

- 実装修正
- merge
- 実機での手動force-push操作

## CI

Reviewed implementation HEAD `d1a2b5ffd69ca5154a426072c63942cfb3b177a6` に一致するGitHub Actions run `31097483947` は `success`。別SHAのrunは代用していない。

CI成功は、現在存在するテストが通過したことを示す。以下のfindingは未試験の障害境界、候補一意性、世代競合、Context/Global整合性、および設計契約違反から検出した。

## 先行レビューの訂正

### T602-R002 — erratum / retracted

- Source severity: medium
- 先行記載: `objectExists` 例外をunresolvedへ変換しないため、fail-closedでない。
- 訂正: mapper全体をrejectするfail-stop動作は、誤った確認済み状態を返さないため安全性上はfail-closedである。利用可能性・診断・再試行はT606の範囲でもある。
- 結論: **required findingから撤回**する。
- 理由: 初回確認が不十分で、利用不能と誤った確認済み保持を混同した。以下のT602-R009は別問題であり、例外処理ではなく重複availability判定の不整合を指摘する。
- Approving authority: 同一normal reviewerによる証拠再評価。

## Required findings

### T602-R001 — high — snapshot世代切替が永続状態commitと整合せず、失敗結果と永続結果が食い違う

- Origin: introduced_by_change
- Location: `src/adapters/document-review-state/persisted-document-review-state-session-provider.ts`, `committer.commit`
- Description: 処理順が `invalidateSnapshots` → `delegateCommitter.commit` → `publishSnapshots` で固定され、旧世代を保持したまま新世代を準備する仕組みがない。
- Impact:
  - delegate commit失敗時、永続状態は旧状態のままなのに対応する正常snapshotだけが消える。
  - delegate commit成功後のsnapshot保存失敗では、永続状態は更新済みなのに呼び出し側へcommit失敗が返る。
  - 再試行時に「未保存」と「保存済みだがsnapshotなし」を区別できない。
- Evidence: rollback、旧pointer復元、prepared generation、commit outcomeの分離がない。runtime testは全成功経路1件だけで、各失敗点を検証しない。
- Required action: 新snapshotを非公開で準備し、永続commit成功後にpointerを切り替える二段階方式等へ変更する。delegate commit失敗、context/global片側保存失敗、pointer切替失敗、保存済み後例外の回帰テストを追加する。

### T602-R003 — medium — PR本文・実装report・handoffの最終HEAD証跡が一致しない

- Origin: introduced_by_change
- Location: PR #49 body/comment、`reports/issue-1-t602-implementation-20260806195000.md`、`reports/issue-1-t602-handoff-20260806195139.yaml`
- Description: reviewed implementation HEADは`d1a2b5f...`だが、PR本文は`f42bdb...`、implementation reportとhandoff主要targetは`424fdd...`を最終実装として記録する。
- Impact: 次workerが誤ったcommit rangeまたは別SHAのCIを採用し、HEAD一致ルールを破る。
- Evidence: PR metadataと各保存証跡のSHA/runが不一致。`d1a2b5f...`にはrun `31097483947`が存在する。
- Required action: 実装修正後の新しいimplementation HEADへreport、handoff、PR本文、実装完了コメントを同期し、そのSHAに一致するrunだけを最終証跡にする。

### T602-R004 — high — `rev-parse` exit 128をobject欠落と誤分類する

- Origin: introduced_by_change
- Location: `src/adapters/local-git/history-rewrite-local-git-adapter.ts`, `listFilePathsAtRevision`
- Description: `revisionResult.exitCode === 1 || exitCode === 128` を一律`undefined`として扱う。
- Impact: 権限、repository破損、不正Git状態などのfatal exit 128が「current revisionが存在しない」という証拠へ変換される。呼出側はtree列挙失敗を認識せずsnapshot fallbackを継続し、候補不足のまま確認済み状態を回復し得る。
- Evidence: 設計書9.2はexit 1のみmissing、その他はdiagnostic付きGit failureとし、exit 128を一律missingへ変換しないと明記する。追加testはexit 1だけを検証する。
- Required action: exit 1のみmissingとし、128を含む他の非0終了は`GitCommandFailedError`にする。exit 128、timeout、stderr付きfatalの回帰testを追加する。

### T602-R005 — high — 候補列挙・current file読取失敗を候補不存在として捨て、証明されていない「一意mapping」を受理する

- Origin: introduced_by_change
- Location:
  - `src/application/review-context/history-rewrite-git-context-revision-mapper.ts`, `currentCandidatePaths`
  - `src/application/history-rewrite-recovery/git-context-recovery.ts`, `loadCurrentCatalog`
- Description:
  - tree列挙例外をcatchして`[]`へ変換する。
  - 各candidateのimmutable read例外、`missing-revision`、`invalid-encoding`等を黙ってcatalogから除外する。
- Impact: 読めなかったcandidateが同一内容またはより正しいrename先でも、残った1件を「一意」と誤判定してstable file IDと確認済み範囲を移す。証拠不足が確認済み化へ変換され、T602の保守性要件に反する。
- Evidence: failureを表す結果型またはunknown-candidate flagがなく、recovery serviceには縮小済みcandidate集合だけが渡る。列挙・読取失敗testがない。
- Required action: proven missing/非reviewableとfatal/unknownを分離する。candidate集合の完全性が証明できない場合はrename・cross-path recoveryをunresolvedにする。tree列挙失敗、1候補読取失敗、current revision missingのtestを追加する。

### T602-R006 — high — snapshot candidate選択がfile identityではなく「確認済み行が残った候補数」で決まり、無関係fileへ追従できる

- Origin: introduced_by_change
- Location: `src/application/history-rewrite-recovery/index.ts`, `recoverWithoutOldObject`
- Description: 複数candidateにsnapshot diffを適用した後、`reviewedRanges.length > 0`のcandidateだけをidentity候補として数える。snapshot mappingが空になったcandidateは、実際のrename先であっても候補から除外される。
- Impact: 旧snapshotの確認済み行が実際のrename先では変更済みで0件となり、無関係fileに偶然同じ確認済み行が1行残ると、無関係fileを唯一のrename先としてstable identityと確認済み範囲を移す。
- Evidence: `NonGitSnapshotTracker.map`は1組のline mappingを返すだけで、cross-file identityを証明しない。LCS長0でも`mapped`かつ空rangeになり得る。testsは同一内容2候補と両候補non-emptyだけで、actual destination empty / unrelated candidate non-emptyを検証しない。
- Required action: reviewed rangeの生存数をfile identity判定に使用しない。snapshot本文とcandidate本文の対応をidentity証拠として別contractで評価し、全candidate間で一意性を証明できない場合はunresolvedにする。

### T602-R007 — high — ContextとGlobalを独立回復し、同じfile IDを異なるpath・異なる解決状態でatomic commitできる

- Origin: introduced_by_change
- Location: `src/application/history-rewrite-recovery/git-context-recovery.ts`, `recover`
- Description: Context snapshotとGlobal snapshotを別々にcandidate評価し、最後のconflict除去も各map内だけで行う。同一file IDについて両sideのdestination一致を検証しない。
- Impact:
  - Contextは`src/a.ts`、Globalは`src/b.ts`へ同じfile IDを回復できる。
  - Contextだけrecovered、Globalはunresolved/削除という不整合も返せる。
  - repository transactionはこの2 snapshotを一体で保存し、その後のtarget照合、装飾、Global反映、snapshot再発行が破綻する。
- Evidence: context/global snapshotはreviewed rangesが異なるため、T602-R006の候補選択結果も異なり得る。追加testは両snapshotの内容・range・destinationが常に同じケースだけである。
- Required action: 共通file IDのdestination identityを一度だけ決定し、Context/Global両方へ適用する。どちらかが矛盾・不明なら両sideを保守的にunresolvedにする。destination divergence、片側snapshot欠落、片側ambiguousのtestを追加する。

### T602-R008 — high — latest pointerが別file/contextの有効snapshotを指しても所有者検証なしでrangeを復元する

- Origin: introduced_by_change
- Location:
  - `src/application/history-rewrite-recovery/git-context-recovery.ts`
  - `src/application/history-rewrite-recovery/adapters.ts`
  - `src/application/non-git-snapshots/index.ts` direct dependency
- Description: `latestSnapshotId(scope,fileId)`で得たIDを`map(snapshotId,currentContent,now)`へ渡すが、mapping portはsnapshot envelopeの`workspaceContextId`と`fileId`を期待値と比較しない。Global側は一度restoreして照合するものの、不一致時も`snapshotId`を破棄せず後段mapへ渡せる。
- Impact: pointer破損、storage bug、世代race等で別fileの有効snapshot IDが入ると、hash整合した「corrupt」ではなく正常snapshotとして他fileのreviewed rangesを移植する。
- Evidence: snapshot IDはpayload hashなのでpayload自体の完全性しか証明しない。ownershipはenvelope内にあるがrecovery portの入力契約から失われる。wrong-owner pointer testがない。
- Required action: expected scope/fileIdをsnapshot restore/map contractへ含め、envelope ownership不一致を`corrupt`または`missing`として未確認化する。Context/Global双方のwrong-context、wrong-file testを追加する。

### T602-R009 — high — availability precheckとdirect mapperが同じobjectを別々に確認し、TOCTOUで直接Git優先順位を破る

- Origin: introduced_by_change
- Location: `src/application/review-context/history-rewrite-git-context-revision-mapper.ts`, `map` / `oldObjectAvailability`
- Description: wrapperが先に`objectExists`を実行し、その後existing direct mapperが独自に再度`objectExists`を実行する。fallback可否は最初の結果、direct mapping結果は後の結果に依存する。
- Impact:
  - precheck=true、direct check=false: direct mapperは全解除するがwrapperはfallbackを呼ばず、SHA-only回復機会を失う。
  - precheck=false、direct check=true: direct mapperが正しいimmutable diffで回復してもwrapperがsnapshot結果で上書きし、直接Git証拠優先を破る。
- Evidence: availability結果をdirect mapperへ注入せず、direct mapperから実際のmissing原因も返さない。可変source/GC race testがない。
- Required action: object availability判定を単一化し、direct mapperから「どのold revisionがproven missingか」を構造化して返すか、同一観測を両処理で共有する。結果が途中で変わるtestを追加する。

### T602-R010 — high — snapshot latest pointer更新に世代CAS/直列化がなく、新しい解除後に古い確認済みsnapshotが再公開され得る

- Origin: introduced_by_change
- Location:
  - `src/adapters/document-review-state/persisted-document-review-state-session-provider.ts`
  - `src/application/non-git-snapshots/index.ts` direct dependency
- Description: 各open/commitが独立してinvalidate/saveLatestを行い、`setLatest`にはexpected generationやserializationがない。
- Impact: commit Aの永続化後snapshot保存が遅延し、その間にcommit Bがより新しい解除状態を保存・publishした後、Aが古いsnapshot IDをlatestへ上書きできる。後のrebaseで解除済みrangeが再び確認済みになる。
- Evidence: tracker latest pointerは単純なlast-writer-wins。state repository CASとsnapshot pointerの世代は結び付いていない。concurrent open/commit testがない。
- Required action: state revision/generationをsnapshot pointerへ含め、expected generation CASまたはprovider単位の直列化で古いpublishを拒否する。古いsaveが後着するrace test、解除後の再回復testを追加する。

### T602-R011 — medium — 新規direct Git adapterはcopyをrename相当として扱い得るうえ、production経路に接続されていない

- Origin: introduced_by_change
- Location:
  - `src/application/history-rewrite-recovery/adapters.ts`, `GitRevisionMappingHistoryRewritePort`
  - `src/application/history-rewrite-recovery/git-context-recovery.ts`
- Description: adapterは`parseZeroContextGitDiff(...).files`をoldPathだけで選び、`isRename`やcopy metadataを判定しない。copy sectionではsource oldPathとcopy destination newPathが返るため、source identityをcopy先へ移し得る。一方production coordinatorは常に`missingOldObject` stubを注入し、このadapterを使用しない。
- Impact: unit test上の「direct Git first-stage」とproduction実装が分岐し、copyの誤動作が見逃される。将来このadapterを接続するとT204のcopy未確認契約を破る。
- Evidence:既存validated file transitionはcopyを別kindとして扱うが、新adapterは扱わない。追加testにcopyがなく、production runtime testはexisting direct mapper経路を通る。
- Required action: 重複direct mapping実装を除去して既存mapperの構造化結果を利用するか、copy/add/delete/renameを完全に分類しproduction経路でも同じ実装を通す。copy source保持・copy destination新規未確認testを追加する。

## Coverage dispositions

| Criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement and design conformance | checked_finding | R004、R005、R006、R009 |
| Correctness and edge cases | checked_finding | R001、R005〜R010 |
| Scope discipline and unrelated changes | checked_no_finding | 変更はT602、test、workflow、report/handoffに限定 |
| Changed files and direct dependency impact | checked_finding | 全20変更fileと主要dependencyを確認。R007、R008、R011 |
| API/data/config/workflow compatibility | checked_finding | snapshot ownership/generation、Local Git error contract |
| Error handling and failure diagnostics | checked_finding | R001、R004、R005 |
| Security and secret handling | checked_no_finding | source本文を新規log出力する変更なし。artifactは既存方針 |
| Tests and validation adequacy | checked_finding | success path中心で上記failure/race/cross-scope test欠落 |
| Current-HEAD CI evidence | checked_no_finding | `d1a2b5f...` run `31097483947` success |
| Report/tracking/documentation accuracy | checked_finding | R003 |
| Regression and maintainability risk | checked_finding | R009〜R011 |

## Held items

なし。T604のmulti-window lockやT606の包括的error policyへ一般化できる領域はあるが、R004〜R010はT602自身が確認済み範囲を誤回復しないために必要であり、後続taskへ保留できない。

## Unexplored

- 実機での手動force-push/reflog expiry/GC操作は未実施。
- CI artifactの全byte内容は成功runでは生成されないため対象外。Red run artifact IDの存在はimplementation report/handoff記録まで確認したが、全archive内容の再解析は未実施。

これらは上記required findingsを弱めず、verdictを`incomplete`へ変更する要因ではない。

## Validation assessment

- Build/typecheck/architecture/lint/unit/focused/Git/GitHub/VS Code tests: supported by exact-head CI success。
- TDD Red evidence: implementation reportに複数exact-SHA run/artifact記録あり。ただし新たに検出した欠陥classのRed testは存在しない。
- Manual acceptance: not_run。

## Verdict

**fail**。

Required findings: `T602-R001`, `T602-R003`, `T602-R004`, `T602-R005`, `T602-R006`, `T602-R007`, `T602-R008`, `T602-R009`, `T602-R010`, `T602-R011`。

`T602-R002`はerratumとして撤回した。修正後は同じnormal review chatでfinding identityとsource severityを維持してfix verificationを行う。修正時は各findingのRed testを先に追加し、実装後の新しいcurrent HEADに一致するCIだけを使用する。mergeは利用者が行う。
