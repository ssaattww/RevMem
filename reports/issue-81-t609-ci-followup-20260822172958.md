# Implementation report

## Work type

Issue #81 / PR #82 の CI-delta follow-up。失敗した `pull_request` CI run `32562360871` / job `97005812011` の T405 回帰だけを対象にした最小修正である。

## Target identity

branch: `task/issue-81-repository-encoding`。開始 technical HEAD: `4fc4bedb53645ba0054b153b8d49f510eca39c39`。base: `3bba5defe32b7da134817492427e09c70c97beaf`。本作業は未commitで、push、CI待機、PR更新、review、mergeは行っていない。local execution capability は `local_execution_available`（npm/Node test、TypeScript、ESLintを実行可能）である。

## Scope

`reviewRange.redetectPullRequest` の terminal failure を後続 Tree refresh が再実行して同じUI errorを二重表示する T405/T406 production seam に限定した。変更は Review Contexts runtime と同seamのfake `vscode.Uri` fixtureだけである。設計、public API、DSL、設定、workflow、Breaking Changes、tracking、historical reports は変更しない。Design impact は internal failure-boundary repair のため none、`Design/BreakingChanges.md` の更新は不要と判断した。

## Failure evidence

CIは T405 follow-up job で `T406 executes the T405 production seam across PR selection, failure fallback, cache recovery, closed state, and isolation` が `errors=[]` に対して2 UI errorsを観測して失敗した。local pre-fix Red も同じ選択testで 0 pass / 1 fail（redetect の validation + permanent UI errors）だった。診断でT609 URI contractに対してfake URIが `authority` / `query` / `fragment` を欠き、最初の TypeErrorを生むことを確認した。fixtureを実URI形状へ直すと既存offline-cache assertionが `offlineRefreshErrors.length`: expected 1, actual 2 で Redとなり、terminal failure後にも `mutate` がprovider refreshを実行して二重通知するproduction原因を固定した。

## Implementation

`src/ui/review-contexts/vscode-review-contexts-runtime.ts`: `runOperation` が cancellation とterminal failureを区別して返すようにし、`mutate` は terminal outcomeで providerをclearして後続refreshを中止する。`test/unit/t405-composition-regression.test.ts`: active documentとworkspace folderのfake file URIを実際の `vscode.Uri` 契約（empty authority/query/fragment）へ整え、既存T406 seamのoffline-cache one-error assertionが実経路を検証するようにした。production API・behaviorの拡張はない。

## Validation

Focused Red: `npm run compile:test; node --test --test-name-pattern='T406 executes the T405 production seam' test-dist/test/unit/t405-composition-regression.test.js` は 0 pass / 1 fail。Focused Green: 同一対象は 1 pass / 0 fail（test 160.3s）。`npm run test:t405` は1回のみ実行し、50 tests中49 pass / 1 fail。T406 production seamは pass。残る failureは非変更path `test/unit/t405-selected-pr-session` の `DocumentReviewStateSessionProvider.openSelectedPullRequest`: selected pull-request context does not own the active editor で、再実行しなかった。`npm run build` pass。`npm run lint` pass（warnings 0）。`git diff --check` pass（CRLF conversion warningのみ、whitespace errorなし）。repository-local `tools/lint/` と `lint:md` wiringは存在しないため、このreportのMarkdown wording checkは unsupportedであり、代替未定義checkerは実行していない。

## Documentation and tracking

Design reflection: none。破壊的変更なし。tracking/handoffは親が最終CI-delta同期を担当するため変更していない。この予約済みreportだけを新規作成して9 placeholderを埋めた。historical reportsは変更していない。

## Held items

`npm run test:t405` の唯一の非変更失敗（selected PR active-editor ownership）はheldであり、本CI-deltaのGreenとは読み替えない。Markdown wording checkはrepo wiring欠如のため unsupported。現在HEADに一致するCIは未実行・未待機であり、push後のexact-head `pull_request` CIが必要である。full local equivalence/Hostは本follow-upでは実行しない。

## Outcome

CIで観測したT405/T406二重UI errorのroot causeを最小変更で修正し、exact failing production seamはGreenになった。変更filesは `src/ui/review-contexts/vscode-review-contexts-runtime.ts`、`test/unit/t405-composition-regression.test.ts`、本report。次は親が差分を確認し、必要なreport/tracking sync、review/commit/push、exact-head CI gateを処理する。mergeは行わない。
