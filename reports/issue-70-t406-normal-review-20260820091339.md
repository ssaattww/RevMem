# T406 / Issue #70 / PR #71 初回通常レビューレポート

## メタデータ

- report type: `review_report`
- review mode: `initial_review`
- reviewer: Codex sub-agent `/root/t406_normal_review`
- reviewer independence: T406 の実装・修正・検証実行には参加せず、本 round は読み取り専用調査と予約済み report の更新だけを実施
- repository: `ssaattww/RevMem`
- Issue / PR: `#70` / `#71`
- branch / base ref: `task/t406-github-pr-integration` / `main`
- base / merge-base: `bd64d0a884ffe469eb4a8292ce09f03a64825144`
- reviewed implementation HEAD: `1658fc9aa6e629ef69e7fa09b25422316d326fc7`
- commit range: `bd64d0a884ffe469eb4a8292ce09f03a64825144..1658fc9aa6e629ef69e7fa09b25422316d326fc7`
- changed files: 11
- reserved report path: `reports/issue-70-t406-normal-review-20260820091339.md`
- persistence mode: `repository_file`
- report attestation allowed: `false`（通常 review のため `not_applicable`）
- verdict: `fail`

技術 verdict は上記 reviewed implementation HEAD にのみ適用する。本 report は通常 review 証跡であり、実装変更、commit、push、PR 操作、merge は行っていない。

## 目的と範囲

Issue #70、T406 / AC-11、設計 rev5、PR #71 の全 11 changed files、T401〜T405 / T305 の主要直接依存、T405 production composition、mock GitHub、cache、PR diff acquisition、closed / merged PR layer、CI diagnostic wiring、実装 report、README、tasks、phases を一度の全範囲 pass で確認した。

対象シナリオは、未認証 public、401 / 403 / 404 / 429 / network、patch missing、multiple / cancel / 0、GitHub unavailable 中の branch fallback と review operation、recovery 後の current immutable revision / cache、closed / merged coexistence と layer 既定 OFF、AC-11 isolation、CI wiring、API / data / cache / configuration / workflow / compatibility / error / security / privacy である。

対象外は T604 storage locking、T605 remote / multi-root、GitHub 実 service の非決定的試験、実装・tracking 修正、test / CI の起動・再実行・待機、commit / push / PR 更新 / merge である。

## 権威ある要件

- Issue #70 の Scope / Acceptance criteria / Non-goals。
- `tasks/tasks-status.md:335-340,366-382` の T401〜T406 と AC-11 traceability。
- `tasks/phases-status.md:129-145` の P4 目的・終了 checkpoint。
- 設計 rev5 §14、§16.2、§16.4、§16.10、§17、§20.4、§21。
- `reports/issue-70-t406-implementation-20260820090333.md` の実装・Red / Green・local validation 証拠。

## Inspected files

### Changed files 11 / 11

- `.github/workflows/ci.yml`
- `README.md`
- `package.json`
- `reports/issue-70-t406-implementation-20260820090333.md`
- `src/t405-review-contexts-runtime.ts`
- `tasks/phases-status.md`
- `tasks/tasks-status.md`
- `test/integration/mock-github.test.ts`
- `test/integration/t402-pr-diff-acquisition.test.ts`
- `test/unit/ci-workflow-contract.test.ts`
- `test/unit/t405-composition-regression.test.ts`

### 主要直接依存

- T401: PR resolver、fetch adapter、VS Code authentication provider、mock GitHub resolver / auth tests。
- T402: acquisition service、GitHub diff adapter、patch / content builders、boundary / follow-up tests。
- T403: cache service、exact request identity、source-redacted entry、filesystem generation pointer、cache tests。
- T404: PR context state service、immutable revision mapper、layer default / visibility override、persistence tests。
- T405: Review Contexts runtime / controller / UI、current PR inference、selection store、PR review runtime、lifecycle / revision / selected-session tests。
- T305: Current Context candidate selection、composition、coordinator、branch / PR ownership publication。
- CI: `tools/run-ci-command.mjs`、failure-context collection / artifact upload、CI contract test。
- 過去の T305 / T401〜T405 report と main 統合 commit。

## 実行・観測

本 reviewer は test、build、lint、CI を起動・再実行・待機していない。`git status / rev-parse / merge-base / diff / log`、`rg`、`Get-Content`、`gh issue view`、`gh pr view`、`gh run list` だけを使用した。

実装 report が記録する local evidence は `test:t406` 28 pass / 0 fail、build、contract typecheck、architecture positive / negative、lint、CI workflow contract 10 pass / 0 fail、`git diff --check` pass である。本 reviewer は再実行していない。

exact-head CI は 2026-08-20 09:18 JST の観測時点で成功していた。

- pull_request run `32316477214`: `headSha=1658fc9aa6e629ef69e7fa09b25422316d326fc7`, `completed/success`
- push run `32316462306`: `headSha=1658fc9aa6e629ef69e7fa09b25422316d326fc7`, `completed/success`

CI success は現在の test 内容が通ることを示すが、以下の missing scenario と semantic defect の解消証拠にはならない。

## Findings

### T406-R001 — High — branch fallback は saved PR が 1 件の通常ケースで成立しない

- origin: `introduced_by_change`（unavailable）および同じ risk family の in-scope latent sibling（0 / cancel）
- location: `src/t405-review-contexts-runtime.ts:819-867`, `src/application/review-contexts/current-pull-request-context.ts:15-27`, `test/unit/t405-composition-regression.test.ts:711-725`
- description: unavailable、not-found、cancelled は `currentPullRequestSelection.clear(...)` 後に Current Context を refresh する。しかし `findCurrentPullRequestContext` は preference がなくても、同一 repository / HEAD の persisted open PR が 1 件なら自動採用する。そのため saved open PR が 1 件の通常ケースでは同じ PR が直ちに再選択され、branch に戻らない。追加 test は PR #52 と #53 の 2 persisted matches を作るため `matches.length === 1` を通らず Green になる。同じ欠陥は network / HTTP unavailable、0 candidate、multiple candidate cancel に及ぶ。
- impact: Issue #70 の branch-context operation と zero / cancel branch fallback を満たさず、通常 editor ownership が stale PR のまま残り、branch state でなく PR reviewed state を表示・更新し得る。
- evidence: runtime line 865 は selection key の削除だけで persisted PR inference を抑止しない。helper line 27 は単一 match を preference なしに返す。T406 test は failure 前に 2 PR を persisted open にし、単一 saved PR と 0 candidate runtime flow を実行しない。
- required action: repository / immutable HEAD ごとに明示 branch / no-PR choice を表現する selection state、または同等に PR auto-inference を抑止する設計を追加する。saved PR 1 件の unavailable / 0 と、saved PR 1 件 + remote multiple の cancel を production composition で Red / Green 化し、Current Context と通常 editor ownership が branch になること、branch mark / unmark が PR state を変えないことまで確認する。

### T406-R002 — Medium — unavailable fallback が failure diagnostics を成功として隠す

- origin: `introduced_by_change`
- location: `src/t405-review-contexts-runtime.ts:819-867`, `src/ui/review-contexts/vscode-review-contexts-runtime.ts:171-191`, `doc/design/vscode-review-range-tracker-design.md:884-918`
- description: 変更後の unavailable は selection clear と refresh の成功経路へ入り、GitHub failure を active Operation Feedback へ渡さない。wrapper は command を `OK` として完了する。
- impact: non-blocking branch fallback でも network / API / rate-limit の原因と処理段階を Output から追跡できず、設計 rev5 §16.10 / §17.3 の fail-closed diagnostic contract に反する。
- evidence: `search.kind === "unavailable"` の reason はあるが、resolution 後の `else` は clear だけを行う。T406 test は `showErrorMessage` だけを収集し、active feedback host / Output failure entry を検証しない。
- required action: local operation を止めずに unavailable reason / stage を privacy-safe typed diagnostic として記録する。network / API / rate-limit が branch を公開しつつ Output に failure evidence を 1 回だけ残す composition test を追加する。

### T406-R003 — Medium — recovery 後の immutable context / cache 再同期を実行していない

- origin: `omitted_requirement`
- location: `test/unit/t405-composition-regression.test.ts:533-646,719-725`, `reports/issue-70-t406-implementation-20260820090333.md:17,54,63`
- description: test 名と report は cache recovery、`live/offline/live` 復帰、current immutable revision への再同期を主張するが、sequence は failure 前の initial live mapping、stale offline cache、意図的 live write failure (`not-cached`)、最後の network discovery failure で終わる。failure 後の successful live step、別 immutable HEAD への mapping、new revision に束縛された fresh cache readback がない。
- impact: 「After recovery, PR context and cache refresh to the current immutable revision」の証拠がなく、old revision cache 再利用、Context / Global / file revision 不一致、stale / not-cached 固着が focused suite を通過できる。
- evidence: initial `sourceHeadSha -> targetHeadSha` mapping は最初の live redetect で outage 前。offline 後は cache write failure を assert し、discovery を network にした後は復旧せず test が終わる。
- required action: live revision A / cache A → outage / exact offline A → recovered live revision B を production composition で実行し、PR Context、Global、per-file revision、registered snapshot、fresh cache entry / pointer が exact BASE / HEAD B に一致し、A cache を B として使わないことを assert する。follow-up report で過大 claim を訂正し、歴史的 report は書き換えない。

### T406-R004 — Medium — AC-11 reviewed-state isolation が end-to-end で固定されていない

- origin: `omitted_requirement`
- location: `test/unit/t405-composition-regression.test.ts:559-577,591-685`, `tasks/tasks-status.md:340,372`, `reports/issue-70-t406-implementation-20260820090333.md:17,54,63`
- description: AC-11 assertion は PR #52 の layer toggle が PR #53 の layer boolean を変えないことだけを確認する。production diff command で PR #52 ranges を変更した後、PR #53 の modified / original ranges が不変であること、逆方向、restart 後の isolation を assert しない。
- impact: layer preference は分離していても diff URI / selected context / repository target の誤 routing で別 PR state が汚染される regression を検出できず、T406 が主担当の AC-11 end-to-end 証拠にならない。
- evidence: `isolated53` への assertion は `isPullRequestDecorationEnabled(...) === true` だけ。mark / unmark 後に load / assert するのは PR #52 だけである。
- required action: PR #52 / #53 に異なる初期 ranges を用意し、selection / canonical diff / normal-editor production seam から片方ずつ操作する。各 transaction と restart 後に対象だけが変化し sibling の modified / original ranges と metadata が不変であることを assert する。shared Global の意図した更新は別 assertion にする。

### T406-R005 — Low — current tracking が commit / PR #71 と不一致

- origin: `stale_tracking`
- location: `tasks/tasks-status.md:10-17,340,386`, `tasks/phases-status.md:40-41,139`
- description: tracking は `Pull Request: 未作成`、`review・commit・PR作成前`、次工程に `commit/PR工程` と記載するが、reviewed HEAD は origin に push 済みで PR #71 が draft / open である。
- impact: restart / handoff で Git / PR lifecycle を誤認し、重複 PR 操作や誤った next action を選ぶ可能性がある。
- evidence: `gh pr view 71` は base `main`、head branch、head OID `1658fc9...`、`OPEN`、draft を返し、local branch は matching origin branch を追跡する。
- required action: findings 対応後に `progress-sync-manager` で PR #71、current HEAD、通常 review fail / finding IDs、次工程を同期する。歴史的 implementation report は保持する。

## Scenario dispositions

| Criterion | Disposition | Evidence |
| --- | --- | --- |
| unauthenticated public PR | `checked_no_finding` | token なし fetch / resolver と production composition |
| 401 / 403 / 404 / 429 | `checked_no_finding` | mock HTTP classification と resolver branch result |
| network interruption | `checked_finding` | lower classification は正しいが runtime は T406-R001 / R002 |
| patch missing fallback | `checked_no_finding` | exact BASE / HEAD contents と `github-content` identity |
| multiple explicit selection | `checked_no_finding` | production quick-pick → PR #53 → Current Context ownership |
| cancel / zero | `checked_finding` | single saved PR sibling が T406-R001 |
| branch operation while unavailable | `checked_finding` | single saved PR では branch に戻らず、operation proof も欠落（T406-R001） |
| closed / merged coexistence / persistence | `checked_no_finding` | lifecycle refresh、group、restart、両 context persistence |
| closed / merged layer default OFF | `checked_no_finding` | T404 helper と T405 composition |
| recovery to immutable revision | `checked_finding` | post-failure successful transition 欠落（T406-R003） |
| exact cache identity / source redaction | `checked_no_finding` | T403 request matching、redaction、generation pointer |
| cache recovery | `checked_finding` | stale offline と live write failure まで（T406-R003） |
| AC-11 PR isolation | `checked_finding` | layer は分離、reviewed state proof 欠落（T406-R004） |
| CI diagnostic wiring | `checked_no_finding` | package command、runner、workflow、failure artifact |
| unrelated change / PR scope | `checked_no_finding` | 11 filesは T406 scope 内 |

## Required coverage dispositions

| Required coverage | Disposition | Evidence |
| --- | --- | --- |
| requirement and design conformance | `checked_finding` | T406-R001〜R004 |
| correctness and edge cases | `checked_finding` | T406-R001 |
| scope discipline and unrelated changes | `checked_no_finding` | 1 commit / 11 files、T406 scope 内 |
| changed files and direct dependencies | `checked_finding` | 11 / 11、T305 / T401〜T405、T406-R001〜R004 |
| API, data, configuration, workflow, compatibility | `checked_finding` | public API / schema / config 変更なし、workflow 整合、selection behavior は R001 |
| error handling and failure diagnostics | `checked_finding` | T406-R001 / R002 |
| security, secret handling, privacy | `checked_no_finding` | token / source persistence・logging 追加なし |
| tests and validation adequacy | `checked_finding` | T406-R001 / R003 / R004 |
| current-HEAD CI evidence | `checked_no_finding` | exact-head push / pull_request CI success |
| report, tracking, documentation accuracy | `checked_finding` | R003 / R004 の過大 claim、R005 |
| regression and maintainability risks | `checked_finding` | R001〜R004 の sibling regression |

## Validation assessment

- local validation: implementation report 由来。reviewer 再実行なし。
- exact-head CI: `checked_no_finding`。2 run とも reviewed HEAD に一致して success。
- TDD evidence: report の Red 27 pass / 1 fail → Green 28 pass / 0 fail。ただし Red fixture は 2 persisted PR のため R001 の通常ケースを再現しない。
- CI wiring: `checked_no_finding`。`test:t406` は compile 後に mock GitHub、T402 acquisition、T405 composition を実行し、workflow は diagnostic runner 経由。
- Markdown wording check:
  - target root: `C:\Users\taiga\source\repos\RevMem-t406`
  - target: 本 report
  - focused / full: `unsupported` — `tools/lint/`、focused wiring、`lint:md` がない。
  - aggregate: `unsupported`。非 blocking held item とする。

## Held items

1. `H406-001` — Markdown wording lint は repository-local wiring 不在で `unsupported`。owner は repository tooling policy。
2. `H406-002` — implementation report の `npm ci` は既存 dependency に high severity 4 件を報告した。`package-lock.json` に T406 差分はなく dependency update は scope 外のため、既存 security backlog / release gate に保持する。

## Unexplored / unknown / not applicable

- unexplored: なし。指定された全 criterion と sibling risk family に disposition を付けた。
- unknown: なし。HEAD、base、merge-base、changed files、PR、CI は解決済み。
- not applicable: independent-final-review freeze / attestation、T604、T605、live GitHub credential test。

## Intentionally untouched

- implementation、tests、workflow、README、tasks、phases、Design、BreakingChanges、historical implementation report。
- test / CI の起動・再実行・待機。
- GitHub issue / PR / review / comment / draft state。
- commit、push、merge、branch cleanup。

## Verdict

`fail`

Required findings は High 1 件、Medium 3 件、Low 1 件。T406-R001 により single saved PR、0、cancel の branch fallback が成立せず、Issue #70 の中心 acceptance criterion を満たさない。R002〜R004 により diagnostics、post-outage immutable recovery、AC-11 reviewed-state isolation の証拠も不足する。

## Remaining risks

- selection state 修正は single / multiple inference と restart persistence を変えるため、explicit PR / explicit branch / 0 / 1 / multiple を同じ matrix で固定する必要がある。
- recovery は cache key だけでなく Context / Global / per-file revision / registered snapshot の同一 immutable identity を確認する必要がある。
- isolation は context-local state と intentional shared Global effect を別 assertion にする必要がある。
- CI は exact-head success だが missing scenarios の代替にならない。

## Next action

T406-R001〜R005 を同一 follow-up batch で対応し identity / severity を保持する。R001 sibling matrix、R002 privacy-safe Output、R003 outage → successful recovery / new immutable revision、R004 two-way reviewed-state isolation を Red / Green 化し、follow-up report と tracking を同期する。その後、同じ normal reviewer に今回 finding だけの fix verification を依頼する。fix verification では新規 finding 探索を行わない。
