# T605 normal review report

## タスク

T605 / Issue #74 / draft PR #75 の initial normal review を、reviewer `sol`（reasoning effort `high`）として一度だけ全範囲で実施した。repository は `ssaattww/RevMem`、review branch は `review/t605-normal`、reviewed implementation HEAD は `c8a24250fee63931e09886a2ff229a2c2c3b9586`、base は `origin/main` の `64e47c590960a810a2439bd33f250ecbda9c41bf`、review range は `64e47c590960a810a2439bd33f250ecbda9c41bf...c8a24250fee63931e09886a2ff229a2c2c3b9586` である。PR head SHA と local reviewed HEAD は一致した。この技術 verdict は reviewed implementation HEAD にだけ適用する。

## sub-agentを使う理由

使わない。親の明示指示に従い、実装者と異なる通常 reviewer が単独で review した。実装、self-fix、test / build / lint の再実行、CI の起動・待機、GitHub 更新、commit、push、merge は行っていない。

## 対象範囲

Authoritative source は Issue #74、draft PR #75、`doc/design/document-context-routing.md`、`Design/BreakingChanges.md`、`tasks/tasks-status.md`、`tasks/phases-status.md`、implementation report、implementation handoff の順に照合した。base...HEAD の全 changed file、production activation、直接 consumer、wrapper composition、storage/history/snapshot/lock/cleanup、Current Context、Git/PR acquisition、package/CI wiringを追跡した。

| Criterion | Disposition | Evidence / result |
| --- | --- | --- |
| Issue / design conformance | `checked_finding` | R001、R003、R004、R006。 |
| Correctness / edge cases | `checked_finding` | nested root、same-name、same repository identity、add/remove、URI suffix、remote authority、restartを確認し、R001〜R004。 |
| Scope discipline / unrelated change | `checked_no_finding` | 変更は T605 と tracking/report 同期に限定。 |
| 全 changed file / direct dependency / consumer | `checked_finding` | production document-provider wrapper、T405 root consumer、startup migrationまで追跡し、R001〜R004。 |
| Activation / composition / dispose | `checked_finding` | registry の add/remove 呼出しは存在するが、capability を wrapper が保持せず R001、R002。 |
| URI identity / longest match / nested / same-name / scheme / authority | `checked_no_finding` | helper 自体は scheme・authority・path semantics・一意な最長一致・query/fragment fail-closedを実装。 |
| Non-Git state / history / snapshot / storage route / lock / cleanup | `checked_finding` | root route は分離するが snapshot commit と startup migration に R001、R004。 |
| Git / PR state and acquisition | `checked_finding` | state の global route は維持するが Git recovery は R002、same-repository multi-root PR root は R003。 |
| Workspace-side Extension Host / file operations | `checked_no_finding` | `extensionKind: ["workspace"]`、workspace-side `process.platform`、URI/fsPath、Node Git adapter を維持。 |
| Failure diagnostics / security / privacy | `checked_no_finding` | 新規 secret 出力なし。Issue/design で承認された threat model 内の path guard / privacy-safe lock diagnostic を維持。same-host malicious syscall 間 swap は要求していない。 |
| Backward compatibility / breaking change | `checked_finding` | legacy state 非移行は記録済みだが、新 root の startup migration regression は R004。 |
| Tests / package / workflow / current-head validation | `checked_finding` | R005、R006。 |
| Reports / handoff / tasks / phases / README | `checked_no_finding` | reviewed HEAD までの実装状態と normal-review pending を表現。 |
| Regression / maintainability | `checked_finding` | T601/T602/T603 consumer contract regression と coverage gap が R001、R002、R004、R006。 |

## 対象外

Remote SSH / Dev Containers / Codespaces service の起動・network E2E、T606 以降、independent final review、report attestation、merge、live credential / external service write は Issue #74 の対象外として `not_applicable`。同一 host の悪意ある syscall 間 ancestor swap/native primitive 保証も承認済み threat model 外として `not_applicable`。後続 review は本 report の finding identity に限定した closure とし、新規観点の full-scope review は行わない。

## 実行コマンド

Review evidence の取得に限り、`git status`、`git rev-parse`、`git diff --name-status`、`git diff`、`git show`、`rg`、`Get-Content`、`gh issue view 74`、`gh pr view 75`、`gh run view <run-id> --json`、`gh run view 32369353292 --log-failed` を read-only で使用した。

実装者提供 local evidence は `npm run test:t605` 14/14、`npm run build`、`npm run compile:test`、`npm run typecheck:contracts`、`npm run lint`、`npm run validate:architecture`、`npm run validate:architecture:negative`、`git diff --check` success との report/handoff 記録であり、この review では再実行していない。matching exact-head CI は pull-request run `32369353292` と push run `32369349221`。両方とも build、contract typecheck、architecture 正負、lint までは success、Unit tests は 540/541 で failure、後続 T602〜T605 focused step は skipped だった。失敗は `test/unit/state-repository.test.ts:220` の legacy workspace root expectationである。Markdown word check は repository に `tools/lint/`、`lint:md`、`cspell.config.jsonc` がなく、focused/full とも `unsupported`。コマンドは実行しておらず pass には変換しない。

## 対象ファイル

全 changed file を確認した: `.github/workflows/ci.yml`、`Design/BreakingChanges.md`、`README.md`、`doc/design/document-context-routing.md`、`handoffs/issue-74-t605-implementation-20260820211845.yaml`、`package.json`、`reports/issue-74-t605-implementation-20260820211845.md`、`src/adapters/document-review-state/document-review-state-session-provider.ts`、`src/adapters/state-repository/storage-router.ts`、`src/adapters/workspace-review-state/index.ts`、`src/adapters/workspace-review-state/workspace-review-state-session-provider.ts`、`src/adapters/workspace-review-state/workspace-root-runtime-registry.ts`、`src/application/workspace-identity/index.ts`、`src/application/workspace-identity/workspace-identity-service.ts`、`src/extension.ts`、`src/t305-extension.ts`、`tasks/phases-status.md`、`tasks/tasks-status.md`、`test/unit/ci-workflow-contract.test.ts`、`test/unit/t605-multi-root-remote-boundaries.test.ts`。

直接依存 / consumer として `persisted-document-review-state-session-provider.ts`、`reconciled-document-review-state-session-provider.ts`、`git-context-document-review-state-session-provider.ts`、`snapshot-tracking-workspace-review-state-session-provider.ts`、`persistence-startup-migration.ts`、`node-non-git-snapshot-adapters.ts`、state repository / JSONL history route consumers、`t305-current-context-git.ts`、`t405-review-contexts-runtime.ts`、Current Context selection key、`test/unit/state-repository.test.ts` を確認した。

## 指摘事項

- `T605-R001` — severity `high` — origin: normal review; location: `src/extension.ts:327-356`、`src/adapters/workspace-review-state/workspace-root-runtime-registry.ts:29-96`、`src/adapters/document-review-state/reconciled-document-review-state-session-provider.ts:216-240`。description: production は `SnapshotTrackingWorkspaceReviewStateSessionProvider` を registry で包むが、reconciliation wrapper は concrete provider の `instanceof` が true の場合だけ `commitWithSnapshot` を使用する。registry は port の `open/loadForDecoration` しか公開しないため判定が false となり、workspace command session の committer は plain state repository に置換される。impact: initial open が empty latest snapshot を保存した後の mark/unmark は state だけを進め snapshot pointer を更新しない。直後の decoration/reopen は古い snapshot を authoritative として reviewed evidence を消し、non-Git workspace の中心機能が production composition で保持されない。evidence: registry 追加前の direct provider が提供していた capability を wrapper が伝播せず、新 T605 test はこの production path を呼ばない。required action: root-selected runtime の snapshot-aware commit capability を型付き port として registry から伝播し、reconciliation が concrete-class `instanceof` に依存せず実 root の `commitWithSnapshot` を呼ぶことを production-level Red/Green で固定する。
- `T605-R002` — severity `high` — origin: normal review; location: `src/extension.ts:327-375`、`src/adapters/document-review-state/persisted-document-review-state-session-provider.ts:36-79,123-140`。description: persisted document provider は `workspaceProvider.historyRewriteSnapshotTracker` が存在するときだけ T602 `GitHistoryRewriteRecoveryCoordinator` を登録するが、新 registry はこの capability を持たない。impact: Git document の recovery snapshot capture と、rebase/force-push後に旧objectが失われた場合の T602 conservative recovery が production 全体で無効になる。これは root 分離と無関係な既存 Git behavior の regression。evidence: constructor は registry を受けて `snapshotTracker === undefined` となり、registration branchへ入らない。required action: Git history-rewrite用 tracker を workspace root registryとは別の安定した production capabilityとして保持するか、複数 tracker を明示的にrouteできる契約へ変更し、実 production compositionで registration/capture/recovery が継続する regression test を追加する。
- `T605-R003` — severity `high` — origin: normal review; location: `src/t405-review-contexts-runtime.ts:226-258,302-359,516-534,625-681`。description: T405 consumer は local roots を `Map<repositoryId, repositoryRoot>` に格納し、Current Context candidate key も branch は `repositoryId+branchRef`、PR は `contextId` だけで、`repositoryRoot` を含めない。同じ repository identity / branch を持つ二つの workspace root は一方へ collapseし、後勝ち root が PR local acquisition/read sourceになる。impact: root A の Current Context / PR operation が root B を選ぶ、または active editorとのroot assertionで失敗し、Issue #74 の root A/B Git/PR acquisition 非混線を満たさない。evidence: base Current Context key は root を含む一方、augmentation時の `candidateKey` と `roots` は除外する。新 test は異なる non-Git authorityだけで、same-repository Git rootsを扱わない。required action: repository root を candidate/action/acquisition identityへ保持し、同一 repositoryId の複数rootを上書きしない。rootがactionから一意に得られない場合はactive document selectionへ束縛してfail-closedにし、same repository/branchのmulti-root PR/Current Context testを追加する。
- `T605-R004` — severity `medium` — origin: normal review; location: `src/adapters/state-repository/storage-router.ts:37-57`、`src/adapters/persistence-startup-migration.ts:57-90,160-205`、`src/t305-extension.ts:80-94`。description: runtime route は `storageUri/workspaces/<hash>` へ移動したが startup migration は legacy `storageUri/workspace-state.json` と legacy root history/snapshotsだけを処理し、`workspaces/` 子 root を列挙しない。impact: root-scoped state/history/snapshotのschema migration、全 historical month、corrupt metadata quarantine、root lock transactionがactivation時に実行されず、T603/T604のeager startup contractを新layoutで失う。evidence: global `repositories` / `external-files` はhashed childを列挙するが workspace collectionには対応ループがない。required action: trusted `storageUri/workspaces` 下の64-hex rootを列挙し、各rootのidentity検証、state/history/snapshot migrationをそのroot lock下で実行する。複数root、corrupt history/snapshot、unrelated root非変更、restartをproduction startup testで固定する。
- `T605-R005` — severity `medium` — origin: exact-head validation evidence; location: `test/unit/state-repository.test.ts:181-237`、CI runs `32369353292` / `32369349221`。description: breaking storage route に対する既存 unit contract expectation が更新されておらず、workspace root を直接 `storageUri` と期待する。impact: exact-head default Unit tests は 540/541 で failureし、workflowは T605 focused stepを含む後続を全skipするため、reviewed HEADにcomplete Green evidenceがない。required action: accepted `storageUri/workspaces/<repository-id-hash>` contractへ既存 testを更新し、異なる workspace repository IDs のroot separationもassertしたうえで、全 finding fix後にdefault unitとexact-head PR CIをGreenにする。
- `T605-R006` — severity `medium` — origin: normal review; location: `test/unit/t605-multi-root-remote-boundaries.test.ts:1-65`、`package.json` の `test:t605`、`test/unit/ci-workflow-contract.test.ts:230-244`。description: T605固有 test は helper longest-match/fail-closed と二つのstorage routeだけで、14 passingという件数の残りは既存 wiring/design testsである。registry add/remove/dispose、production activation、state/history/snapshot/lock/cleanup isolation、snapshot-aware commit、startup migration、same-repository Git/PR rootsを実行しない。impact: Issue #74 の production composition と root A/B 非混線 acceptance が未証明で、R001〜R004を検出できない。required action: production seamまたは同じ concrete wrapper chainを使い、multi-root add/remove/nested/same-name、remote scheme/authority、review mark/reopen/history/snapshot/lock/cleanup、Git/PR acquisition、startup/restartをfocused suiteに追加し、CI wiring contractがその全suiteを必須実行することを固定する。

## 結果

Verdict は `fail`。required findings は 6件（high 3件、medium 3件、low 0件）。全 required criterion は `checked_no_finding`、`checked_finding`、`held`、`not_applicable` のいずれかへ処分済みで、`unexplored: none`。reserved report path は `reports/issue-74-t605-normal-review-20260820213218.md`、persistence mode は normal review の repository file、`report_attestation_allowed: false`。reviewed HEAD は変更していない。

Next action は実装 owner が T605-R001〜R006 を同一batchで修正し、local validationと新 exact-head CI evidenceを取得すること。その後は同じ normal reviewerによる finding-limited closure のみを行い、finding identity と severity を維持する。

## リスク

Held は二つ。`H605-001`: Markdown wording check は repo-local wiring不在で `unsupported` のため、自動用語検査漏れを明示保持する。`H605-002`: merge gate の exact-head pull-request CI Green は実装修正・commit/push後の owner responsibility。現 reviewed HEAD の pull-request / push CI は既に Unit failureであり、reviewerは再実行・待機していない。Remote service/network E2E と T608 final acceptance は本 Issue の `not_applicable` scopeで、required findingやheld CIへ変換しない。remaining risk は実環境固有のRemote service behaviorだがIssue #74の承認済み非目標。`unexplored: none`。
