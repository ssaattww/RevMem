# PR #94 CI1 normal finding follow-up

## Finding

PR94-CI1-NR-001 High。base-only transitionがfull immutable snapshot hitを通ると、early returnがold-base `originalReviewedByDiff`を残していた。

## Red

- `npm run compile:test` passed, then direct store/history coverage had 25 tests: 24 passed and 1 failed.
- The new actual `GitHubPullRequestContextStateService` fixture used a base-only transition, empty Global files, and exact Context/Global target snapshots. It committed once and recorded `exact-revision-snapshot-restored`, but incorrectly retained the old `base..head` range.

## Change

- `src/application/github-pr-context/immutable-pull-request-revision-mapper.ts`: computes base-only state before the full-hit early return. A base-only full restore now clears only `originalReviewedByDiff`; modified ranges, exact snapshots, Global state, and restored disposition remain intact.
- `test/unit/github-pr-context-layer-store.test.ts`: adds the actual store/history regression fixture. It proves one CAS, restored history reason, retained modified ranges, empty Global preservation, and old-base original-range removal.

## Green

- Direct finding set: `npm run compile:test` plus layer-store, T404, and immutable snapshot tests — 25 passed, 0 failed.
- PR94 regressions: projection, Issue92 selection, diff command service, immutable snapshot, and T405 evidence — 27 passed, 0 failed.
- `npm run build`, `npm run lint`, and `git diff --check` — pass.
- No workflow, performance, `test:t607`, commit, push, or CI wait action occurred.
- Markdown focused lint is unsupported: the repository has neither `tools/lint/` configuration nor a `lint:md` script, and no lint setting was changed.
