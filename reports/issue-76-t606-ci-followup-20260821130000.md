# T606 CI follow-up report

## タスク

Issue #76 / PR #77 の exact-head CI follow-up。attestation HEAD `b747c80d62ac293f3a45f8bc932154e2e72421b2` は CI run `32431194872` / Unit tests job `96622916836` の失敗によって無効化された。CI follow-up technical HEAD は `2a2b59fe0cf8a9f65c79fd87ebd50386ae6a488e` である。previous technical R6 HEAD は `ce584b29e6f584234c7bab050d24d2dd163ae3d3`、previous final admin target は `8a3c86c89fcfea91d18ee621d42d9d178a1425c0`、previous attestation HEAD は `b747c80d62ac293f3a45f8bc932154e2e72421b2` である。

## sub-agentを使う理由

指定されたCI failure follow-up の実装・focused validation・管理記録だけを行う。self-review、CI再実行・待機、same independent reviewer verification、commit、push、PR更新、merge は実施しない。

## 対象範囲

- `PR68-R003` の stale A success、stale A failure、leaving PR context、same-context re-registration の4テストを、superseded `activateProgress` が `OperationCancelledError` をtyped rejectionとして返す契約へ同期する。stale publish抑止のassertionを維持し、失敗するdeferred promiseが非捕捉にならないよう消費する。
- `PR69-R001` のGlobal file-open testを、direct controller は stale nodeに `RangeError` をrejectし、実際に登録されたcommandがgeneric UIとredacted terminal feedbackを所有する契約へ同期する。
- current product実装は変更しない。CI follow-up report/handoff、README、tasks、phasesには、exact-head CI failure addressed、same independent reviewer CI-delta verification pending、technical HEAD `2a2b59fe0cf8a9f65c79fd87ebd50386ae6a488e` のPR body external sync completed、parentによるresulting final admin HEAD refresh pending、new attestation pending を記録する。

## 対象外

- 新規scope、self-review、新規finding、design、product実装、CI再実行・待機、same independent reviewer verification、PR body更新、commit、push、merge。
- full unitで検出されたT606と無関係なWindows path ownership 19件とSIGTERM/SIGKILL timing 1件の修正。

## 実行コマンド

- Red: `npm run compile:test; node --test test-dist/test/unit/issue-66-pr68-review-findings.test.js test-dist/test/unit/t505-refresh-invalidation.test.js` は 13 tests、8 pass / 5 fail。失敗5件はCI記録と一致し、stale failureには旧testの非捕捉rejectionも出た。
- Green: `npm run compile:test; node --test test-dist/test/unit/issue-66-pr68-review-findings.test.js test-dist/test/unit/t505-refresh-invalidation.test.js test-dist/test/unit/t606-r5-production-activation.test.js` は 15 pass / 0 fail。非捕捉rejectionなし。
- Required full: `npm run test:unit` は 543 tests、521 pass / 20 fail / 2 skipped。20 failは対象外（Git owner path 19、SIGKILL timing 1）であり、Greenとは扱わない。
- `npm run build`、`npm run typecheck:contracts`、`npm run lint`、`npm run validate:architecture`、`npm run validate:architecture:negative`（expected 11 violations）は各1回pass。`git diff --check` はcode validation時と管理記録更新後の2回ともpass。
- `tools/lint/`、`lint:md`、Markdown target/whitelist/prh/cspell wiringは存在しないためMarkdown wording checkはunsupported/held。CIは起動・待機していない。

## 対象ファイル

- `test/unit/issue-66-pr68-review-findings.test.ts`
- `test/unit/t505-refresh-invalidation.test.ts`
- `test/unit/t606-r5-production-activation.test.ts`
- `README.md`
- `tasks/tasks-status.md`
- `tasks/phases-status.md`
- `reports/issue-76-t606-ci-followup-20260821130000.md`
- `handoffs/issue-76-t606-ci-followup-20260821130000.yaml`

## 指摘事項

- `activateProgress` は既にsupersede / clear / re-registrationで `OperationCancelledError` をrejectする。4旧テストはresolveを期待していたためCI失敗した。stale A failure は開始済みdeferred readをsettleしてrejection handlerで消費し、unhandled rejectionを発生させない。
- `GlobalUnderstandingFileOpenController` はdirect boundaryで `RangeError` とopen failureをrejectする。reportingは登録済み `reviewRange.openGlobalUnderstandingFile` commandのgeneric UI boundaryであり、private error/pathをUI/terminalへ露出しない。production command testはgeneric messageと`Operation failed; details were redacted.`を固定した。
- source product変更は0。previous independent review identitiesとattestationはrequired test-record修正により再利用できない。same independent reviewerのCI-delta限定verificationと新attestationはpendingである。technical HEAD `2a2b59fe0cf8a9f65c79fd87ebd50386ae6a488e` のPR body external syncはcompletedし、parentがresulting final admin HEADをrefreshする。

## 結果

**CI failure addressed locally; not merge-ready.** CIの5件はfocused Greenになり、static gatesはGreenである。一方、required full unitは対象外20 failuresによりGreenではないためheldとする。attestation `b747c80d62ac293f3a45f8bc932154e2e72421b2` は無効であり、technical HEAD `2a2b59fe0cf8a9f65c79fd87ebd50386ae6a488e` に一致するCI rerun、same independent reviewer CI-delta verification、予約済みではないnew attestation lifecycleが必要である。PR body external syncはtechnical HEADに対してcompletedし、parentがresulting final admin HEADをrefreshする。CI run `32431194872` / job `96622916836` はprevious attestation HEADだけのfailure evidenceであり、technical HEADのsuccessではない。

## リスク

- full Unit 20 failuresはcurrent Windows local environmentで未解決。対象外のため変更せず、required full Greenを主張しない。
- technical HEAD `2a2b59fe0cf8a9f65c79fd87ebd50386ae6a488e` のexact-head CI rerunはheld。CI起動・待機はparent ownerの次工程である。
- previous reviewer/attestationはtechnical修正に対して無効。same independent reviewerのCI-delta verificationとnew attestationがpendingである。PR body external syncはcompleted済みだが、parentによるresulting final admin HEAD refreshはpendingである。
- Markdown wording checkはrepository wiring不在でunsupported/heldである。
