# PR #108 PRODUCT-001 fix evidence

- Mode: review follow-up; no independent review verdict and no merge.
- Starting PR HEAD: `b157ab14fcdf1ad81019fcfc0e219cf9f0efc09a`.
- Source tree: `281d79303262294ff8cdb71dac4d843a55bb83b8`, reconstructed from connector-provided archives and checked against GitHub. No CI-driven source updates.
- Scope: PRODUCT-001 in `reports/2026-09-05-pr108-product-impact-action-scope.md`.

## Change

Keep each PR's immutable mapping source distinct from the captured owner-current Global. Pass the prepared source to evidence acquisition rather than reloading the owner-current Global. When the historical Global snapshot is absent, use an empty temporary Global projection only for Context mapping, and require the separate mapper to derive the published owner Global from its authoritative current state. Metadata-only updates use the actual current Global. A missing source without that independent mapper fails closed. Correct the undefined `clone` call in the earlier runtime composition to `structuredClone`.

## Actual production regression

`test/helpers/pr108-production-fixture.ts` uses real Git commits, filesystem/debounced state repositories, immutable mapper, JSONL history, T405 registration and registered commands. Only the external VS Code host and GitHub HTTP responses are mocked.

`test/unit/pr108-product-001-production.test.ts` verifies B/B/B -> C/B/C -> C/D/D, one owner CAS per transition, retained review ranges, history, retry idempotence, restart, and Global-already-advanced cases both with and without a historical source snapshot. The first test deliberately checks the durable owner boundary separately from the still-open PRODUCT-003 projection defect; it does not attest that defect is fixed.

## TDD and verification

- Before implementation: `product001-production-red` reported 0 pass / 3 fail.
- After implementation: `product001-production-green` reported 19 pass / 0 fail / 0 skip, covering the new production regression plus both existing Issue106 owner suites.
- `git diff --check`: passed.
- Local commands: `node /mnt/data/pr108-transpile-local.cjs`; `node tools/run-ci-command.mjs product001-production-green node --test test-dist/test/unit/pr108-product-001-production.test.js test-dist/test/unit/issue-106-t405-owner-synchronization.test.js test-dist/test/unit/issue-106-global-three-way-synchronization.test.js`.
- Local runtime: Node 22.16.0, TypeScript 5.8.3 transpile-only. Dependency installation did not complete; these results are not build/typecheck/lint or full CI-equivalence evidence.
- Local diagnostics: `test-output/ci/product001-production-{red,green}.*` contain result JSON, stdout, stderr and combined logs.
- Starting-HEAD CI run 33964976162 matched `b157ab1` and failed on the earlier undefined `clone` call. It is not evidence for this pending commit.
- Existing CI diagnostics workflow already retains test results/stdout/stderr/failure context; no workflow change was needed.
- Commit/push pending at report generation; resulting SHA is recorded in the subsequent PR summary. Updated-HEAD CI remains to be checked after publication.
- PRODUCT-002/003/004 remain separate follow-up changes; PRODUCT-005 was already committed.

## Concurrent update integration

Before publication, PR HEAD advanced to `c68c88531a94b4e76905e4fa660d996dce52c231`. Its source tree `a4964f6e718c46dc9957f275111830fbea9a7eac` was reconstructed and verified. Preserve the added `test/support/t405-owner-product-fixture.ts`, `test/unit/issue-106-product-composition.test.ts`, CI test entry and identical `structuredClone` correction. The original unpublished commit is not pushed. The replacement commit uses `c68c8853` as its sole parent. Integrated focused execution `product001-integration-green` passed 20/20 tests, including the concurrently added production regression.
