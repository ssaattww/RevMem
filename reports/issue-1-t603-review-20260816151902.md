# T603 初回通常レビュー報告

## メタデータ

- Repository: `ssaattww/RevMem`
- Pull Request: #53 `T603 schema migration・破損隔離・回復`
- Review mode: `initial_review`（通常レビュー）
- Branch: `task/t603-schema-migration-recovery`
- Base: `main` (`146aec15783294da1795f268315c85d1a0dffa56`)
- Reviewed implementation HEAD: `6a7d66e2cfbdfe226b8e958151b318ced83e6d13`
- Reviewed range: `146aec15783294da1795f268315c85d1a0dffa56..6a7d66e2cfbdfe226b8e958151b318ced83e6d13`
- Reviewer: ChatGPT T603 normal reviewer（このchat、2026-08-16）
- Verdict: **fail**
- Generated at: `2026-08-16T15:19:02+09:00`

PR本文には次工程を「独立review」とする記載があるが、implementation handoffは「separate normal review against its current post-report HEAD」を要求しており、独立最終review用の予約済みreport pathも存在しない。配布skillのreview lifecycleに従い、本レビューは独立最終reviewではなく初回通常レビューとして実施した。

本レビューでは実装・テスト修正、task tracking更新、mergeを行っていない。

## 1. タスク・設計要件

T603の終了条件は `tasks/tasks-status.md` で次のように定義されている。

- schema migration chainを実装する。
- migration前backupを作成する。
- JSON / JSONL / snapshotの破損を検出・隔離・回復する。
- 旧schema fixtureを段階移行できる。
- migration失敗時はbackupから戻す。
- 不確実な範囲を未確認にする。

設計書rev4 §15.3は、全保存modelに`schemaVersion`を持たせ、起動時に段階移行し、移行前backupを作成し、破損dataを隔離して不確実な範囲を確認済みにしないことを要求する。T603はAC-22、AC-23、AC-24にも関係する。

## 2. 確認範囲

### PR変更ファイル（全9ファイル確認）

- `.github/workflows/ci.yml`
- `handoffs/issue-1-t603-implementation-20260816150000.yaml`
- `reports/issue-1-t603-implementation-20260816145900.md`
- `src/adapters/non-git-snapshots/node-non-git-snapshot-adapters.ts`
- `src/adapters/state-repository/jsonl-review-history-store.ts`
- `src/adapters/state-repository/persistence-schema-recovery.ts`
- `src/adapters/state-repository/validated-file-system-review-state-repository.ts`
- `test/unit/review-history-jsonl-store.test.ts`
- `test/unit/t603-schema-migration-recovery.test.ts`

### 主要直接依存・契約

- `src/adapters/state-repository/file-system-review-state-repository.ts`
- `src/adapters/state-repository/coherent-file-system-review-state-repository.ts`
- `src/adapters/state-repository/owner-global-state-loader.ts`
- `src/adapters/state-repository/contracts.ts`
- `src/adapters/state-repository/index.ts`
- `src/core/contracts/review-state.ts`
- `src/core/contracts/schema-version.ts`
- `src/core/review-history/review-history-event-codec.ts`
- `src/application/non-git-snapshots/index.ts`
- `tasks/tasks-status.md`
- `doc/design/vscode-review-range-tracker-design.md`
- PR #53 metadata/comments、implementation report/handoff、GitHub Actions metadata/artifacts

## 3. Reviewer continuity / independence

- このreviewerはPR #53の実装を行っていない。
- このreviewerはPR #53のreview finding修正を行っていない。
- このreviewerは本round以前のT603通常reviewerではない。
- implementation workerのhandoffを入力として使用したが、finding判定はcurrent HEADの実ファイル・直接依存・GitHub API上のworkflow/commit/artifact証跡を再確認して行った。
- 本roundは通常レビューであり、独立最終review verdictではない。

## 4. TDD / CI / failure diagnostics

### 4.1 TDD Red

Test-only HEADは `3c88a86bd0e1a47a43a259b53db431cc8724d40e`。このcommitはT603 testとfocused CI stepを先に追加している。

GitHub APIでこのSHAに一致するworkflow runのみを確認した。

- Run: `31929190714`
- Actual job: `95121366762`
- `head_sha`: `3c88a86bd0e1a47a43a259b53db431cc8724d40e`
- Result: failure
- Build / typecheck / architecture / lint / existing unit / T602: success
- Newly added `T603 schema migration and corruption recovery tests`: failure
- Failure diagnostics collection/upload: success
- Actual artifact: `9258779700`
- Artifact name: `ci-failure-diagnostics-31929190714-1`

artifactを実際に取得して確認した。少なくとも以下を含む。

- `test-output/ci/test-t603.log`（6 tests / 0 pass / 6 failとstack trace）
- build/typecheck/architecture/lint/unit/T602等の各log
- `environment.txt`
- `git-status.txt`
- `generated-files.txt`
- `src/`, `test/`, `dist/`, `test-dist/`
- package/tsconfig/eslint/workflow等の調査用context

workflowは各commandを`2>&1 | tee`で記録するため、標準出力・標準エラーを統合logへ保存する。RevMemのfailure diagnostics artifact要件は満たしている。

### 4.2 Reviewed implementation HEADのCI

Reviewed implementation HEAD `6a7d66e2cfbdfe226b8e958151b318ced83e6d13` に**完全一致**するrunだけを最終技術CIとして確認した。

- Run: `31930532442`
- Job: `95124484220`
- `head_sha`: `6a7d66e2cfbdfe226b8e958151b318ced83e6d13`
- Result: **success**
- Build: success
- Contract typecheck: success
- Architecture validation / negative contract: success
- Lint: success
- Unit tests: success
- T602 focused tests: success
- T603 focused tests: success
- T403/T404/T304/T502/T503/T504/T505 focused suites: success
- Temporary Git integration: success
- Mock GitHub integration: success
- VS Code Extension Host: success

別SHAのrunは代用していない。

CI成功は現在存在するtestが成功したことを示すが、以下のrequired findingsは未試験の破損境界・migration contract・永続metadata整合性をsource/direct-dependency reviewで検出したものである。

## 5. Required findings

### T603-R001 — high — 不完全なpreflight validationにより、構造破損後も旧cacheの確認済みstateを露出できる

- Origin: `introduced_by_change`
- Location:
  - `src/adapters/state-repository/persistence-schema-recovery.ts`: `validateContextDocument`, `validateGlobalDocument`, `validateManifestDocument`
  - `src/adapters/state-repository/validated-file-system-review-state-repository.ts`: `load`, `prepareTarget`
  - Direct dependencies: `file-system-review-state-repository.ts`, `coherent-file-system-review-state-repository.ts`
- Description:
  - T603 preflightはcontextについてschema/repositoryId/contextId/files内schema程度しか検証せず、`kind`、descriptor、timestamp、fileの必須field/range等をcanonical contractとして検証しない。
  - `prepareTarget`はpreflightが`ready`なら`uncertainTargets`からkeyを削除する。
  - その後の`super.load`が失敗しても、`ValidatedFileSystemReviewStateRepository.load`はtargetを再びuncertainへ戻さない。
  - low-level repositoryはload失敗時に以前のin-memory cacheを消去しない。
- Reproduction class:
  1. valid workspace stateをloadしてcacheへ確認済みrangeを入れる。
  2. persisted current-schema JSONを構造破損させる。例: `contextState.kind`を`workspace`から`branch`へ変更する。
  3. preflightはこのfieldを検証しないため`ready`を返し、uncertain flagを解除する。
  4. downstream loadはtarget-kind不一致でrejectするが、旧cacheは残る。
  5. `getCurrent()`はuncertain flagが無いため旧reviewed stateを返せる。
- Impact: persisted dataが不確実なのに以前の確認済み範囲を表示でき、T603終了条件とAC-24「不確実な範囲を表示しない」に直接反する。
- Evidence: current focused testはmalformed JSONの初回loadだけを検証し、valid load後のcurrent-schema structural corruptionとstale cacheを検証していない。direct low-level validatorsもdeep schemaを完全検証しない。
- Required action:
  - `ready`を返す前に保存model全体をcanonical runtime validationする、またはpreflight後の全downstream validation/load failureでtargetを必ずuncertain化してcacheを非表示/破棄する。
  - valid load → current-schema structural corruption → load failure/quarantine → `getCurrent() === undefined` の回帰testを追加する。
  - duplicate manifest context、missing `updatedAt`、deep file shape/range破損等も含めてfail-closedを固定する。

### T603-R002 — high — v0→v1 stepがcurrent定数へ結合され、次回schema bumpでadjacent migration chainが壊れる

- Origin: `introduced_by_change`
- Location:
  - `src/adapters/state-repository/persistence-schema-recovery.ts`: v0→v1 context/global/workspace/manifest steps
  - `src/adapters/state-repository/jsonl-review-history-store.ts`: `HISTORY_MIGRATION_STEPS`
  - `src/adapters/non-git-snapshots/node-non-git-snapshot-adapters.ts`: `SNAPSHOT_MIGRATION_STEPS`
- Description:
  - 各stepは`fromVersion: 0`, `toVersion: 1`と宣言しているが、migrate結果の`schemaVersion`には固定値`1`ではなく`REVIEW_RANGE_SCHEMA_VERSION`を代入する。
  - `runSchemaMigrationChain`はstep実行後に`current.schemaVersion === step.toVersion`を要求する。
  - 将来current schemaが2となり1→2 stepを追加すると、既存0→1 stepがschemaVersion 2を生成して`toVersion: 1`検査で即失敗する。
- Impact: helper commentやimplementation reportが意図する「後続versionではadjacent stepを追加する」方式が最初のschema bumpで成立せず、v0のstate/history/snapshotをv0→v1→v2へ段階移行できない。T603のmigration chain基盤として将来互換性がない。
- Evidence: state/history/snapshotの全v0→v1 production stepが同じcurrent定数を使用している。現在のtestはcurrent=1だけなので検出できない。
- Required action:
  - 各historical migration stepはそのstep固有のtarget version（このstepなら`1`）を生成し、global current定数へ依存させない。
  - state/history/snapshotそれぞれで複数stepを通過できる回帰test、またはcurrent targetをinject可能なchain testを追加する。

### T603-R003 — medium — repository manifestだけ全context referenceをv1へ進め、未選択context documentはv0のまま残る

- Origin: `introduced_by_change`
- Location: `src/adapters/state-repository/persistence-schema-recovery.ts`: `MANIFEST_STEPS`, repository branch of `preparePersistedReviewState`
- Direct contract: `src/adapters/state-repository/contracts.ts` `RepositoryStateManifestContextReference.schemaVersion`
- Description:
  - manifest v0→v1 migrationは`contexts`配列の**全reference**へcurrent schemaVersionを設定する。
  - 一方、repository preparationはowner-globalと**現在targetのcontextだけ**を読み、migrationする。targetがmanifestに無い場合はcontext documentを1つもmigrationせず、globalとmanifestだけをpublishできる。
  - その結果、未訪問contextの実体fileはv0のままなのにmanifest referenceはv1を宣言する。
- Impact: `RepositoryStateManifestContextReference.schemaVersion`は「referenced immutable documentのschema version」という既存contractだが、migration後manifestがその事実を偽る。subsequent reader/maintenanceがreference metadataを信頼できず、整合性がlazy repairに依存する。
- Evidence: focused T603 testはworkspace stateのみで、repository-style multi-context legacy manifestを検証していない。
- Required action:
  - current manifestをpublishする前に全referenced context documentをmigration/validateしてreferenceと実体versionを一致させる、またはmixed-version manifestを正しく表現できる方式へ設計する。
  - 複数legacy context、target absentを含むfixtureで、publish後の全reference schemaVersionと参照先documentのschemaVersionが一致するtestを追加する。

### T603-R004 — medium — legacy root migrationがnested future schemaを上書きしてdowngradeする

- Origin: `introduced_by_change`
- Location: `src/adapters/state-repository/persistence-schema-recovery.ts`: `migrateContextV0ToV1`, `WORKSPACE_STEPS`, `MANIFEST_STEPS`
- Description:
  - legacy rootをv0→v1へ移行するとき、nested file、workspace context/global、manifest context/global referenceの`schemaVersion`を内容確認なしでcurrentへ上書きする。
  - 例としてroot schemaVersion 0のcontext内にschemaVersion 2のfileが存在しても、future/unsupportedとしてrejectせずschemaVersion 1へ書き換える。
- Impact: newer/unsupported nested dataを古いv1 modelとして再解釈し、active persistenceへpublishできる。future schemaをcorruptionと混同せず保全・rejectするという実装方針と、安全な段階移行の前提に反する。
- Evidence: nested modelごとのsource versionをmigration chainへ通さず、親stepが直接上書きする。nested future schema testは無い。
- Required action:
  - nested保存modelのversionも個別に検証/migrateし、currentより新しいversionは変更せずunsupportedとしてrejectする。
  - root v0 + nested v2のcontext file、workspace nested state、manifest referenceについてregression testを追加する。

### T603-R005 — medium — malformed schemaVersionをfuture/unsupportedと誤分類してquarantineせず、snapshotのnullはlegacy扱いになる

- Origin: `introduced_by_change`
- Location:
  - `src/adapters/state-repository/persistence-schema-recovery.ts`: `runSchemaMigrationChain`, `decodeOrQuarantine`
  - `src/adapters/state-repository/jsonl-review-history-store.ts`
  - `src/adapters/non-git-snapshots/node-non-git-snapshot-adapters.ts`: `parseMigratedRecord`
- Description:
  - `runSchemaMigrationChain`はschemaVersionがstring、negative、non-integer等でも`UnsupportedPersistedSchemaVersionError`を投げる。
  - recovery側はこのerrorだけを「future/unsupported」とみなしてquarantineせずrethrowするため、明らかなschema metadata破損が隔離されない。
  - snapshotは`value.schemaVersion ?? 0`相当のfallbackを使うため、明示的な`schemaVersion: null`まで「schemaVersion missingのlegacy v0」として受理・rewriteする。
- Impact: corrupt state/history/snapshotが毎回operationを停止させ続け、T603の破損隔離・回復が働かない。snapshot nullは逆に破損をlegacy dataへ補完してしまう。
- Evidence: testsはbroken JSONのみで、invalid schemaVersion type/range/nullを検証していない。
- Required action:
  - validなnon-negative integerかつcurrentより大きいversionだけをunsupported/futureとして保存する。
  - type/range不正はcorruptionとしてquarantineする。
  - missing-version legacy fallbackは`undefined`のみに限定し、`null`等をlegacyとして補完しない。
  - state/history/snapshot全てにinvalid schema metadata testを追加する。

### T603-R006 — medium — snapshot payload本体の破損は検出されても隔離・除去されない

- Origin: `introduced_by_change`
- Location:
  - `src/adapters/non-git-snapshots/node-non-git-snapshot-adapters.ts`: `get`
  - Direct dependency `src/application/non-git-snapshots/index.ts`: `NonGitSnapshotTracker.read`
- Description:
  - storage adapterはwrapper JSONと`createdAt`、`bytes`がstringであることだけを確認し、`Buffer.from(bytes, "base64")`でpayloadを返す。base64 decoderは不正文字に対して厳密rejectしない。
  - gzip破損、payload hash不一致、snapshot envelope破損はapplication trackerで後から`corrupt`として検出される。
  - trackerはこの場合reviewed rangeを返さない点は安全だが、underlying persisted snapshot fileをquarantine/deleteしない。
- Impact: T603の「snapshot破損検出・隔離・回復」はmalformed wrapper JSON/fieldには適用されるが、snapshotデータ本体の破損には適用されない。破損entryがactive storageに残り、latest pointerから繰り返し選択・読取される。
- Evidence: focused testは`"{broken-snapshot"`のwrapper JSON破損のみ。valid wrapper + damaged compressed payload/hash/envelopeのtestは無い。
- Required action:
  - snapshot payload integrityを検証できるpersistence/recovery boundaryを設け、gzip/hash/envelope破損時もactive entryをquarantineまたは安全に除去する。
  - latest pointerとの整合もfail-closedに扱う。
  - valid wrapper + invalid base64/compressed bytes、hash mismatch、invalid envelopeのregression testを追加する。

### T603-R007 — medium — implementation report/handoff/PRの永続証跡がGitHub上の実体と複数箇所で一致しない

- Origin: `introduced_by_change`
- Location:
  - `reports/issue-1-t603-implementation-20260816145900.md`
  - `handoffs/issue-1-t603-implementation-20260816150000.yaml`
  - PR #53 body / implementation completion comment
- Description: current HEADのGitHub APIでworkflow jobs/artifacts/commitsを照合すると、implementation evidenceに複数の誤ったID/SHA/commit messageが記録されている。
- Evidence:
  - Red commit `3c88a86...` actual message: `test: define T603 migration recovery behavior`; reportは`test: add T603 migration and recovery coverage`と記載。
  - Run `31929190714`: actual job `95121366762`, artifact `9258779700`; report/PR/handoffはjob `95120961967`（GitHub APIで404）、artifact `5717201218`。
  - Run `31929450198`: actual job `95121963562`, artifact `9258848153`; artifact記載は`5717346948`。
  - Run `31929675591`: actual job `95122502746`, artifact `9258909583`;記載はjob `95122672378`, artifact `5717460644`。
  - Run `31929798612`: actual job `95122794408`; reportは`95123012762`。
  - Run `31929935759`: actual job `95123109494`; report/PRは`95123412884`。
  - handoffのimplementation commits 2/3は`8e5851...` / `9dda6c...`と記載するが、PRのactual commitsは`5fa3b0c2c86f335851fb0b4dd29c0364c5a6133a` / `c77e375cab5e17787d7c8e36ca5b869982276f87`。
  - current reviewed HEADのrun `31930532442` / job `95124484220`は正しく記録されている。
  - PR本文は次工程を「独立review」とするが、handoffは別normal reviewを要求し、independent-final report pathも予約されていない。
- Impact: next workerが存在しないjob/artifactや誤ったcommit identityを追跡し、exact-head CIやreview continuity判定を誤る可能性がある。report-writer/handoffのevidence-faithful要件を満たさない。
- Required action:
  - GitHub API上のauthoritative値でimplementation report/handoff/PR evidenceを訂正する。
  - review lifecycle記述をnormal review → finding closure → reserved independent finalの順に揃える。
  - finding修正後のimplementation follow-up report/handoffでは、その時点の新current implementation HEADとそのSHAに一致するCI runのみを最終証跡とする。

## 6. Coverage disposition

| Criterion | Disposition | Evidence / finding |
| --- | --- | --- |
| 要件・設計適合 | `checked_finding` | R001〜R006 |
| correctness / edge cases | `checked_finding` | R001〜R006 |
| scope / unrelated changes | `checked_no_finding` | current net diffはT603実装・test・CI focused step・required artifactsに限定 |
| changed files / direct dependencies | `checked_finding` | 9変更fileと主要direct depsを確認、R001〜R006 |
| API / data / config / workflow / compatibility | `checked_finding` | R002〜R005。CI diagnostic workflow自体は適合 |
| error handling / diagnostics | `checked_finding` | R001, R005, R006。failure artifact自体は必要情報を保持 |
| security / secret handling | `checked_no_finding` | T603差分でtoken/source本文を新規診断logへ出す変更なし |
| tests / validation adequacy | `checked_finding` | exact-head CIはgreenだがR001〜R006の境界testが不足 |
| exact-head CI | `checked_no_finding` | `6a7d66e...` = run `31930532442` head SHA、success |
| report / handoff / PR evidence | `checked_finding` | R007 |
| task tracking | `checked_no_finding` | `tasks/tasks-status.md`は更新権限ルールに従いPRでは未変更。本reviewerも変更しない |
| regression / maintainability | `checked_finding` | R002, R003, R004 |
| TDD / diagnostic artifact policy | `checked_no_finding` | test-first Redをexact-headで確認し、failure artifact内容も実確認 |

`unexplored`: なし。

## 7. Held / task boundary

### H001 — T604: cross-window/process lock、atomic history append、cache/snapshot cleanup

T604が明示的に複数window競合、atomic history append、cache・snapshot整理を所有するため、本reviewではrequired findingにしない。T603が作るmigration backup / quarantine sidecarの長期cleanup・容量管理もT604で扱うべきremaining riskとして保持する。

### H002 — 将来schema v2の具体的semantic transform

現時点でv2 contractは存在しないため、具体的なv1→v2 transform内容は将来schema bump taskにheldする。ただし、R002の「既存v0→v1 stepが将来current定数へ結合されている」問題は現在のchain設計そのものの欠陥なのでT603 required findingである。

## 8. Additional remaining risks

- `quarantinePersistedText`はquarantine writeをinjected `AtomicTextFileStore`で行う一方、active originalの削除は直接`node:fs rm`を使う。production default Node storeでは動くが、custom store/test seamとI/O abstractionの不一致が残る。現時点ではrequired findingには昇格していない。
- migration/quarantine sidecarのretention/容量上限はT604 cleanup完了までboundedではない。

## 9. Verdict

**fail**

理由: required findings 7件（high 2、medium 5）が残る。特にR001は不確実なpersisted stateからstaleな確認済みcacheを再露出できるためT603の安全性終了条件を満たさず、R002はschema migration chainを次回version bumpで破壊する。

Current reviewed implementation HEADのexact-head CIは成功しているが、CI successはこれら未カバーのcontract/edge caseを否定しない。

## 10. 次の作業

1. implementation workerがT603-R001〜R007を全て修正する。
2. behavior findingsはTDDでregression testを先に追加し、Redを確認してから実装修正する。
3. implementation follow-up report/handoffとPR evidenceを更新する。
4. 新implementation HEADに一致するworkflow runのみでCIを判定する。
5. 同じnormal review chatを利用可能ならfinding fix verificationを行い、全finding closure後に独立最終review用report pathを予約してからfresh reviewerへ引き渡す。

Mergeは利用者が行うため、本reviewerはmergeしない。
