# Sub-agent実行レポート

## タスク

- 目的: Issue #78 / T610の固定実装commit `faf3c8ea98ce3ad7e26daef12b6688c8b01abaef`を、`origin/main` merge-base `477725632177f5c4fcbca5eb587644fdef06e4df`からの全差分、直接依存、consumer、production composition、test、設定、CI証跡まで通常reviewする。
- タスク種別: fresh exhaustive normal review

## sub-agentを使う理由

- 理由: 実装ownerから分離されたfresh normal reviewerとして、既存reportの結論に依存せず、immutable targetの全criterionを独立にdispositionするため。

## 対象範囲

- 対象: `origin/main...faf3c8ea98ce3ad7e26daef12b6688c8b01abaef`の20 changed paths、Issue #78、PR #83、design rev6 §§11.3/16.5/16.8-16.10/17-20、T610とP6 tracking、folder scope controller/store/enumerator、T505 source、T305 production factory/extension event path、Global Tree runtime/model、package/menu/config/CI wiring、T610/T505/UI test、workspace identity・canonical path・background recalculator・atomic storage/lockの直接依存、T505/T607/T609 regression、local/CI/report evidence。

## 対象外

- 対象外: production/test/design/trackingの修正、test/build/lintの再実行、commit、push、CI待機、GitHub write、merge、nested agent。唯一のwriteは本固定reportのplaceholder置換。

## 実行コマンド

- 実行コマンド: `Get-Content`、`rg`、`Select-String`、`Get-ChildItem`による指定Skill・AGENTS・設計・tracking・report・source・test・直接依存のread、`git status --short --branch`、`git rev-parse`、`git merge-base`、`git diff --name-status/--stat/--unified=0`、`git show`、`git log`によるtarget固定と全diff確認、`gh issue view 78`、`gh pr view 83`、`gh run view 32568991178/32568988847 --json/--log-failed`によるread-only GitHub/CI確認、PowerShell JSON/count照合によるpackage/menu/config/CI exactly-once確認。test/build/lint/diff-checkは再実行していない。Markdown lintは`tools/lint/`と`lint:md` wiringがないため`unsupported`。

## 対象ファイル

- 変更または確認したファイル: changed 20/20: `.github/workflows/ci.yml`、`doc/design/vscode-review-range-tracker-design.md`、`package.json`、T610 design/implementation/closure reports、`src/adapters/repository-files/node-repository-file-path-enumerator.ts`、`src/adapters/state-repository/node-folder-understanding-stopped-store.ts`、`src/application/global-understanding/folder-understanding-scope-controller.ts`とbarrel、`src/t305-extension.ts`、`src/t305-global-understanding-composition.ts`、`src/t505-global-understanding-source.ts`、Global Understanding UI model/runtime/barrel、tasks/phases、T505/T610 tests。direct dependencies/consumers: current-context runtime/selection、selected review context、workspace identity/canonical relative path、PR HEAD registry、Global background recalculator/cache、atomic text store、storage route/lock、Global UI/T505/T607/T609/Extension Host test wiring、`Design/BreakingChanges.md`。固定report以外は変更していない。

## 指摘事項

- 指摘要約または「指摘なし」: 10件（High 5、Medium 4、Low 1）。全件originはnormal review、全件required、severity reclassificationなし。

  **T610-NR-001 — High — controller未指定consumerをdirect-root列挙へ変え、exact-head CIを破壊している**

  - 場所: `src/t505-global-understanding-source.ts:108`、`src/t505-global-understanding-source.ts:131`、`test/unit/t505-global-understanding-source.test.ts:107`
  - Evidence: `folderScopes`未指定時も`activeFolders`を`[""]`とし、その後は旧`enumerate()`ではなく`enumerateDirectFolders()`を使うため、nested opened evidenceが候補から消える。変更testは未open件数を2から1へ下げたが、他のdirect consumersは更新されていない。exact-head pull_request run `32568991178`とpush run `32568988847`はいずれもUnit 605件中4件失敗し、Issue #66 Windows path identity、T505 retained evidence、PR69 owner-pinned open target、stale-node command regressionが再現した。build/contracts/architecture/lintはGreenだが後続focused/Hostはskip。
  - Impact: required CIがredで、T505/T609の既存公開挙動とconsumer contractが破壊される。T610 focused結果だけではmerge可能性を示せない。
  - Required action: controllerなしlegacy consumerの互換経路を維持するか、controllerを必須化して全consumerを正しいscope activationへ移行し、弱めた期待値を戻す。4 regressionそれぞれをproduction behaviorでGreenにし、full local equivalenceとexact-head required CIを取得する。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | optional-controller経路を互換化または廃止契約化 | 4 CI regressionとnested legacy consumerを固定 | 全T505 constructor consumerをproduction同等scopeへ配線 | Unit/focused/full gateとexact-head CI Green | T610/PR evidenceにconfirmed regression closureを記録 |

  **T610-NR-002 — High — stopped ancestorをfile openが越え、parent stop/resumeがsubtreeを復元できない**

  - 場所: `src/application/global-understanding/folder-understanding-scope-controller.ts:63`、`src/application/global-understanding/folder-understanding-scope-controller.ts:90`、`src/application/global-understanding/folder-understanding-scope-controller.ts:100`、`src/adapters/repository-files/node-repository-file-path-enumerator.ts:170`
  - Evidence: `openFile()`はexact folderの`stopped`だけを確認し、stopped ancestorを確認しない。`stop()`は既知descendant全件を`stopped`へ変更して全件をmarkerとして保存する一方、`resume()`は選択scope 1件だけを`running`へ戻す。そのためparent stop後のfile openは新しいdescendantをactiveにでき、parent resume後は以前のdescendantが独立markerとして残る。またexplicit/auto subtreeはstopped markerをcontrollerへ渡す前に`enumerateSubtreeFolders()`でsubtree全体を再帰するため、stopped descendantの再帰skipを実現していない。
  - Impact: 「stopped subtreeはfile openで自動再開しない」「resumeは同じ位置から再検証」「stopped descendantは読込も再帰もしない」というprivacy/lifecycle contractを破る。
  - Required action: inherited stopとexplicit markerを分離し、open/start/enqueue前に全ancestor markerを確認する。resume時は選択markerだけを外してsubtreeを新generationで再構成し、独立停止descendantだけをskipする。enumeratorへprune predicateまたはstopped setを渡す。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | ancestor-aware stop/inherited state/pruned subtree queue | parent stop→descendant open、stop→resume、独立child stopを追加 | actual factoryとruntime commandでsubtree lifecycleを通す | focused、integration、Hostでprivacy境界を確認 | finding IDとmarker semanticsをdesign/report/tasksへ同期 |

  **T610-NR-003 — High — stopped markerが停止直後とrestart後にTreeへ表示されない**

  - 場所: `src/t505-global-understanding-source.ts:108`、`src/t505-global-understanding-source.ts:109`、`src/ui/global-understanding/vscode-global-understanding-runtime.ts:340`
  - Evidence: restore後にstopped recordしかない通常restartでは`activeFolders.length === 0`となり、`snapshots()`を作る前にfoldersなしの`emptySnapshot()`を返す。停止commandも保存後に`invalidate()`するだけでpresentationをclearし、stopped snapshotをrefreshしない。
  - Impact: 中核受け入れ条件であるrestart後のstopped強調と同一rowの`再開`actionが消え、停止直後もViewが空になる。
  - Required action: active workが0でもrestored stopped/ancestor rowsをsnapshotへ含め、stop保存成功後にcurrent identityへ束縛したrefreshを行う。保存失敗時は旧表示を成功状態へ進めない。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | empty-active snapshotとstop refreshを修正 | actual store restart、only-stopped、running中stop UI test | exported T305 runtime/Tree providerで停止と再起動を実行 | focused integrationとExtension Host restart Green | restart evidenceとunsupported Host状態を更新 |

  **T610-NR-004 — High — folder階層・recursive aggregate・partial repository表示が実装されていない**

  - 場所: `src/application/global-understanding/folder-understanding-scope-controller.ts:162`、`src/application/global-understanding/folder-understanding-scope-controller.ts:168`、`src/ui/global-understanding/vscode-global-understanding-runtime.ts:145`、`src/ui/global-understanding/vscode-global-understanding-runtime.ts:176`、`src/ui/global-understanding/global-understanding-ui-model.ts:268`
  - Evidence: `aggregate()`はdirect childの`child.total`だけを足し、child自身のaggregateを使わないためgrandchild totalとgrandchild incomplete stateがancestorへ伝播しない。ancestor recordも自動生成されない。Tree providerは全folderをroot childrenへflat配置しfolder itemを`CollapsibleState.None`にする。summary/statusはpartial情報を持たず常に`リポジトリ全体`の百分率を表示するため、active scopeだけの既知分母をrepository全体と誤表示する。
  - Impact: Issue #78のfolder hierarchyと「direct + complete child」「unknown childを持つparentはpartialでratio非表示」の正確性contractが成立しない。
  - Required action: ancestor chainとdirect-child relationをmodel化し、recursive/current child aggregateをgeneration単位でpublishする。Treeを親子構造へし、root/partial scopeとStatus Barはcompleteになるまでrepository全体ratioを表示しない。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | recursive aggregate、ancestor rows、hierarchical Tree、partial summary | 3階層、inactive/stopped/failed grandchild、root fileを追加 | actual TreeDataProvider/Status Barをproduction sourceへ接続 | focused UI、T607 incremental、Host Tree assertions | design acceptance matrixとT610 reportを訂正 |

  **T610-NR-005 — High — scope/action identityがcanonical URI・path・generationへ束縛されず、別rootまたはroot外へ作用できる**

  - 場所: `src/application/global-understanding/folder-understanding-scope-controller.ts:27`、`src/application/global-understanding/folder-understanding-scope-controller.ts:28`、`src/t505-global-understanding-source.ts:21`、`src/ui/global-understanding/global-understanding-ui-model.ts:41`、`src/ui/global-understanding/vscode-global-understanding-runtime.ts:150`、`src/adapters/state-repository/node-folder-understanding-stopped-store.ts:14`
  - Evidence: owner keyは`repositoryId + repositoryRoot` filesystem stringのみで、URI scheme/authority/path semanticsを持たない。folder normalizationはslash置換と外側slash削除だけで`..`、`.`、重複segment、Windows caseをcanonicalize/rejectしない。storeも任意string markerを受理する。enumeratorはその値を`path.join(repositoryRoot, ...)`するため、corrupt markerをresumeするとroot外を列挙できる。Windowsではevidence pathをlowercase化した後もfolder keyは元caseのためdirect filterが一致しない。Folder node/actionはrepository root identityとgenerationを持たずpath stringだけをcommandへ渡し、context switch後のstale rowが現在ownerの同名folderへ作用する。Command Paletteは引数なしならno-opで、設計要求のselected current-generation nodeも保持しない。packageのeditor context menu登録も0件だった。
  - Impact: multi-root/Remote/Windowsでmarker混在、誤scope操作、誤集計、privacy境界外enumerationが起こり得て、stale action rejectionもない。
  - Required action: 既存workspace identity/canonical repository-relative pathを利用し、scheme/authority/path semanticsを含むopaque scope IDとgenerationをnode/commandへ運ぶ。全marker/action pathをcanonical validationし、root containmentをI/O直前にも検証する。Palette/editor menuはcurrent selected nodeからtyped targetを生成し、stale/別rootを拒否する。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | URI+semantics+canonical path+generation identity | Windows case、remote authority、`..` marker、context switch stale action | actual VS Code Uri/multi-root command pathを通す | T605/T609/architecture/Host regression Green | identity schemaとBreakingChanges/marker compatibilityを記録 |

  **T610-NR-006 — Medium — active scopeのdirectory/file-open変更が自動再計算されない**

  - 場所: `src/t305-extension.ts:600`、`src/t305-extension.ts:671`、`src/t505-global-understanding-source.ts:207`
  - Evidence: production compositionはtext change/save/close等ではrefreshするが、file create/delete/renameまたはdirectory entry用のwatcherを登録していない。`onDidOpenTextDocument`は`observeFileOpen()`をfire-and-forgetするだけでGlobal refreshを要求せず、background openやactivation時点で既にopenのdocumentは別eventがなければ計算されない。
  - Impact: active scopeがdirectory entry・open対象の変化へ追従せず、手動refreshまで古いtotal/Treeを残す。
  - Required action: validated owner/scopeへ限定したfilesystem/document lifecycle通知をcoalescerへ接続し、open observation完了後に最新generation refreshを要求する。inactive/stopped siblingはenqueueしない。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | scoped watcherとopen-refresh lifecycle | create/delete/rename/background-open/startup-openを追加 | T305 actual event listenerとcoalescerを使用 | focused integration/Hostで自動更新を確認 | automatic-recalc evidenceをT610 trackingへ同期 |

  **T610-NR-007 — Medium — stopped storeがT604 atomic/lock/failure contractを迂回し、失敗を無言で成功扱いする**

  - 場所: `src/adapters/state-repository/node-folder-understanding-stopped-store.ts:14`、`src/adapters/state-repository/node-folder-understanding-stopped-store.ts:17`、`src/adapters/state-repository/node-folder-understanding-stopped-store.ts:26`、`src/t505-global-understanding-source.ts:176`、`src/ui/global-understanding/vscode-global-understanding-runtime.ts:331`
  - Evidence: loadはENOENT以外のJSON破損・permission・I/Oも全て`[]`へ無言変換する。saveは固定`${file}.tmp`、plain write、renameで、既存`AtomicTextFileStore`のunique temp、exclusive create、fsync、cleanup、root containmentとT604 storage-root lockを使わない。cross-window lost update/temporary collisionを防げない。scope read/recalc errorもsourceが`failed`へ変えて吞み込み、runtimeはoperationをOKで終える。start/stop/resume commandは共通error boundaryで包まれていない。
  - Impact: stopped markerの耐久性・cross-window整合が保証されず、corruption/save/enumeration failureがOutputへ残らないかunhandled command rejectionになる。
  - Required action: shared routed atomic store/lease lockとprivacy-safe diagnosticsを再利用し、invalid markerをmissingと区別してfail-closed報告する。command/recalc failureを共通feedback boundaryへ通し、保存成功前にUI成功状態をpublishしない。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | shared atomic/lock/diagnostic store route | corruption、permission、disk-full、concurrent windows、tmp cleanup | startup feedbackとruntime command boundaryへ配線 | T604/T606 focused、integration、failure artifact Green | storage schema/failure evidenceとrisksを更新 |

  **T610-NR-008 — Medium — folder enumeration/projectionがT607のbounded-stage contractを満たさない**

  - 場所: `src/adapters/repository-files/node-repository-file-path-enumerator.ts:138`、`src/adapters/repository-files/node-repository-file-path-enumerator.ts:209`、`src/application/global-understanding/folder-understanding-scope-controller.ts:162`、`src/t505-global-understanding-source.ts:119`
  - Evidence: `readdir()`がdirectory全entryを一括materializeし、file-heavy directoryのloopにはitem-budget yieldがない。subtree discoveryは全path配列を保持して再帰する。`snapshots()`は各recordごとに全recordをscanする`aggregate()`を呼ぶためO(scope²)。sourceはactive folderごとに全open evidence copyと全persisted Global path projectionを繰り返す。T610 testはenumeration/folder projectionのbatch accounting、yield、memory、stale stageを測っていない。
  - Impact: large repository/subtreeや多数scopeでExtension Hostを長時間占有し、T607で固定したincremental UI/large-workload保証を後退させる。
  - Required action: enumeration・folder discovery・ancestor aggregationを明示item budgetとevent-loop yieldでchunk化し、single-pass/tree-index aggregationとscope-local evidence projectionを使う。stale/cancel後のstage/cache/tree publishを測定fixtureで拒否する。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | bounded enumerator/indexed aggregate/重複projection削減 | large flat dir、deep subtree、多scope、cancel accounting | production scheduler/accountWorkを実factoryへ接続 | `test:t607`含むdeterministic performance gate Green | before/after budgetとmemory evidenceをreportへ記録 |

  **T610-NR-009 — Medium — focused fixtureがactual extension compositionを通さず、closure reportの証明範囲が過大**

  - 場所: `test/unit/t610-folder-understanding.test.ts:82`、`test/unit/t610-folder-understanding.test.ts:119`、`reports/issue-78-t610-implementation-closure-20260822194055.md:34`
  - Evidence: 「exported T305 composition」testはfactoryを直接生成しsource methodを直接呼ぶだけで、`activate()`、`onDidOpenTextDocument`、TreeDataProvider、command handlers、configuration event、actual VS Code Uri、restartしたstoreを通らない。nested aggregate、parent stop/resume、ancestor stop、root open、actual multi-root/restart/UIはassertしない。T610専用Extension Host runnerは存在せず、exact-head CIではUnit失敗後にT610/T607/T609/Host stepsがskipした。それでもclosure reportはroot、restart、multi-root、partial ancestor、同一row action、stale nonpublicationをsemantic matrix Greenと記載する。
  - Impact: composition wiringの欠陥を検出できず、review/tracking/PRが存在しないevidenceを完了扱いする。
  - Required action: actual exported activation/runtime fixtureとT610 Host selectorを追加し、各claimを実際のboundaryでassertする。既存reportは履歴として残し、follow-up report/tracking/PR bodyで証明範囲とCI failureを訂正する。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | finding fixesのactual seamsをtest可能に保持 | design §20.1-20.4 matrixを欠落なく追加 | activate→events→commands→Tree/store restartを実行 | focused/integration/Host/full/exact-head CI Green | closure reportの過大claimをerratumとして同期 |

  **T610-NR-010 — Low — public API documentationとBreakingChanges記録が不足している**

  - 場所: `src/t505-global-understanding-source.ts:39`、`src/ui/global-understanding/vscode-global-understanding-runtime.ts:36`、`src/application/global-understanding/folder-understanding-scope-controller.ts:150`、`reports/issue-78-t610-design-20260822185714.md:30`
  - Evidence: 新規public dependencies `folderScopes`/`readAutoStartDescendants`、runtime sourceのstart/stop/resume、controllerの`state()`/`activeFolders()`に契約JSDocがない。既存consumer behaviorとGlobal policyを変更しexact-head regressionsも生じているのに、design reportは「BreakingChangesの直接更新根拠はない」とし、`Design/BreakingChanges.md`を更新していない。
  - Impact: external/fixture implementerがoptional behavior、identity、failure/cancellation semanticsを誤用し、既存利用者に変更境界が伝わらない。AGENTS.mdのbreaking-change記録ruleも満たさない。
  - Required action: 全新規public/protected APIへidentity・state・error・cancellation・ownership JSDocを追加し、repository-wide diagnosticsからfolder-scope policyへの移行とmarker schema/互換性をBreakingChangesへ記録する。

  | Production | Test | Composition | Validation | Tracking |
  | --- | --- | --- | --- | --- |
  | public API commentsを補完 | contract/type fixtureでrequired shapeを固定 | factory/consumer docsと実配線を一致 | build/contracts/Markdown dispositionを再確認 | BreakingChanges、design/report/tasksを同期 |

## 結果

- 結果: verdict `fail`。reviewed implementation HEADは`faf3c8ea98ce3ad7e26daef12b6688c8b01abaef`、base/merge-baseは`origin/main` / `477725632177f5c4fcbca5eb587644fdef06e4df`、rangeは`477725632177f5c4fcbca5eb587644fdef06e4df...faf3c8ea98ce3ad7e26daef12b6688c8b01abaef`。reviewerはimplementation/fixを行っていないfresh normal reviewer。verification capabilityは`local_execution_available`だが、review instructionによりlocal test/build/lint再実行は未実施。targetはcommitted/pushed、CI waitはcompleted-with-failureで、matching pull_request `32568991178`とpush `32568988847`はいずれもUnit 601/605 pass、4 fail。report attestationは対象外で、本normal reportはrepository fileとしてparent persistence待ち。

  Coverage disposition:

  - requirement/design conformance: `checked_finding`（T610-NR-002〜006、010）
  - correctness/edge cases/state/restart/partial: `checked_finding`（T610-NR-002〜005）
  - scope discipline/unrelated changes: `checked_no_finding`
  - changed files/direct dependencies/consumers/composition: `checked_finding`（T610-NR-001、005、007〜009）
  - API/data/config/menu/workflow compatibility: `checked_finding`（T610-NR-001、005、010）。設定、3 activation events、3 command contributions、3 mutually exclusive inline menu items、`test:t610` script、CI invocationは各exactly onceであること自体は`checked_no_finding`。editor context menu/current-node Palette contractはT610-NR-005。
  - cancellation/generation/stale publication: `checked_finding`（scope result accept fenceとsibling-local AbortSignalは`checked_no_finding`、stale UI action identityはT610-NR-005）
  - failure/security/privacy/persistence/resource lifecycle: `checked_finding`（T610-NR-002、003、005、007）
  - performance/T607 regression: `checked_finding`（T610-NR-008）
  - tests/validation/current-HEAD CI: `checked_finding`（T610-NR-001、009）
  - reports/tracking/documentation truthfulness: `checked_finding`（T610-NR-009、010）
  - T505/T607/T609 regression risk: `checked_finding`（T610-NR-001、005、008）
  - held: 0、unknown: 0、unexplored: 0、not applicable: secret exposure/network egressとreport attestation/merge。

## リスク

- 未解決のリスクまたは後続対応: 全10 findingがopenでmergeをblockする。parentはfindingごとに上記5-cell matrixをcompleteにし、production/test/composition/tracking fixを同一review targetへcommitした後、local routeのfocused/full equivalenceを実行し、同じnormal reviewerへfix verificationを依頼する必要がある。CI failureをbase failureまたはheldへ分類してはならない。Markdown word checkはrepo-local `tools/lint/`、`lint:md`、focused target wiringが存在しないため`unsupported`でありpassではない。通常review closure後もfresh independent review、report attestation、exact-head required pull_request CIが残る。mergeは実施していない。
