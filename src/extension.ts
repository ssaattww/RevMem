import type * as vscode from "vscode";

/** Activates the Review Range Tracker extension. */
export function activate(_context: vscode.ExtensionContext): void {
  void _context;

  // Extension services are registered by subsequent implementation tasks.
}

/** Deactivates the Review Range Tracker extension. */
export function deactivate(): void {
  // No disposable resources are created during bootstrap.
}
